# 타추위 추첨 프로그램 작업 계획서
### AI 개발 에이전트(Sonnet/Opus) 수행용 실행 계획

- 작성일: 2026-07-28
- 제출 마감: 2026-08-07(금)
- 상위 문서: `타추위-추첨프로그램_기획안.md` (설계 배경과 의사결정 근거는 기획안 참조. 충돌 시 본 계획서가 아니라 기획안의 원칙이 우선하며, 충돌을 발견하면 작업을 멈추고 사용자에게 보고)
- 개발 주체: Claude Sonnet / Opus 코딩 에이전트. 사용자는 리뷰어이자 의사결정자.

---

## 0. 실행 모드 — 단일 세션 순차 실행

이 계획서의 "Day N" 표기는 원래 여러 날에 걸친 개발 일정을 가정했으나, 실제로는 **단일 에이전트가 하나의 세션 안에서 순차적으로(병렬 서브에이전트 없이) 전 태스크를 실행**한다. 따라서:

- "Day N"은 달력 날짜가 아니라 **태스크 배치 순서**로 읽는다. Phase 순서(0→1→2→3→4→5)와 Phase 내 태스크 번호가 실제 실행 순서다.
- 각 태스크 완료 시 §7 운영 규칙에 따라 커밋한다. 게이트(A~D)는 여전히 유효하며, 판정 기준 미달 시 해당 계층을 축소·비활성화하고 다음으로 진행한다(사용자 승인 없이 진행 — 사전 위임됨).
- **실사용 환경 의존 DoD(실제 250대 폰 동시 접속, 실제 대형 스크린 실측, 실제 행사장 Wi-Fi)는 이 세션에서 물리적으로 검증할 수 없다.** 이런 항목은 스크립트 기반 시뮬레이션(예: asyncio WebSocket 클라이언트 250개로 부하 재현)으로 대체하고, 결과 보고 시 "시뮬레이션 기반"임을 명시한다. 실제 실측이 필요한 항목은 완료 보고에서 별도로 표시한다.
- LLM API 키가 없는 환경일 수 있으므로, MC Agent의 정적 폴백 경로가 항상 완전히 동작하는지가 실질적 완료 기준이다.

## 1. 목표와 산출물

**목표**: 250명 규모 타운홀 미팅에서 5분 안에 진행되는 AI 에이전트 기반 경품 추첨 웹 서비스.

**최종 산출물**:
1. 실행 가능한 서비스 (로컬 원커맨드 실행 + 선택적 클라우드 배포 주소)
2. 소스 코드 저장소 (현 저장소) + `THIRD_PARTY_NOTICES.md`
3. 서비스 소개·사용 방법 README (스크린샷/GIF 포함)
4. 원클릭 데모 모드 (가상 참가자 250명)

**연출 모드 2종** (공용 추첨 엔진 위에 얹히는 프레젠테이션 계층):
- **룰렛 모드** (안전망, 2순위까지의 제출 기본형): AI MC 해설이 붙은 룰렛. 카드 방식은 채택하지 않음(룰렛으로 확정).
- **레이싱 모드** (플래그십): 3라운드 부서 대항 서바이벌 퍼널 카트 레이스(연출은 최소화, §4.3 참조). Day 8 go/no-go 게이트 적용.

**참여형 예측 게임 계층** (레이싱 모드 위에 얹히는 가산 계층, 게이트 D 적용): 참가자가 QR로 개인 모바일 접속 → 확신도 100을 3라운드에 배분(사전 설정·수정 가능) → **각 라운드 선택 창(30초)에서만** 팀/카트 대상 선택, 미선택 시 무작위 배정 → 적중 시 점수 획득(감점 없음) → 리더보드. **도박 요소 없음**(차감·파산 개념 부재), **경품 추첨의 공정성에 영향을 주지 않으며**, 이 계층 전체가 죽어도 행사는 완주된다.

**화면 4종**: Stage(대형 스크린) / Mobile(개인 폰) / Admin(운영 콘솔) / Verify(공정성 검증).

---

## 2. 확정 기술 스택 (변경 금지 — 변경 필요 시 사용자 승인)

| 계층 | 선택 | 사유 |
|---|---|---|
| 백엔드 | Python **3.13 권장** (3.14 허용, 최소 3.11), FastAPI, uvicorn | WebSocket 지원, 단순 배포. 버전 기준은 "최신"이 아니라 "전 의존성이 확실히 지원": 3.13은 이 스택에서 완전 검증됨. 3.14는 개발 환경에서 `pip install -r requirements.txt`가 깨끗이 성공하면 사용 가능. 3.11은 하한선일 뿐 목표 버전이 아님 |
| 프런트엔드 | 순수 HTML/CSS/JS + Canvas 2D, **빌드 도구 없음** | 노드 툴체인 리스크 제거, 로컬 구동 단순화 |
| 실시간 동기화 | WebSocket (stage ↔ admin) | 운영 콘솔 조작이 무대 화면에 즉시 반영 |
| 난수/해시 | Python 표준 `secrets`, `hashlib`, `hmac` | 외부 의존성 없는 검증 가능성 |
| 명단 파싱 | CSV/클립보드 붙여넣기(기본), `openpyxl`(xlsx, 선택) | pandas는 사용하지 않음 (의존성 최소화) |
| LLM (MC 멘트) | `anthropic` SDK, 모델 `claude-sonnet-5` | 사전 생성·캐싱 전용. **없어도 완전 동작해야 함** |
| QR 코드 | `qrcode[pil]` 또는 클라이언트 JS 생성 중 택1 | 검증 페이지 연결용 |
| 테스트 | pytest | 공정성 엔진은 테스트 필수 |

**절대 원칙**:
- LLM API·네트워크가 전혀 없어도 추첨·연출·검증의 전 기능이 동작해야 한다 (MC 멘트는 정적 폴백으로 대체).
- 실명/사번을 외부 API로 전송하지 않는다. MC 멘트는 익명 템플릿 생성 후 로컬 치환.
- **예측 게임은 경품 추첨 결과에 어떤 영향도 주지 않는다.** 모바일 미참여자도 당첨 확률이 동일해야 한다. 이 원칙을 깨는 구현은 어떤 이유로도 허용하지 않는다.
- **예측 게임에 손실 구조를 만들지 않는다.** 점수는 단조 증가하며, 차감·잔액 부족·파산 개념을 도입하지 않는다. UI 문구도 "베팅/판돈/배당률/사이버머니"를 쓰지 않고 "예측/확신도/보너스/점수"로 통일한다 (사내 행사 적합성).
- **모바일에 프레임 단위 데이터를 전송하지 않는다** (§4.7 결정론적 로컬 재생). 250대 동시 접속을 전제로 설계한다.
- 의존성을 추가할 때마다 `THIRD_PARTY_NOTICES.md`에 라이선스를 즉시 기록한다.

---

## 3. 저장소 구조 (목표)

```
Lucky_draw_Service/
├── app/
│   ├── main.py               # FastAPI 앱, 라우팅, WS 허브
│   ├── config.py             # 설정 (포트, API 키는 .env)
│   ├── models.py             # 데이터 모델 (Participant, Session, RoundPlan, DrawResult)
│   │                         # ※ Participant.id = 사번(roster.py가 이미 "사번" 별칭으로 매핑), team = 부서. 추가 필드 불필요
│   ├── fairness.py           # Fairness Agent: 커밋-리빌, 점수 유도, 검증
│   ├── director.py           # Director Agent: 시간 예산, 라운드 통과 인원 산출, 페이싱
│   ├── mc.py                 # MC Agent: 멘트 템플릿, LLM 사전생성, 폴백
│   ├── predictions.py        # Prediction Agent: 예측 카드, 라운드 잠금, 분포 집계, 채점, 리더보드
│   ├── roster.py             # 명단 파싱 (CSV/붙여넣기/xlsx), 샘플 데이터 생성. id(사번)·name(실명)·team(부서) 별칭 이미 지원 — 필수 컬럼 강제만 추가
│   └── store.py              # 세션 상태 (인메모리 + JSON 파일 스냅샷으로 장애 복구)
├── static/
│   ├── stage.html / stage.js / stage.css      # 무대(대형 스크린) 화면
│   ├── mobile.html / mobile.js / mobile.css   # 참가자 개인 모바일 화면
│   ├── admin.html / admin.js / admin.css      # 운영 콘솔
│   ├── verify.html / verify.js                # 공정성 검증 페이지 (QR 연결 대상)
│   ├── roulette.js           # 룰렛 렌더러
│   ├── race/                 # 레이싱 렌더러 (engine.js, camera.js, karts.js) — stage·mobile 공용
│   └── shared.js             # WS 클라이언트, 공용 유틸
├── tests/
│   ├── test_fairness.py      # 결정론·검증·제외 로직·팀 편성 중첩 성질
│   ├── test_director.py      # 시간 예산·퍼널 인원 산출
│   ├── test_predictions.py   # 채점 순수성·재계산 일치·잠금·자동 규칙·배분 제약
│   └── test_roster.py        # 파싱 엣지 케이스
├── data/samples/participants_250.csv
├── run.py                    # 원커맨드 실행 (서버 기동 + 브라우저 오픈)
├── requirements.txt
├── THIRD_PARTY_NOTICES.md
└── README.md
```

---

## 4. 핵심 설계 스펙 (개발 에이전트가 임의 변경할 수 없는 계약)

### 4.1 Fairness 엔진 — 중첩 생존 집합(nested survivor sets) 방식

기획안 §2의 커밋-리빌을 다음 알고리즘으로 구현한다. **핵심 성질: 당첨자가 모든 라운드를 자동으로 통과한다** — 별도 유도 규칙 없이 수학적으로 보장된다.

```
1. 세션 시작 시:
   seed = secrets.token_hex(32)
   snapshot = canonical_json({
     session_id, participants: [{id, name, team}...],   # id 기준 정렬
     draw_count: N, excluded_ids: [...], created_at
   })
   commit = SHA256(seed + snapshot)                     # 화면에 사전 공개
2. 추첨:
   eligible = participants - excluded
   score(p) = HMAC_SHA256(key=seed, msg=p.id) 를 정수로 해석
   ranking = eligible를 score 내림차순 정렬
   winners = ranking[:N]
3. 라운드 통과자 (개별 단위, 순위의 상위 절단):
   F = clamp(max(5, 2N), 5, 10)                         # 결선 진출 목표 규모
   R1_pass = ranking[:100]                              # 제한 시간 내 통과
   R2_pass = ranking[:F]
   → winners(N) ⊂ R2_pass(F) ⊂ R1_pass(100)  자동 성립  (부서와 무관하게 항상 참)
4. 부서 집계 (표시·예측용, 생존을 결정하지 않음):
   부서 그룹 g 의 통과율 = |R?_pass ∩ g| / |g|          # 인원 차이 보정을 위해 '율'로 집계
   부서 순위 = 통과율 내림차순
5. 결선 진출자:
   finalists = {각 부서 그룹의 최상위 생존 카트} ∪ ranking[:N]
   → N=1 이면 전체 1위가 이미 자기 부서 최상위이므로 '부서별 대표'와 정확히 일치
   → N≥2 에서 상위 N이 같은 부서에 몰려도 합집합으로 포함되므로 모순 없음
6. 리빌:
   seed·snapshot 원본 공개 → 검증 페이지에서 commit 재계산 + 순위/통과자/부서 집계/결선 진출자 재유도
7. 재추첨/다음 세션: 새 seed로 1~6 반복. 당첨 이력은 store에 누적, "이미 당첨자 제외" 토글 시 excluded_ids에 반영
```

- 룰렛 모드도 동일 엔진 사용: winners를 룰렛이 순차 발표할 뿐이다(부서 집계는 사용하지 않음).
- 검증 페이지(`verify.html`)는 서버 API로 리빌 데이터를 받아 **브라우저 WebCrypto로 독립 재계산**한다 (서버를 믿지 않아도 되는 구조).
- **부서를 통째로 탈락시키지 않는다.** 부서는 실제 조직이므로 순위에서 유도할 수 없고, 부서 단위로 탈락시키면 그 부서에 속한 당첨자까지 탈락해 중첩 성질이 깨진다. 통과 판정은 항상 개별 카트 단위이며 부서 집계는 파생 지표다. **이 구분을 흐리는 구현은 금지한다.**
- **커밋 재생성**: 명단이 변경되면 snapshot이 달라져 기존 commit이 무효가 된다. 명단 변경 API는 커밋 존재 시 경고를 반환하고, 명시적 재커밋 후에만 진행한다.

### 4.6 예측 채점 엔진 (`predictions.py`) — 무손실·순수 함수

**도박 요소 없음**: 차감·잔액·파산 개념이 존재하지 않는다. 참가자는 확신도를 *배분*하고, 적중 시 점수를 *획득*한다. 점수는 단조 증가하며 감소하지 않는다.

```
예측 카드 (참가자당 1개, 3라운드분):
  predictions[pid] = {
    alloc:   {1: 20, 2: 30, 3: 50},        # 확신도 배분, 합계 100, 각 ≥ 10
    target:  {1: "영업본부", 2: None, 3: None},  # R1·R2=부서 그룹 / R3=카트 id
                                           # 해당 라운드 선택 창에서만 설정 가능
    is_auto: {1: False, ...},              # 무작위 배정 여부 (표시·검증용)
    locked:  {1: False, 2: False, 3: False}
  }

수정 규칙 (2가지를 명확히 분리):
  alloc[r]  : 라운드 r이 잠기기 전까지 언제든 재배분 가능 (온보딩 시점 사전 설정 포함)
              잠긴 라운드의 alloc 은 고정, 잠기지 않은 라운드끼리만 조정하며 합계 100 유지
  target[r] : **라운드 r의 선택 창이 열린 동안에만** 설정/변경 가능 (last-write-wins)
              창이 열리기 전에는 설정 자체를 거부 → "미리 걸어두고 잊는" 경로를 차단
              (R2 팀·R3 결선 카트는 직전 라운드가 끝나야 확정되므로 물리적으로도 불가)

라운드 잠금 시 (서버 시각 기준):
  target[r] 이 None 인 참가자에게 무작위 배정:
    autopick(pid, r) = 후보목록[ HMAC(seed, "autopick:{pid}:{r}") mod len(후보목록) ]
    is_auto[r] = True
  → 커밋된 시드에서 파생되므로 리빌 후 재계산 검증 가능 (임의 배정 의심 차단)
  → 0점 처리하지 않음: 아무도 게임에서 밀려나지 않는다

채점 (라운드 r 종료 시):
  hit_set(r)  = R1: 통과율 상위 2개 부서 그룹 / R2: 통과율 1위 부서 그룹
                R3: 당첨 카트 N대                                ← Fairness 결과에서 유도
  share(t)    = (직접 선택한 참가자 중 t 를 고른 수) / (직접 선택한 참가자 수)
                ※ 무작위 배정분은 분모·분자에서 제외 — 선택 창에 표시한 배수와
                   실제 지급 배수가 어긋나면 안 되기 때문 (표시 = 지급)
                ※ 직접 선택자가 0명이면 모든 bonus = 1.0 (중립)
  bonus(t)    = 1 + (1 - share(t))                     # 범위 [1, 2]
  weight(r)   = {1: 1.0, 2: 1.5, 3: 2.5}
  gain(pid)   = target[r] ∈ hit_set(r) ? floor(alloc[r] × weight(r) × bonus(target[r])) : 0
  score(pid) += gain(pid)

리더보드: 상위 10명만 Stage에 전송 (전체 250명 브로드캐스트 금지)
분포:     share(t) 와 미선택 인원 수를 선택 창 개방 중 1초 간격으로 Stage에 브로드캐스트
```

- **채점은 순수 함수**: `(저장된 예측, Fairness 결과) → 점수`. 어느 시점에든 전량 재계산해 동일 결과가 나와야 한다. 무작위 배정도 시드 파생이므로 재계산에 포함된다. 서버 재시작 시 이벤트 로그 재생이 아니라 **단순 재계산**으로 복구한다.
- **선택 창 상태는 서버 권위**. 창 개방 전/잠금 후의 `target` 설정 요청은 400으로 거부하고 현재 상태를 되돌려 보낸다.
- **선택 창은 최소 30초를 보장한다.** 자동 규칙이라는 도피처가 없으므로 창이 짧으면 다수가 무작위로 밀려 이 계층의 목적이 무너진다. Director가 구간을 축소할 때도 선택 창은 30초 미만으로 줄이지 않는다(축소는 레이스·발표 구간에서 흡수).
- 재접속 시 예측 카드·점수를 서버에서 복원한다(폰 잠금·백그라운드 전환이 빈번하므로 필수).
- 균형 상수(`weight`, `bonus` 상한, 최소 배분 10, 선택 창 하한 30초)는 `config.py`에 모아 Day 9 리허설에서 튜닝한다.
- 검증 페이지에서 참가자가 **자기 채점 내역(라운드별 배분·대상·무작위 여부·적중·보너스·획득 점수)** 을 확인할 수 있게 한다.

### 4.7 모바일 클라이언트 — 예측 UI 전용 (주행 뷰 없음)

**모바일에 주행 화면을 넣지 않는다.** 이 결정으로 결정론적 로컬 재생·프레임 동기·모바일 렌더러가 전부 불필요해졌다. 주행은 대형 스크린으로 함께 본다.

- **화면 구성 2요소**: ① **R1·R2·R3 예측 카드 3장** ② 내 점수·리더보드 순위·내 카트 통과 여부. 그 이상은 만들지 않는다(스코프 방어).
- **통신량**: 선택 요청(라운드당 1~수 회) + 선택 창 개폐·카운트다운 + 라운드 결과 갱신뿐. 라운드당 수 KB 수준이며 250대 동시 접속에서도 부담이 없다. 주행 좌표는 **절대 전송하지 않는다.**
- **온보딩 2단계 실명 선택**: `부서 그룹 선택 → 해당 부서 이름 목록에서 본인 선택`. 250명 단일 목록은 금지(찾기 어려움). 별칭 체계는 두지 않는다 — **실명만 사용**한다(당첨자 발표·부서 대항 구도가 실명 기준으로 성립하므로 이중 신원을 만들지 않는다).
  - 같은 부서 내 동명이인이 있으면 목록 항목에 사번 뒷 3자리를 병기한다(`홍길동(123)`).
  - 최초 선택 시 서버가 기기 토큰을 발급하고 클라이언트가 `localStorage`에 보관 → 새로고침·폰 잠금 후 재접속 시 본인 상태 자동 복원.
  - 이미 점유된 이름을 **다른 토큰**이 선택하면 409로 거부하고 안내 문구를 표시한다. Admin에 점유 해제 기능을 둔다(기기 교체 대응).
- 미접속·연결 끊김은 정상 상태로 취급한다. 예측 없는 참가자도 당첨 후보에서 제외되지 않는다.

### 4.2 Director — 시간 예산과 퍼널 산출

- 입력: 총 시간(기본 300초), 참가자 명단(부서 포함), 당첨 인원 N, 예측 게임 사용 여부.
  > *(2026-08-05 사용자 요청으로 기본값이 600초/10분으로 갱신됨 — AI의
  > 임의 변경이 아니라 사용자 지시. 현재 정확한 값은 README §3·§4 참고.
  > 이 §4.2 본문은 원 설계 기록으로 그대로 둔다.)*
- 퍼널 구성: R1 통과 100대 → R2 통과 F대 → R3 결선(당첨 N대). `F = clamp(max(5, 2N), 5, 10)`.
- **부서 그룹 자동 편성**: 실제 부서를 **5~8개 그룹**으로 묶는다(예측 대상 수를 이 범위로 유지하는 것이 목적). 알고리즘 — 부서를 인원 내림차순 정렬 후 큰 부서는 단독 그룹, 소규모 부서는 인원 합이 균형을 이루도록 연합 그룹으로 병합. 결과를 Admin에서 확인·수동 조정할 수 있게 하고, 확정된 그룹 편성은 커밋 snapshot에 포함한다.
- 시간 배분: 기획안 §4 런북(10+10+45+30+45+30+70+30+30 = 300초)을 기본값으로 오프닝/선택 잠금/각 라운드/채점+선택 창/최종 발표/검증 구간에 배분. 한 구간이 지연되면 이후 구간을 비례 축소하되, **선택 창은 30초 하한을 지키고 축소는 레이스·발표 구간에서만 흡수한다.**
- 예측 게임 비활성 시: 선택 창·채점 구간을 짧은 발표 구간으로 대체하고 남는 시간을 레이스에 재배분.
- 레이스는 **고정 상영 시간**: 시뮬레이션은 목표 시간에 정확히 종료되도록 진행률 기반으로 설계 (물리 시뮬 아님).

### 4.3 레이싱 렌더러 — 최소 연출 (스코프 축소 확정)

**연출은 의도적으로 최소화한다.** 아래 목록 밖의 시각 효과는 구현하지 않는다 — 시간이 남으면 그때 논의한다.

- **구현 대상**: 고정 부감 뷰 1종(전 라운드 공통), 부서 색상 점/막대 + 번호, 통과선(R1·R2) / 결승선(R3), 잔여 시간 게이지, **부서별 통과율 실시간 랭킹 바**, 통과 시 간단한 하이라이트.
- **제외**: 1인칭 시점, 카메라 전환·줌, 클로즈업, 리더 추적, 부스터 역전 이벤트, 스프라이트 아트·애니메이션, 파티클, 배속 UI.
  > *(이후 여러 세션에 걸쳐 사용자 요청으로 이 "최소 연출" 스코프를
  > 넘어섰다 — 라운드별 카메라 전환·줌·리더 추적, S자 곡선 트랙 + 카트
  > 회전, 팀 특수능력·개인 캐릭터 스프라이트, 장면별 배경음악까지 전부
  > 구현·커밋됨. AI의 임의 변경이 아니라 매번 사용자 지시로 진행. 이
  > §4.3 본문은 원 설계 기록으로 그대로 두고, 현재 실제 구현은 README
  > §3을 참고.)*
- **수렴 방식(단순화)**: `pos(t) = lerp(자율 진행, 목표 진행, w(t))` — 자율 진행은 약한 노이즈만, `w(t)`는 단조 증가해 종료 시 1.0. 정교한 러버밴딩 튜닝은 하지 않는다. **요구되는 것은 "결과 정합성 + 어색하지 않은 움직임"이며 그 이상은 스코프 밖이다.**
- **R1·R2 통과 판정**: 제한 시간(45초) 종료 시점에 통과선을 넘은 카트 = 통과자. 목표 통과자(`ranking[:100]`, `ranking[:F]`)가 정확히 그 시점에 통과선 안쪽에 있도록 진행률을 설계한다. 통과 순서는 의미 없고 **통과 여부만** 의미가 있다.
- **R3 판정**: 결승선 도착 순서 = 목표 순위. 상위 N대가 당첨.
- 성능 계약: R1 250개 점/막대를 1080p 캔버스에서 30fps 이상.
- 위치는 프레임 간 누적이 아니라 **경과 시간 t의 순수 함수**로 계산한다(재현성 확보, 프레임 드롭 시에도 결과 불변).

### 4.4 MC Agent

- 멘트는 **익명 슬롯 템플릿**으로 생성/캐싱: `"{lane}번 레인, 무서운 추격입니다!"` → 로컬에서 이름 치환.
- LLM 사용 시점: 행사 **전** 사전 생성(세션 설정 시 일괄 배치 생성)만 허용. 레이스 도중 실시간 API 호출 금지.
- 폴백: `mc.py`에 정적 멘트 풀(상황 태그별 최소 10개: 오프닝/레이스 진행/통과 발표/부서 순위 변동/최종 발표/검증)을 하드코딩. API 키 부재·실패 시 자동 사용.

### 4.5 화면 구성

- `stage.html`: 대형 스크린 전용. 조작 UI 없음. 상태는 WS로 수신. 1920×1080 기준, 폰트·대비는 원거리 가독성 우선. 구성: 트랙 뷰(주 영역) + 팀 순위/점수 + **포인트 리더보드 상위 10** + 실시간 배당률 + MC 자막 + **참여 QR·접속 인원(온보딩 구간)**.
- `mobile.html`: 세로 화면 전용. §4.7의 3요소만. 큰 터치 타겟. 예측 카드는 **R1/R2/R3 3장을 세로로 쌓고** 카드별 3가지 상태를 명확히 구분한다 — **① 대기**(확신도만 조정 가능, "대상은 라운드 시작 시 선택" 안내) **② 선택 중**(대상 버튼 활성 + 굵은 카운트다운 + 미선택 시 무작위 경고) **③ 확정**(비활성 + 결과 표시: 대상·무작위 여부·적중·보너스·획득 점수). 확신도는 슬라이더 + 프리셋(균등/후반집중/전반집중). 재접속 시 상태 복원.
- `admin.html`: 명단 업로드(CSV/붙여넣기/xlsx), N 설정, 중복 제외 토글, 모드 선택(룰렛/레이싱), **예측 게임 on/off**, 총 시간 설정, [커밋 생성]→[온보딩 개방]→[추첨 시작]→[다음 구간] 진행 버튼, 초기화·재추첨, 비상 버튼(수동 배속/스킵/예측 강제 잠금), 남은 시간·접속 인원·예측 입력률 표시.
- `verify.html`: 리빌 데이터 표시 + 브라우저 재계산 결과 일치 여부(전체 순위·팀 편성·생존 팀·당첨자) + **본인 예측 채점 내역 조회**. QR로 접근.
- 데모 모드: stage 첫 화면의 "데모로 체험하기" 버튼 → 샘플 250명 자동 로드 + **예측 봇 249명 활성화** 후 전체 흐름 자동 진행. 심사자는 별도 창에서 `mobile.html`에 들어와 직접 예측 체험 가능.

---

## 5. 작업 분해 (태스크 카드)

### 모델·Effort 배정 정책

각 태스크에 권장 모델과 reasoning effort를 명시한다. 표기는 `모델 · effort` (S = Sonnet, O = Opus / effort = low, medium, high, xhigh).

- **배정 기준**: 스펙이 §4에 정밀하게 고정된 태스크는 Sonnet·낮은 effort로 충분하다 — 판단할 것이 적기 때문이다. Opus·높은 effort는 (a) 정합성이 치명적인 곳(공정성 엔진), (b) 스펙만으로 답이 정해지지 않고 튜닝 감각이 필요한 곳(단순 보간 수렴, 시간 예산·부서 편성 로직)에만 쓴다.
- **에스컬레이션 규칙**: 낮은 배정으로 시작했다가 같은 문제로 2회 이상 실패하면 effort를 한 단계, 그래도 막히면 모델을 Opus로 올린다. 반대로 O 태스크라도 순수 보일러플레이트 구간은 Sonnet으로 낮춰 진행해도 된다.
- **effort 의미**: low = 기계적 구현/정형 작업, medium = 일반 구현(기본값), high = 정합성 검증·미묘한 엣지 처리·성능 분석, xhigh = 반복 튜닝과 설계 판단이 얽힌 최고 난도 구간.

각 태스크는 완료 기준(DoD)을 만족해야 다음으로 진행.

### Phase 0 — 골격 (Day 1: 7/29)

| ID | 태스크 | 모델·Effort | DoD |
|---|---|---|---|
| P0-1 | 저장소 구조 생성, FastAPI 앱 + 정적 서빙 + WS 허브, `run.py`, `requirements.txt` | S · low | `python run.py`로 서버 기동 + 브라우저에 stage/admin 빈 화면 표시, WS 에코 동작 |
| P0-2 | 데이터 모델(`models.py`) + 세션 스토어(`store.py`, JSON 스냅샷 복구 포함) | S · medium | 서버 재시작 후 세션 상태 복원되는 테스트 통과 |
| P0-3 | 명단 파싱(`roster.py`): CSV/붙여넣기 + 샘플 250명 생성기 | S · medium | `tests/test_roster.py` 통과 (인코딩·중복 사번·공백 엣지 포함, CP949/UTF-8 모두) |

### Phase 1 — 공정성 엔진 + 안전망 MVP (Day 2~3: 7/30~7/31) → **게이트 A: 제출 가능 상태**

| ID | 태스크 | 모델·Effort | DoD |
|---|---|---|---|
| P1-1 | `fairness.py`: §4.1 알고리즘 전체 (커밋/추첨/부서 통과율 집계/결선 진출/리빌/검증) | O · high | `test_fairness.py` 통과 — 결정론(같은 시드→같은 결과), **중첩 성질(N⊂F⊂100)이 임의의 N·부서 분포에 대해 성립**, 부서 통째 탈락 없음(생존은 항상 개별 단위), 제외 로직, 커밋 재계산 일치 |
| P1-2 | `roster.py`에 필수 컬럼(사번·실명·부서) 강제 검증 추가, admin 콘솔 1차(명단 업로드, N 설정, 제외 토글, 커밋→추첨→결과, 초기화/재추첨) | S · medium | 필수 컬럼 누락 시 업로드 거부 및 에러 위치 표시. 수동 시나리오: 250명 업로드→N=5 추첨→재추첨(당첨자 제외)→결과 상이 확인 |
| P1-3 | stage 룰렛 모드: 룰렛 휠 렌더링, 스핀→당첨자 정지 연출, N명 순차 발표, WS 연동 | S · medium | admin에서 시작 시 stage 룰렛이 당첨자 N명을 순차 발표, 정확히 Fairness 결과와 일치 |
| P1-4 | `verify.html`: 리빌 표시 + WebCrypto 독립 재계산, QR 생성 | S · high | 조작된 리빌 데이터 입력 시 "불일치" 검출 데모 가능. Python과 JS의 canonical JSON·HMAC 재계산이 바이트 단위로 일치해야 하므로 high |

### Phase 2 — AI MC 계층 (Day 4~5: 8/1~8/2) → **게이트 B: 2순위 완성형**

| ID | 태스크 | 모델·Effort | DoD |
|---|---|---|---|
| P2-1 | `mc.py`: 상황 태그별 정적 멘트 풀 + 템플릿 치환 엔진 | S · low | API 키 없이 룰렛 전 구간에 멘트 출력 |
| P2-2 | LLM 사전 생성 배치(세션 설정 시 익명 템플릿 N세트 생성·캐싱) + 실패 시 폴백 자동 전환 | S · medium | 키 있음/없음/API 타임아웃 3케이스 모두 정상 진행 |
| P2-3 | stage 멘트 연출(자막 밴드, 타이밍), 5분 런북의 룰렛 버전 적용 | S · medium | 데모 모드로 룰렛 전체 흐름이 5:00 내 완주 |

### Phase 3 — 레이싱 부서 대항 퍼널 (Day 6~7: 8/3~8/4) → **게이트 C (8/4 종료 시): go/no-go**

| ID | 태스크 | 모델·Effort | DoD |
|---|---|---|---|
| P3-1 | `director.py`: 시간 예산 배분·선택 창 30초 하한·구간 축소, F 산출, **부서 그룹 자동 편성(5~8개)** | O · medium | `test_director.py` 통과 — 임의의 (총시간, N) 조합에서 구간 합계=총시간·선택 창 ≥30초, F 규칙 준수. 임의의 부서 분포(3개~20개, 편중 포함)에서 5~8개 그룹 생성 및 그룹 간 인원 균형 |
| P3-2 | 레이스 엔진(`race/engine.js`): 고정 시간 진행, 목표 순위로의 단순 보간 수렴, 통과 판정 | O · high | 100회 자동 실행에서 R1·R2 통과자·부서 통과율 순위·R3 도착 순위가 Fairness 결과와 100% 일치, 지정 시간 ±0.5초 내 종료. **정교한 러버밴딩 튜닝은 스코프 밖** — 결과 정합성과 어색하지 않은 움직임까지만 |
| P3-3 | Stage 화면: 고정 부감 뷰, 부서 색상 카트, 통과선/결승선, **부서별 통과율 실시간 랭킹 바**, 잔여 시간 | S · medium | 3라운드 데모에서 통과율 랭킹이 실시간으로 갱신·역전되는 것이 보임 |
| P3-4 | 250대 렌더링 성능 확인 | S · medium | 1080p 캔버스 30fps 이상 (성능 측정 스크립트 첨부). 점/막대 렌더링이므로 최적화 여지가 크지 않음 |
| P3-5 | 레이싱 모드 통합: 런북 진행 상태 머신, MC 자막, 통과자·결선 진출자 발표, 커밋 배지 | S · medium | 데모 모드 레이싱 전체 흐름 5:00 내 완주 (예측 게임 없이) |

**게이트 C 판정 기준**: P3-2와 P3-5의 DoD 미달 시 레이싱 모드를 제출 스코프에서 제외하고 룰렛 버전으로 확정 (기획안의 타임박스 원칙). 판정은 사용자에게 보고 후 결정.

### Phase 4 — 참여형 예측 게임 계층 (Day 8~9: 8/5~8/6) → **게이트 D (8/6 오전): go/no-go**

**전제**: 게이트 C가 go일 때만 착수한다. C가 no-go면 Phase 4는 폐기하고 Phase 5로 직행한다. 이 계층의 모든 태스크는 **가산적**이어야 하며, `예측 게임 off` 상태에서 Phase 3 결과물이 그대로 동작해야 한다(P4-6에서 검증).

| ID | 태스크 | 모델·Effort | DoD |
|---|---|---|---|
| P4-1 | `predictions.py`: 예측 카드 저장/수정, 선택 창 상태 머신, 무작위 배정, 분포 집계, 채점 | O · high | `test_predictions.py` 통과 — **채점 재계산 멱등성**(같은 입력 → 같은 점수, 재시작 후에도 동일), **창 개방 전 target 설정 거부 / 창 중 허용 / 잠금 후 거부**, 배분 합계 100·최소 10 강제, 잠긴 라운드 값 보존 재배분, **무작위 배정의 시드 파생 결정론**, **분포·보너스가 직접 선택자만으로 계산됨**, 직접 선택자 0명 시 bonus=1.0, 점수 단조 증가 |
| P4-2 | QR 온보딩: 참여 URL·QR, **2단계 실명 선택(부서→이름)**, 기기 토큰, 중복 점유 거부, 접속 현황 집계 | S · medium | 다른 기기 브라우저로 QR 접속 → 부서 선택 → 이름 선택 → 확신도 100 카드 생성. **새로고침 시 토큰으로 복원 / 다른 기기의 같은 이름 선택은 409 / 동명이인 사번 병기 / Admin 점유 해제** 4케이스 확인 |
| P4-3 | `mobile.html`: 예측 카드 3장 UI(대기/선택 중/확정 3상태), 카운트다운, 점수·순위 | S · medium | 좁은 창에서 3라운드 완주. **① 대기 상태에서 확신도만 조정(대상 버튼 비활성) ② 선택 창에서 대상 선택·변경 ③ 잠금 후 거부 및 결과 표시 ④ 미선택 시 무작위 배정 표시** 4케이스 확인. 새로고침 후 상태 유지. **주행 뷰는 구현하지 않는다** |
| P4-4 | Stage 통합: 실시간 예측 분포 바, **선택 카운트다운·미선택 인원 카운터**, 리더보드 상위 10, 채점 연출, 온보딩 QR·커밋 해시 화면 | S · medium | 예측 게임 포함 5분 런북 완주, 예측 쏠림에 따른 분포 바·보너스 배수 변동이 보임, 미선택 인원이 실시간 감소하는 것이 보임 |
| P4-5 | 예측 봇(데모용) + **가산성 검증**: 예측 게임 off 시 Phase 3 동작 무손상 | S · high | 봇 249명으로 분포·리더보드 요동 재현. off 상태에서 P3-5 DoD 재통과(회귀 없음) |

**게이트 D 판정 기준**: 8/6 오전까지 P4-1·P4-3·P4-5가 DoD 미달이면 예측 게임 계층을 **비활성화**하고 레이싱 버전으로 제출 확정. 코드는 남겨두되 기본값 off로 두고 README에 "실험 기능"으로 표기한다. 판정은 사용자에게 보고 후 결정.

**우선순위 내 손절 순서**: 시간이 부족하면 P4-4의 부가 연출(채점 애니메이션 등)을 먼저 버린다. **P4-1·P4-2·P4-3·P4-5가 최소 성립 조합**이다.

### Phase 5 — 완성도·제출 (Day 10: 8/6~8/7 오전)

| ID | 태스크 | 모델·Effort | DoD |
|---|---|---|---|
| P5-1 | 원클릭 데모 모드 마감(무대 첫 화면 버튼, 자동 진행, 봇 연동) | S · medium | 심사자 시나리오: 배포 주소 접속→버튼 1회→5분 내 전체 체험. 모바일 화면도 별도 창으로 즉시 체험 가능 |
| P5-2 | 5분 런북 리허설 자동화: 3회 연속 세션 실행 스크립트, 시간 로그 | S · medium | 3회 연속 5:00±3초 내 완주, 오류 0건 |
| P5-3 | 부하 테스트: 모바일 250 동시 접속 + 라운드별 동시 예측 수정 | S · high | 250 WS 세션 유지, 채점 정확, 라운드당 서버 송신량 측정 결과 첨부 |
| P5-4 | 장애 리허설: 서버 강제 재시작→세션·예측·점수 복구, WS 재접속, LLM 차단, 모바일 전체 이탈 | S · high | 4개 시나리오 모두 행사 재개 가능. **재시작 후 채점 재계산 결과가 이전과 동일**함을 확인 |
| P5-5 | README(스크린샷/GIF, 60초 가독), `THIRD_PARTY_NOTICES.md` 최종화 | S · low | 제출 체크리스트(§8) 전 항목 충족 |
| P5-6 | 배포: 로컬 원커맨드 확정 + (선택) 클라우드 배포 주소 | S · medium | 클린 환경(가상환경 신규 생성)에서 `pip install -r requirements.txt && python run.py` 만으로 구동. **모바일 접속은 같은 Wi-Fi의 사설 IP로도 동작해야 함**(행사장 인터넷 불가 시 대비) |

---

## 6. 일정 요약과 게이트

| 날짜 | Phase | 게이트 |
|---|---|---|
| 7/29 | P0 골격 | — (완료) |
| 7/30~31 | P1 공정성+MVP | **게이트 A**: 이 시점부터 항상 제출 가능 |
| 8/1~2 | P2 AI MC | **게이트 B**: 2순위 완성형 확보 |
| 8/3~4 | P3 레이싱 | **게이트 C (8/4 종료)**: go/no-go — no면 룰렛 버전 확정, P4 폐기 |
| 8/5~6 오전 | P4 예측 게임 | **게이트 D (8/6 오전)**: go/no-go — no면 계층 비활성화(off 기본값) |
| 8/6~7 오전 | P5 완성도·제출 | 리허설·부하·문서·배포 완료 후 제출 |

**게이트 우선순위 원칙**: 게이트 A 이후의 모든 작업은 "이미 제출 가능한 상태"를 깨뜨리면 안 된다. 큰 변경은 브랜치에서 작업 후 데모 모드 완주 확인을 거쳐 main에 반영한다. **P4는 가장 바깥의 가산 계층이므로 가장 먼저 버려지는 대상**이다 — P5의 리허설·문서·배포 시간을 확보하는 것이 P4 완성보다 우선한다. P5 착수 시점(8/6 오전)에 P4가 미완이면 미련 없이 off로 내린다.

### 6.1 일정 실현 가능성 평가 (7/30 기준)

**현재 상태**: Phase 0 완료 (커밋 P0-1·P0-2·P0-3, 약 650줄 — FastAPI 골격 + WS 허브 + 모델/스토어 + 명단 파싱 + 테스트 2종). Phase 1 미착수. **남은 가용 시간 약 8일**(7/30 잔여 ~ 8/6, 8/7 오전은 제출 버퍼).

| Phase | 남은 작업 | 이전 추정 | **재추정** | 비고 |
|---|---|:-:|:-:|---|
| P1 공정성+룰렛 MVP | 4 태스크 | 2.0일 | **2.0일** | P1-1(Opus·high)이 핵심. 부서 집계·결선 진출 규칙 포함 |
| P2 AI MC | 3 태스크 | 1.0일 | **1.0일** | 정적 폴백 우선, LLM은 배치 1회. 가장 예측 가능한 구간 |
| P3 레이싱 퍼널 | 5 태스크 | 2.0일 | **1.25일** | 연출 스코프 축소로 P3-2가 xhigh→high, 카메라 전환 태스크 소멸 |
| P4 예측 게임 | 5 태스크 | 1.5일 | **1.0일** | 모바일 주행 뷰 제외로 결정론적 로컬 재생 태스크(Opus·high) 완전 소멸 |
| P5 완성도·제출 | 6 태스크 | 1.5일 | **1.5일** | 축소 불가 — 리허설·부하·문서·배포는 제출물의 일부 |
| | **합계** | 8.0일 | **6.75일** | 가용 8일 → **버퍼 약 1.25일 확보** |

**결론: 세 차례의 단순화로 일정이 실현 가능한 범위로 들어왔다.** 이전 추정은 버퍼 0이었으나, 아래 세 결정이 난이도 최상위 작업들을 제거했다.

| 결정 | 사라진 작업 | 효과 |
|---|---|---|
| 도박 요소 제거 (무손실 채점) | 잔액 차감·초과 검증, 파산 재지급, 원자적 트랜잭션, 정산 총액 보존 검증, 배당률 재계산 | 채점이 순수 함수화 → 테스트 표면·장애 복구 경로 단순화 |
| **모바일 주행 뷰 제외** | 결정론적 로컬 재생, 프레임 동기 tick, 모바일 렌더러, 기기 간 재생 일치 검증 (Opus·high 1개 태스크) | 네트워크 리스크도 근본 해소(라운드당 수 KB) |
| **연출 스코프 축소** | 카메라 전환·1인칭·클로즈업(태스크 1개), 부스터 역전, 정교한 러버밴딩 튜닝(xhigh→high) | 최대 리스크 태스크의 난이도 하향 |

**여전히 지켜야 할 원칙**:
1. **게이트를 문자 그대로 집행한다.** 8/4 종료 시 P3 미완이면 룰렛 버전 확정(P4 폐기), 8/6 오전에 P4 미완이면 off. 판정을 미루는 것이 유일한 진짜 실패 모드다.
2. **확보된 1.25일은 새 기능이 아니라 리허설·버그 픽스에 쓴다.** 여기서 연출을 다시 늘리면 원점으로 돌아간다. 연출 추가는 P5를 모두 통과한 뒤에만 검토한다.
3. **P3-2를 8/3 안에 끝낸다.** 난이도가 낮아졌어도 결과 정합성 검증(100회 실행)은 남아 있어 여전히 최대 리스크 태스크다.

---

## 7. AI 개발 에이전트 운영 규칙

1. **태스크 단위 진행**: 태스크 카드 하나를 완료(DoD 충족 확인)한 뒤 다음으로. 두 태스크를 섞어 작업하지 않는다.
2. **커밋 규칙**: 태스크 완료마다 1커밋 이상, 메시지에 태스크 ID 포함 (`P1-1: fairness engine with nested survivor sets`). main 직접 커밋은 게이트 A 전까지만, 이후는 브랜치+검증 후 병합.
3. **검증 방법**: 각 태스크의 DoD를 코드 리뷰가 아니라 **실행으로** 확인한다 — 서버 기동, 데모 모드 완주, pytest, 성능 측정 스크립트. 실행 결과(테스트 출력, 소요 시간)를 작업 보고에 포함한다.
4. **스펙 준수**: §2 기술 스택과 §4 설계 계약은 임의 변경 금지. 더 나은 방법이 보이면 구현하지 말고 근거와 함께 사용자에게 제안한다.
5. **의존성 추가 시**: `requirements.txt`와 `THIRD_PARTY_NOTICES.md`(패키지명, 버전, 라이선스, 용도)를 같은 커밋에서 갱신한다.
6. **시간 초과 시**: 태스크가 예상보다 커지면 쪼개서 DoD를 부분 달성 가능한 형태로 재정의하고 보고한다. 게이트 날짜는 이동하지 않는다.
7. **모델·Effort 선택**: 표의 `모델 · effort` 배정은 §5 정책의 권장값이다. 에스컬레이션 규칙(같은 문제 2회 실패 → effort 한 단계 상향, 그래도 막히면 Opus)을 따르되, O 태스크라도 보일러플레이트 구간은 Sonnet·낮은 effort로 낮춰 진행해도 된다. 상향/하향 전환 시 그 사실을 작업 보고에 남긴다.

---

## 9. Phase R — 레이싱 시각 연출 추가 (7/31 착수, D-7)

게이트 A~D(안전망 MVP·MC·레이싱 부서대항 퍼널·예측 게임)가 완료되고 실제
데모를 돌려본 뒤, 애초에 "연출 스코프 축소"로 미뤄뒀던 시각 효과(빨리감기·
부스터·카메라 전환)를 추가하기로 확정했다. 범위는 **Stage(무대) 화면
전용**으로 확정했다 — 모바일에 레이싱 뷰를 다시 넣으면 "모바일에 프레임
데이터 전송 금지" 원칙과 충돌하고 개발 리스크가 다시 커지므로, 모바일은
예측 UI 전용을 유지한다.

핵심 통찰: Stage는 이미 `race_tick` WS 이벤트로 매 틱 `positions`(참가자별
0..1 진행률)를 받고 있다. 지금까지 이 데이터를 부서 막대 그래프로만 집계해
써서 "실제로 움직이는 카트 그림"이 화면에 없었다. 즉 **백엔드 변경 없이
프런트엔드 렌더링만 추가**하면 트랙 시각화의 대부분이 해결된다.

| ID | 태스크 | 위치 | DoD |
|---|---|---|---|
| R0 | 데모 모드 버그 수정: 봇이 전체 인원을 다 채워 실제 테스터가 참여 불가능한 문제 | main.py | 데모 시작 후 `/mobile`에서 최소 1명 이상 참여 가능(봇이 전원을 채우지 않음) |
| R1 | Canvas 트랙 렌더러: 부서 색상 점이 실제로 트랙을 따라 이동, 결승선 표시 | stage.html/js/css | race_tick 수신 시 60fps 근접 렌더링, 250개 점에서도 끊김 없음 |
| R2 | 라운드별 카메라 자동 전환(R1 광각 부감 → R2 중간 줌 → R3 리더 추적 클로즈업) + 수동 토글 | stage.js | 라운드 전환 시 카메라가 자동으로 바뀌고, admin에서 수동 전환 가능 |
| R3 | 추월/부스터 시각 효과: 순위 역전 시 클라이언트에서 감지해 부스터 이펙트 표시(서버 결과에 영향 없음) | stage.js (클라이언트 계산, 백엔드 불필요) | 역전 발생 시 시각 효과 표시, 최종 판정은 여전히 서버 값과 100% 일치 |
| R4 | 비상 배속/스킵: admin이 진행이 밀릴 때 현재 레이스 구간을 앞당겨 종료 | main.py(엔드포인트+틱루프 훅), admin.js | 버튼 클릭 시 해당 라운드가 조기 종료되고 다음 단계로 정상 진행, 시간 예산 절약 |
| R5 | 통합 리허설: 실제 서버로 데모 실행 + WS 이벤트 스트림 확인, 전체 테스트 통과 | 수동 검증 + pytest | 데모 시작→R1~R3 시각 효과 확인→최종 발표까지 오류 없이 완주 |

**원칙 준수**: 연출은 여전히 "이미 정해진 결과를 향해 극적으로 도달"하는
역할만 한다 — R3(부스터/추월 이펙트)는 클라이언트에서 계산하는 순수 표시
효과이며 서버가 계산한 `position_at()` 값 자체는 건드리지 않으므로, 공정성
원칙과 100% 일치 검증(P3-2에서 이미 확립)은 그대로 유지된다.

---

## 10. Phase T — 팀 대항전 + 라운드별 자원 투자 시스템 (2026-08-06 착수·부분 완료)

> **2026-08-06 기록.** 사용자가 예전에 요구했던 내용인데 지금까지 구현·
> 문서화가 안 돼 있었던 걸 발견해 여기에 상세 명세로 남겼고, 마감(08/07)
> 전날 밤 같은 세션에서 바로 착수했다. **결과: T2만 구현·커밋, T3는
> 착수 중 설계 충돌을 발견해 사용자 확인 후 폐기, T1·T4는 시간 관계상
> 보류.** 아래는 원래 작성한 설계 초안(그대로 보존)이고, 실제 진행
> 결과는 각 하위 항목 끝에 "**진행 결과**" 블록으로 덧붙인다.

### 10.1 팀 대항전(부서 대항 차등 보상 + 결선 팀당 1대)

**요구사항 원문(사용자)**: "각 라운드에서 가장 많이 살아남은 부서원을
가진 팀 순으로 보상을 차등적으로 받는 거야. 물론 결승선을 통과하여
살아남은 각 개인들에게도 보상은 주어져야지. 그리고 마지막 라운드에서는
반드시 각 팀 별 카트 하나씩만 남는 거지. 시스템 상으로."

두 가지 하위 기능으로 나뉜다.

**(a) 라운드별 팀 순위 차등 보상.** 지금은 `app/gambling.py`의
`award_round_rewards`가 "그 라운드 최고 통과율 부서(1팀만) 소속 통과자"
에게만 `TEAM_WIN_REWARD`(+60)를 추가 지급하는 **이진(우승팀/그 외)**
구조다. 요구사항은 전체 부서를 그 라운드 통과율(또는 생존 인원수)로
**순위 매겨 차등 보상**하는 것 — 예: 1위 부서 +100, 2위 +70, 3위 +40,
나머지 +0 같은 계단식. 개인이 결승선을 통과해 받는 기본 보상
(`FINISH_REWARD`)은 지금처럼 그대로 별도 유지.
- 영향 범위: `app/gambling.py`(`award_round_rewards`의 우승팀 판정을
  순위표로 확장), 확신도 모드에도 같은 개념을 넣을지는 별도 결정 필요
  (`app/predictions.py`는 지금 팀 단위 보상 개념이 아예 없음).
- 열린 질문: 보상 테이블(몇 등까지, 얼마씩)을 몇 개 팀 기준으로 설계할지
  — 부서 자동 편성이 5~8개 그룹으로 묶이므로(기획안 §4) 이 범위 안에서
  설계하면 됨.

**진행 결과(2026-08-06)**: **구현 완료, 커밋됨**(`app/gambling.py`,
`app/main.py`). `TEAM_WIN_REWARD`(이진) → `TEAM_RANK_REWARDS = [100, 70,
40]`(3단계 계단식, 사용자 확인)로 확장. `award_round_rewards`가 이제
순위 정렬된 부서 id 목록(`ranked_dept_ids`)을 받아 1~3위 부서 소속
통과자에게 각각 차등 지급(4위 이하는 추가 보상 없음). 개인
`FINISH_REWARD`는 그대로 별도 유지. 확신도 모드는 이번 범위에서 제외
(사용자 확인 — 갬블링 모드 전용). 레이스 계산(`fairness.py`)에는 전혀
관여하지 않으며, 기존 328개 테스트 전부 통과(회귀 없음).

**(b) 결선(R3)에 팀당 정확히 카트 1대만 진출.** 이건 시각 연출이 아니라
**공정성 알고리즘 자체의 구조 변경**이라 가장 신중하게 다뤄야 한다.
지금 `app/fairness.py`의 `_compute_outcome`(`r2_pass = ranking[:finalist_count]`)
은 R2 생존자 중 **순수 전체 순위 상위 F명**을 결선 진출자로 뽑을 뿐,
부서 제약이 전혀 없다 — 한 부서에서 결선 진출자가 0명일 수도, 여러 명일
수도 있다. "팀당 정확히 1대"를 시스템적으로 보장하려면:
  1. R2 생존자를 부서별로 묶는다.
  2. **부서 내부에서 전체 순위가 가장 높은 1명만** 결선 후보로 남긴다
     (동일 시드 기반 순위이므로 여전히 결정론적·검증 가능 — 공정성 원칙
     위반 아님, 단지 "누가 진출하는가"의 선발 규칙이 바뀌는 것).
  3. 그 결과 결선 인원(F)은 더 이상 `resolve_finalist_count(draw_count)`
     공식이 아니라 **"R2 생존자가 1명 이상 남은 부서 수"**로 정해진다
     (보통 5~8명 — 부서 자동 편성 그룹 수와 자연스럽게 맞아떨어짐).
  4. 특정 부서가 R2에서 전멸(생존자 0명)하면 그 부서는 결선에 대표를
     못 보낸다 — "억지로 살려서 진출"시키지 않는다(순수 확률적 결과를
     왜곡하면 안 되므로).
- 영향 범위: `app/fairness.py`의 라운드 진행 알고리즘(가장 핵심적인
  변경), `tests/test_fairness*.py` 전면 재검토 필요, README/게임소개의
  "R2: 생존자 중 결선 진출 인원(F) 통과" 설명도 다시 써야 함.
- **주의**: 이건 지금까지 이 프로젝트에서 가장 손대지 않으려 애썼던
  `fairness.py` 핵심 로직을 실제로 바꾸는 유일한 예정 작업이다 — 322개
  기존 테스트 중 다수가 이 파일을 직접 검증하므로, 착수 시 회귀 위험이
  이번 세션의 다른 어떤 변경보다 크다는 걸 감안해야 한다.

**진행 결과(2026-08-06)**: **폐기(사용자 확인 후 착수 중단)**.
`app/fairness.py`에 실제로 구현해봤다(부서별 R1 생존자 중 전체 순위
최상위 1명만 결선 진출, `static/fairness.js` parity까지 이식) — 여기까지는
성공했고 79개 테스트로 문제를 정확히 잡아냈다. 그런데 이 시점에
**설계 문서에 없던 2차 충돌**을 발견했다: `static/stage.js`의 Round 2
레이스 시각화가 "통과선(pass_line) 하나로 앞/뒤를 가른다"는 **연속
구간 절단** 방식인데, "부서당 1명"은 전체 순위 어디에 있든(예: 71등이
유일한 생존자인 부서 대표) 뽑힐 수 있는 **불연속 집합**이라 단일
통과선으로는 절대 표현이 안 된다. 즉 화면에는 통과선보다 한참 앞선
카트가 "탈락"으로, 한참 뒤처진 카트가 "통과"로 표시되는 모순이 생긴다.
Round 2만 따로 "카트별 팀대표 배지" 방식으로 다시 그리는 방안을
설계했으나(백엔드는 이미 정상 동작), 사용자에게 상황을 설명하는
과정에서 **사용자가 스스로 재고**했다 — "71등이 2등·3등을 제치고
결승에 가는" 결과 자체가, 화면을 아무리 잘 고쳐도 현장에서 "조작
아니냐"는 의심을 살 수 있어 이 프로젝트가 가장 중요하게 여기는 신뢰
**체감**을 깎아먹는다는 결론. **최종 결정: 결선 진출은 지금처럼 등수
그대로(팀 제약 없음) 유지, `fairness.py`/`static/fairness.js` 변경은
전부 되돌림(스태시 폐기, 커밋 없음).** "팀 대항전" 요구는 이미 구현된
(a) 라운드별 팀 순위 차등 보상만으로 충분히 충족한다고 판단.
`resolve_finalist_count` 공식과 R2→R3 top-K 절단 로직은 손대지 않은
원래 상태 그대로다.

### 10.2 라운드 시작 전 자원 투자 선택권 (개인/팀 업그레이드 + 베팅)

**요구사항 원문(사용자)**: "각 라운드 시작 전에, 이 자원(사이버머니/
확신도)을 투자할 선택권이 참여자에게 주어지는 거야. 1. 개인 카트
업그레이드(속도 5 향상 등) 2. 팀 능력치 향상(팀 전체 카트 속도 1 향상
등) 3. 특정 카트에 베팅 4. 특정 팀에 베팅. 계속 사용자는 선택할 수
있는 거야 -- 내 카트를 업그레이드할지, 베팅을 해서 머니를 불릴지 등등."

**이미 있는 부분**: 3·4(카트/팀 베팅)는 `app/gambling.py`의 라운드별
베팅(R1·R2는 부서 대상, R3는 결선 카트 대상)으로 이미 구현돼 있다 —
"매 라운드 후보에게 베팅"이라는 틀 자체는 재사용 가능.

**새로 필요한 부분**: 1·2(카트/팀 업그레이드)는 지금 "라운드 시작 전
선택"이 아니라 **행사 끝나고 남은 돈으로 즐기는 사후 소비**(코스메틱
상점, `purchase_personal_upgrade`/`contribute_team_upgrade`)로만 있다.
요구사항은 이걸 **매 라운드 시작 전에 베팅과 같은 급의 선택지**로
승격시키는 것 — 매 라운드마다 "이번엔 업그레이드에 쓸지, 베팅에 쓸지"
전략적으로 고르게 하는 구조.

**⚠️ 반드시 먼저 결정해야 하는 핵심 설계 질문 (임의로 정하지 않음)**:
사용자 표현이 "속도 5 향상", "팀 전체 카트 속도 1 향상"으로 되어
있는데, 이걸 **글자 그대로 실제 레이스 계산에 반영**하면 이 프로젝트
전체의 핵심 신뢰 장치(추첨 결과가 커밋 시점 시드로 완전히 고정되고
`/verify`로 재계산 검증 가능)가 깨진다 -- 지금 코스메틱 상점이 딱 이
문제 때문에 "시각 효과만 바꾸고 레이스 계산에는 어떤 값도 안 넘긴다"고
설계돼 있다(`app/gambling.py` 모듈 docstring 참고). 두 가지 길이 있다:
  - **(A) 코스메틱 유지형**: "속도 향상"은 문구·연출일 뿐 실제
    `position_at()` 계산에는 관여하지 않는다(카트가 더 빨라 보이는
    파티클/글로우 연출만). 기존 신뢰 구조를 100% 유지. 지금 코스메틱
    상점을 "라운드 시작 전 선택지"로 승격만 시키면 되므로 구현
    난이도가 낮다.
  - **(B) 실제 반영형**: 구매한 업그레이드가 `app/race.py`의 카트별
    진행 계산에 실제로 영향을 준다. 이러면 "레이스 결과가 커밋
    시점에 이미 고정된다"는 전제 자체가 무너지므로, 공정성 검증
    구조(`/verify`, 커밋-리빌 전체) 자체를 다시 설계해야 한다(예:
    "구매 내역까지 포함해 매 라운드 직전에 재커밋"하는 식으로
    확장하거나, 아예 "레이스는 최종 근거가 아니다" 라는 §7/§8 전환의
    연장선으로 다룰 수도 있음). 구현 난이도·리스크가 훨씬 크다.

  **선례**: 이번 세션에서 "예측/갬블링 리더보드가 실제 당첨자를
  정한다"(§7 전략노트)로 바꿀 때도 똑같은 종류의 질문을 먼저 사용자에게
  확인받고 진행했다. 이 기능도 착수 전에 (A)/(B) 중 어느 쪽인지 반드시
  먼저 확인할 것.

**추가로 정할 것**:
- 확신도 모드(무손실 자원)에서도 "개인/팀 업그레이드"가 가능한지, 아니면
  이 4개 선택지 전체가 갬블링 모드 전용인지(확신도는 원래 "잃을 게
  없는" 예측 전용 설계라 "업그레이드 구매"라는 소비 개념과 잘 안
  맞을 수 있음).
- 매 라운드 "선택 창"이 하나 더 늘어나는 셈이라, `app/director.py`의
  시간 예산 배분에 새 구간을 넣어야 할 수도 있음(또는 기존 선택 창
  안에서 탭 전환으로 처리).
- 팀 업그레이드는 이미 있는 "부서원 공동 투자" 구조를 재사용 가능
  (`contribute_team_upgrade`, 임계값 도달 시 레벨업).

**진행 결과(2026-08-06)**: **T1 → (A) 코스메틱 확정 → 이후 상점 자체를
전면 제거. T4는 폐기.**
1차로 T1을 물었더니 (A) 코스메틱 유지형으로 확정됐고, 이어서 "그럼
업그레이드 상점이 결국 코스메틱 전용인데 T4가 무슨 실익이 있냐"는
사용자의 재질문에 따라 T4를 보류했다.
그 뒤 같은 날 심야 피드백에서 **"카트 업그레이드 항목은 제거해도 좋을
거 같다"**는 최종 판단이 나와, 코스메틱 상점 자체를 전면 삭제했다:
- 백엔드: `purchase_personal_upgrade` / `contribute_team_upgrade` /
  `team_upgrade_level`, `BetCard.personal_upgrade_level` /
  `team_upgrade_contributed`, `team_upgrade_pool`,
  `PERSONAL_UPGRADE_COST` / `TEAM_UPGRADE_THRESHOLD` 등 상수,
  `/api/bet/upgrade/personal` · `/api/bet/upgrade/team` ·
  `/api/bet/upgrades` 엔드포인트 전부 제거.
- 프런트: 모바일 업그레이드 패널, 무대 카트 글로우 부스트, 팀 레전드
  별(⭐) 표시 제거.
- 그 자리에 **라운드별 보상 내역 표시**(`BetCard.rewards`)를 넣었다 —
  "돈을 어디에 쓰는가" 대신 "돈이 어떻게 들어오는가"를 보여주는 쪽이
  이 게임(최종 잔액이 곧 당첨 순위)의 구조와 훨씬 잘 맞는다고 판단.

**결론: T4는 폐기.** 업그레이드가 사라진 지금 "라운드 시작 전 자원 투자
선택지"는 결국 베팅 하나뿐이라, 별도 메뉴를 만들 실익이 없다.

| ID | 태스크 | 위치 | DoD | 결과 |
|---|---|---|---|---|
| T1 | (A)/(B) 설계 방향 확인 -- 업그레이드가 코스메틱인지 실제 레이스에 반영되는지 | 사용자 확인 필요 | 방향 확정 전까지 착수 금지 | ✅ (A) 코스메틱 유지형으로 확정 |
| T2 | 라운드별 팀 순위 차등 보상 테이블 설계 + `award_round_rewards` 확장 | app/gambling.py | 라운드마다 부서 순위별로 다른 금액이 지급되고 개인 통과 보상은 그대로 유지됨을 테스트로 확인 | ✅ 구현·커밋 완료(1~3위 계단식) |
| T3 | 결선 팀당 1대 진출 규칙 구현 | app/fairness.py + 관련 테스트 전면 재검토 | R2 생존 부서 수만큼 결선 인원이 정해지고, 부서당 정확히 1명(전체 순위 최상위)만 진출함을 다수 시드로 검증 | ❌ 폐기(Round 2 시각화 신뢰도 문제로 사용자가 철회) |
| T4 | 라운드 시작 전 "투자 선택 메뉴"(업그레이드/베팅 통합) UI + API | app/main.py, static/mobile.js/html | 매 라운드 선택 창에서 4가지 선택지가 모두 노출되고 자원이 정확히 차감·반영됨 | ❌ 폐기(업그레이드 상점 자체를 제거해 선택지가 베팅뿐이 됨) |
| T5 | README/게임소개/전략노트 갱신 | 문서 전체 | 새 팀 대항전·투자 시스템 규칙이 참가자·심사자 문서에 정확히 반영 | ✅ 이 문서 + README/게임소개/진행상황 갱신, **게임구조.md 신설** |

---

## 11. Phase U — 실사용 피드백 반영 (2026-08-06 심야, 완료)

실제로 데모를 돌려본 사용자 피드백 11건을 백엔드/프런트/운영콘솔
3개 커밋으로 나눠 전부 반영했다. 상세 내역은
`타추위-추첨프로그램_진행상황.md` §1의 21~31번 참고.

| # | 피드백 | 조치 | 위치 |
|---|---|---|---|
| 1 | AI MC가 레이스 종료 후에도 계속 중계 | 장면 전환마다 `mcEpoch` 증가, 이전 세대 응답 폐기 + 진행률 90% 이후 실황 멘트 잠금 | static/stage.js |
| 2 | 좌우 패널이 레이스를 가림 | 패널 폭·글자 축소, 배경 반투명화 | static/stage.css |
| 3 | 사이버머니 리더보드가 MC 자막에 가림 | MC 바를 좌우 패널 사이 가운데 폭으로 제한 | static/stage.css |
| 4 | **모바일 베팅 불가(치명적)** | 폴링 재렌더가 선택을 초기화 → `pendingBetTarget/Amount`로 보존, 입력 중 재렌더 스킵 | static/mobile.js |
| 5 | 결선이 너무 느림 | `race_r3` 70→40 (600초 기준 140→80초) | app/director.py |
| 6 | 라운드별 맵이 다 동일 | 트랙 폭이 굴곡보다 2~3배 커서 뭉개졌던 것 → R1 스피드웨이/R2 시케인/R3 테크니컬 서킷으로 분리 + 차선 수 자동 조정 | static/stage.js |
| 7 | 카트 업그레이드 제거 | §10.2 참고 — 백엔드·프런트 전면 삭제 | 전역 |
| 8 | 최종 당첨자에 사이버머니 표기 | `DrawResult.prize_scores` 추가 + 시상대 표시 | app/models.py, static/stage.js |
| 9 | 폰에 라운드별 보상 내역 표시 | `BetCard.rewards` 항목별 기록 + 모바일 카드에 표시 | app/gambling.py, static/mobile.js |
| 10 | 콘솔에서 룰렛 제거 | 모드 선택자·리빌 버튼 삭제, 항상 racing | static/admin.* |
| 11 | 장애물 다양화 + 실제 변수 | 3→8종, 크기 랜덤, 개수 7~12개, 스핀아웃/정지/감속/이탈 효과 | static/stage.js |

**11번의 공정성 안전장치(중요)**: 장애물 효과는 "보이는 위치"에만
적용되고, 각 효과가 시간 경과로 사라지는 것과 **별개로** 레이스 막판
12% 구간(`EFFECT_FADE_TAIL`)에서 강도에 0으로 수렴하는 페이드가 곱해진다.
따라서 결승선 통과 시점에는 보이는 위치와 서버가 정한 실제 위치가 정확히
일치하며, 화면과 발표 결과가 어긋날 수 없다. §4의 "연출과 결과 분리"
원칙을 그대로 지킨 구현이다.

---

## 8. 제출 체크리스트 (기획안 §10 상속 + 구체화)

> **[2026-08-08 정리]** 이 목록은 한동안 손대지 않아 **이미 제거된 기능을
> 가리키는 항목**이 남아 있었다(확신도 배분, `/verify` 검증 페이지 등).
> 아래는 실제 코드 상태와 대조해 정리한 것이다. 자동화 테스트로 이미
> 고정된 항목은 `[x]`로 닫고 근거를 적었다. 남은 `[ ]`는 **사람이 실제
> 기기·행사장에서 해봐야만 하는 것들**이다.

**자동화로 고정된 것 (코드/테스트가 계속 지킨다)**

- [x] 예측 게임 off 상태 정상 완주 (P4-6) — `test_predictions_disabled_produces_no_prediction_events`, `test_predictions_disabled_falls_back_to_race_winners`
- [x] 장애 복구 + 채점 재계산 일치 (P5-4) — `tests/test_fault_recovery.py`
- [x] 예측 카드 상태 전이(대기 / 선택 창 / 잠금 후 거부 / 미선택 자동 배정 R1·R2 자기 부서 · R3 무작위) (P4-3) — `tests/test_predictions.py`
- [x] `THIRD_PARTY_NOTICES.md` 전체 의존성 라이선스 기록 — `tests/test_third_party_notices.py`가 requirements와 **버전까지** 대조한다
- [x] pytest 전체 통과 — 315개
- [x] (선택) 클라우드 배포 주소 — https://taachuwi-grandprix.onrender.com/
- [x] UI 문구에 도박 용어(베팅/판돈/배당률/사이버머니) 미사용 — 갬블링 모드 자체를 제거해 구조적으로 보장(§13-1)
- [x] README (소개, 사용법, 공정성 서술, 예측 게임 안내) — 스크린샷/GIF는 의도적으로 넣지 않았고 그 사유를 README에 적어 뒀다

**사람이 해야 하는 것 (자동화 불가)**

- [ ] 클린 환경 원커맨드 구동 확인 + 사설 IP 모바일 접속 (P5-6)
- [ ] 데모 모드를 실제 브라우저에서 끝까지 1회 체험 (P5-1)
- [ ] 10분 런북 3회 연속 리허설 통과 (P5-2)
- [ ] 모바일 250 동시 접속 부하 테스트 실측 (P5-3) — `scripts/load_test.py` 준비돼 있음
- [ ] 250대 렌더 성능(체감 fps)을 행사 PC에서 측정
- [ ] 리허설 실측: 선택 창 안에 **직접 선택하는 비율** (자동 배정이 과반이면 창 시간 재조정 — 다만 자동 배정도 자기 부서라 과반이어도 치명적이지 않다)
- [ ] **타추위 협의 확인**: 예측 게임 상위 입상자 소상품 지급 여부

> ~~커밋-리빌 검증 페이지 데모 (P1-4)~~ — `/verify` 화면은 §12-4로 제거됐다.
> 판정 로직의 재현성 자체는 `tests/test_fairness.py`의 위변조 탐지
> 테스트가 계속 지킨다(§13-6).

---

## 11. (2026-08-07) 예측 게임 전면 개편 — Phase T 후속 결정

§10 Phase T는 "팀 대항전"을 갬블링 모드의 보상 테이블 위에 얹는 전제로
설계돼 있었다. 그 갬블링 모드가 없어졌으므로, 이 절이 §10의 결론을
현재 상태로 갱신한다.

### 11-1. 갬블링 모드 폐지에 따른 §10 항목 재정리

| 항목 | §10 시점 상태 | 현재 |
|---|---|---|
| T2 라운드별 팀 순위 차등 보상 | `app/gambling.py`의 `TEAM_RANK_REWARDS = [100, 70, 40]` | **유지되되 예측 점수 단위로 이관** — `app/predictions.py`의 `TEAM_RANK_POINTS = [20, 14, 8]`. 개인 통과 보상도 `FINISH_POINTS = 8`, 결선 당첨은 `FINAL_WIN_POINTS = 60` |
| T3 결선 팀당 카트 1대 | 폐기(§10.1(b)) | 폐기 유지 — 판단 근거 불변 |
| T4 라운드 시작 전 투자 메뉴 | "선택지가 베팅뿐이라 실익 없음"으로 보류 | **완전 폐기** — 베팅 자체가 없어졌고, 확신도 배분(특히 R3 몰아주기)이 그 역할을 대신한다 |
| 도박 용어 미사용 | 미확인 항목 | **구조적으로 달성** — 관련 코드·문구 전부 제거 |

### 11-2. 새로 확정된 규칙 (구현 완료)

1. **순위 차등 채점** — 고른 대상의 순위에 따라 배점의
   `RANK_RATIOS = [1.0, 0.6, 0.35, 0.2, 0.1]`, 순위표 밖은
   `FLOOR_RATIO = 0.05`. R1·R2는 부서 통과율 순위, R3는 결승 등수 순위.
   소수파 보너스는 **1위 정확 적중에만** 적용(차등 구간까지 주면
   "틀렸는데 아무도 안 골라서 더 받는" 모순이 생긴다).
2. **R3 몰아주기(올인)** — `MIN_ALLOC_ROUNDS = (1, 2)`로 R3만 하한 해제
   (0~80), `ROUND_WEIGHTS[3]`을 2.5 → 3.0.
3. **전원 자동 참가** — `enroll_all()`이 커밋 시점에 명단 전원의 카드를
   생성. 미선택 시 **R1·R2는 자기 소속 부서**, **R3는 시드 파생 무작위**.
   부서 매핑은 커밋 스냅샷의 `departments`를 뒤집어 만들므로
   (`_department_by_pid`), 자동 배정값이 후보에 없는 사태가 없다.

### 11-3. 리허설 때 확인할 것 (§8 체크리스트 보강)

- [ ] 봇 없이(`with_bots: false`) 데모를 끝까지 돌려, **아무도 폰을 안
      들어도** 전원이 점수를 쌓고 경품 당첨자가 정원만큼 나오는지
      (자동화 테스트로는 이미 고정 —
      `test_non_mobile_participants_still_score_and_stay_prize_eligible`)
- [ ] 라운드 종료 후 폰의 **점수 내역**이 예측/통과/팀 순위로 쪼개져
      뜨는지
- [ ] 무대 우측 **실시간 선택 분포**가 선택 창 동안에만 뜨고 닫히면
      사라지는지, 인원수·소수파 배수(💎)가 함께 보이는지
- [ ] 발표 후 **폰에 당첨/미당첨 결과**가 뜨는지(2026-08-08 추가)
- [ ] 결선에서 **1위 통과 후 5초 카운트다운**이 돌고, 끝나면 곧바로
      결과 발표로 넘어가는지(2026-08-08 추가)

> ~~모바일에서 R3에 80 몰아주기가 저장되는지~~ — 확신도 배분은 §13-2로 제거됐다.

---

## 12. (2026-08-07) 남은 작업 목록 — 다음 세션에서 이어서

> **[2026-08-07 갱신] 12-1 / 12-2 / 12-3은 구현·커밋 완료됐다.** 남은 것은
> 12-4(사용자가 명시적으로 뒤로 미룬 항목, 착수 전 확인 필요)와
> 12-5(합성 BGM 유지로 결론 — 구현 대상 아님)뿐이다. 각 항목 제목의
> 상태 표시를 참고할 것.
>
> **의사결정 원칙**: 사용자 확인이 필요한 갈림길은 "권장안대로 먼저
> 구현하고, 그 근거를 여기에 남긴다"로 합의됐다(2026-08-07). 아래 각
> 항목의 "결정" 줄이 그 기록이다.
>
> **[2026-08-08 갱신, 최종] §12의 인수인계 항목이 전부 완료됐다
> (12-1~12-4, 12-6~12-9).** 12-6(라운드 속도/시간), 12-4(장애물 실제
> 순위 반영 + `/verify` 제거), 12-8(1·2라운드 결승선 컷오프
> 카운트다운), 12-9(MC {team} 버그 수정 + 보이스 톤 재확인), 12-7
> (무대/모바일 게임 설명 보강) 전부 같은 세션에서 구현·테스트·문서화
> 까지 끝내고 커밋·푸시했다. 12-5(외부 음원)만 "합성 BGM 유지"로
> 결론이 나 구현 대상이 아니다. 각 항목 다 아래에 상세를 적어뒀다.

### 12-1. ⑨ 모바일 라운드별 점수 — 전 항목 + 소계/총계 ✅ 완료(2026-08-07)

**요청**: "각 라운드마다 얻은 점수 중 예측으로 얻은 점수는 나오는데,
팀의 승리나 나의 등수로 얻은 포인트도 함께 보여주고, 각 라운드별 소계와
총계도 모두 보여달라."

**현황**: 서버는 이미 필요한 데이터를 전부 내려주고 있다 —
`PredictionCard.rewards[round]`에 `{hit_rank, predict, finish, team_bonus,
team_rank, final, total}`이 항목별로 들어 있고 `/api/predict/me`로 전달된다.
`static/mobile.js: renderRewardBreakdown()`이 이미 항목별로 그리고 있으나
**라운드 소계는 표시하되 3라운드 누적 총계가 없고**, 아직 채점되지 않은
라운드는 아무것도 안 보여준다.

**할 일**(프런트 전용, 백엔드 변경 불필요):
- 라운드 카드마다 "이번 라운드 소계"를 항상 표기(미채점이면 `-`).
- 예측 카드 목록 아래에 **누적 총계 패널** 신설: 라운드별 소계 3줄 +
  총계 1줄, 각 줄을 예측/통과/팀/결선으로 다시 쪼개 표시.
- `card.score`와 총계가 항상 일치하는지 화면에서 검증(불일치 시 서버
  값인 `card.score`를 신뢰).

**결정**: 백엔드 스키마는 건드리지 않는다. `rewards`가 이미 항목별
내역을 담고 있어 프런트 집계만으로 요구사항이 전부 충족되고, 서버
스키마를 바꾸면 스냅샷 호환성(`load_dict`)까지 손봐야 해 위험 대비
이득이 없다.

### 12-2. ⑩ 무대 화면 — 라운드 전환기 정보 패널 ✅ 완료(2026-08-07)

**요청**: "2라운드 선택할 때는 1라운드 결과 팀별로 남아 있는 카트 수를,
3라운드 고르는 시간에는 2라운드에서 결선 진출한 카트들의 등수를 보여줘.
각 라운드가 끝날 때마다 그 결과도 보여줘."

**현황**: `round_revealed` WS 메시지에 `pass_ids`와
`department_pass_rate`가 이미 실려 있다. 무대는 지금 배너 한 줄
("ROUND N 통과!")만 띄우고 지나간다.

**할 일**:
- 백엔드: `round_revealed`에 `survivors_by_department`(부서명 → 생존
  수)와, R2 종료 시 `finalists`(결선 진출자 id를 등수 순으로)를 추가.
  둘 다 `draw.round_pass_ids`와 `draw.snapshot["departments"]`에서
  파생 가능하므로 새 계산 로직은 거의 없다.
- 프런트(`stage.js`): 선택 창 구간(`score_r1_select_r2` /
  `score_r2_select_r3`) 동안 화면 중앙에 전환기 패널을 띄운다.
  - R2 선택 중 → **팀별 생존 카트 수** 막대(내림차순)
  - R3 선택 중 → **결선 진출자 등수표**(1위부터, 부서색 뱃지)
  - 각 라운드 종료 직후 → 통과/탈락 요약(통과 N명, 부서별 통과율 상위 3)
- 이 패널은 예측 선택 창과 같은 구간에 뜨므로, 참가자가 폰에서 고를 때
  참고 자료가 된다(예측 게임의 정보 비대칭을 줄이는 효과).

**결정**: 기존 `round_revealed` 메시지를 확장하는 방식을 택한다.
새 메시지 타입을 만들면 재접속 시 놓친 이벤트를 복구할 경로를 따로
만들어야 하는데, `round_revealed`는 이미 `/api/session`의 draw 스냅샷
으로부터 재구성 가능해 그 문제가 없다.

### 12-3. ⑤ 카트 능력을 "유의미한 선택"으로 ✅ 완료(2026-08-07)

**요청**: "초기에 선택하는 카트 능력이 지금은 골라도 유의미한 변화가
없다. 어떻게 유의미한 선택지로 만들 수 있을까?"

**제약**: 카트 능력이 **레이스 순위**에 영향을 주면 안 된다 — 능력은
참가자가 자유롭게 고르는 값이라, 순위에 영향을 주는 순간 "능력 잘 고른
사람이 유리한 추첨"이 되어 공정성이 깨진다(장애물과는 성격이 다르다.
장애물은 시드에서 파생되지만 능력은 사람이 고른다).

**권장안 — 예측 게임 레이어에서 의미를 준다**: 능력마다 **점수 규칙을
살짝 비트는 특성**을 부여한다. 레이스 결과는 전혀 안 건드리고, 같은
예측을 해도 능력에 따라 점수가 달라지므로 "고르는 재미"가 생긴다.
초안(8종, `app/characters.py`의 기존 id 그대로):

| 능력 | 특성 | 노림수 |
|---|---|---|
| 🔥 니트로 부스트 | R1 예측 점수 +25% | 초반 집중형 |
| 🛡️ 배리어 실드 | 순위표 밖(참여 보상) 시 최소 점수 2배 | 안정 지향 |
| ⚡ 스파크 러시 | 1위 정확 적중 시 +15% 추가 | 하이리스크 |
| 🌪️ 슬립스트림 | 내가 고른 팀이 2~3위일 때 +30% | 차점 노림 |
| 💎 럭키 드래프트 | 소수파 보너스 배수 +0.3 | 역배 전문 |
| 🌊 타이달 웨이브 | 팀 순위 보상 +50% | 팀플레이형 |
| ⭐ 스타더스트 | 결승선 통과 보상(+20) 2배 | 생존 보상형 |
| 🚀 로켓 대시 | R3 예측 점수 +20% | 막판 역전형 |

- 구현 위치: `app/predictions.py: score_round()`에서 `character_choices`
  를 참조해 배수를 적용. 카드에 `character` 필드를 추가하거나
  `main.py`의 `character_choices`를 인자로 넘긴다.
- 모바일 캐릭터 선택 화면에 특성 설명을 함께 노출해야 "선택"이 된다.
- 미선택자는 부서 기반 폴백을 그대로 쓰되, **폴백에는 배수를 적용하지
  않는다**(안 고른 사람이 우연히 유리해지면 안 되므로 중립 1.0배).

**결정**: 레이스가 아니라 예측 점수 레이어에서 의미를 주는 쪽으로 간다.
(a) 순위 공정성을 건드리지 않고, (b) 이미 "예측 리더보드가 당첨자를
정한다"는 구조라 점수에 영향을 주면 실제로 유의미하며, (c) 구현이
`score_round` 한 곳에 모인다. 다만 **배수 폭은 ±30% 이내로 제한**한다 —
그 이상이면 능력 선택이 예측 실력을 압도해 "무엇을 맞혔는가"보다
"무엇을 골랐는가"가 중요해진다.

### 12-1~12-3 구현 결과 요약 (2026-08-07 세션)

세 항목 모두 위에 적힌 "결정"을 그대로 따라 구현했다. 파이썬 테스트
291개 통과(신규 9개 추가). 구현하면서 사용자가 추가로 요청한 3건도
함께 처리했다.

| 항목 | 결과 |
|---|---|
| 12-1 | 라운드 카드에 소계 상시 표기(미채점은 `-`) + 예측/통과/팀/결선 4항목 x 3라운드 표와 총계. 백엔드 스키마 변경 없음 |
| 12-2 | `round_revealed`에 `survivors_by_department`/`finalists` 추가, 무대 중앙에 전환기 패널. R1 선택 구간에도 팀별 참가 카트 수를 띄운다 |
| 12-3 | `app/characters.py`에 `AbilityEffect` 8종, `score_round(character_by_pid=...)`로 적용. 미선택자는 중립 1.0배 |
| 추가 | 폰에도 후보 판단 지표(`/api/predict/me`의 `candidate_stats`) |
| 추가 | 무대·모바일에 레이스 시작 전 게임 소개/참여 방법 |
| 추가 | 스타트 라이트를 실제 F1 절차로(5열 x 2단 갠트리, 불규칙한 정적, 소등음 재작성). `RACE_COUNTDOWN_SECONDS` 2.8 -> 5.2초 |

**주의**: 스타트 라이트 연출 길이와 `app/main.py`의
`RACE_COUNTDOWN_SECONDS`는 **반드시 함께 움직여야 한다**. 연출이 더
길어지면 라이트가 켜져 있는 동안 카트가 이미 달려나가는 것이 보인다
(과거에 실제로 발생했던 버그). `static/stage.js`의 `LIGHT_*` 상수 위
주석에 예산 계산이 적혀 있다.

### 12-4. 장애물이 실제 순위에 영향 + `/verify` 제거 ✅ 완료(2026-08-08)

**요청**: "장애물에 부딪히면 카트 속도나 순위에 실제 영향을 받게 해달라",
이어서 "verify 같은 기능은 필요 없다. 커밋 해시도 보여줄 필요 없다.
중간 순위뿐 아니라 전체 카트에 모두 적용되도록."

**[2026-08-08 확정]** 실제 플레이 후 같은 요청이 다시 나와("장애물을 맞아도
카트 위치가 실제로 바뀌어야 하는데 안 바뀐다"), 착수 전 확인 질문으로
"시각 효과만 강화" vs "실제 순위에도 영향(12-4 전체 실행)" 두 선택지를
제시했고 사용자가 **후자를 명확히 선택**했다. 보류를 해제하고 다음
세션에서 아래 "할 일" 순서 그대로 착수한다. 착수 전 유의점(공정성 논증
변경, 되돌리기 어려운 삭제, 브랜치 분리 권장)은 여전히 유효하니 시작
전에 다시 한번 읽을 것.

**현황**: 장애물은 100% 클라이언트 연출이다. 순위·통과 판정은
`fairness.py`가 커밋된 시드만으로 계산하고, `race.py`는 그 결과로
수렴하는 애니메이션만 그린다.

**할 일**(순서 중요):
1. `race.py`에 시드 파생 장애물 배치(`obstacle_layout`)와 충돌 감속
   (`drag_at`)을 추가. 카트 레인도 시드에서 파생시켜 충돌 여부가
   결정론적이 되게 한다. (이 코드는 한 번 작성했다가 범위 조정으로
   되돌렸다 — 커밋 이력에서 복원 가능.)
2. `fairness.py`의 순위 산출에 **장애물 누적 페널티를 접어넣어** 최종
   순위 자체가 바뀌게 한다. 순환 참조를 피하려면: 기본 순위 → 기본
   목표 위치 → 충돌 판정 → 페널티 → 최종 순위(1패스).
3. `/verify` 페이지, 커밋 해시 배지(무대·운영 콘솔), `static/fairness.js`,
   `tests/test_js_fairness_parity.py`를 제거.
4. 영향받는 테스트 정리(현재 282개 중 fairness/race 계열 다수).

**결정 및 유의점**: 사용자가 "남은 TODO 먼저"를 선택해 이 작업은
**뒤로 미뤘다**. 착수 전 반드시 인지할 것 —
- 공모전 심사 기준에 **"공정성"**이 있고, 현재 제출물의 핵심 논리가
  "커밋-리빌로 사전 봉인 + 누구나 재계산 검증"이다. `/verify`를 없애면
  그 논증 수단이 사라진다.
- 다만 장애물을 **시드에서 파생**시키면 결과는 여전히 100% 결정론적
  이므로 "조작 불가능"이라는 성질 자체는 유지된다. 문서(README·게임소개)
  의 공정성 서술을 "사전 봉인 + 사후 검증" → "시드 기반 완전 결정론"
  으로 함께 고쳐 써야 앞뒤가 맞는다.
- 되돌리기 어려운 삭제가 포함되므로, 착수 전 브랜치를 따로 두는 편이
  안전하다.

**[2026-08-08 구현 결과]** 위 "할 일" 순서 그대로 착수했다. 실제로는
"1번(장애물 배치) → 2번(순위 반영)"을 **레인 도달 여부 판정 없이
레인 일치만으로** 구현했다(§4의 "할 일" 초안은 `drag_at`처럼 진행률
도달 여부까지 따지는 모델을 제안했지만, 그러면 target_position이
낮은 후순위 카트일수록 장애물 구간에 못 미쳐 페널티를 덜 받게 되어
"전체 카트에 모두 적용"이라는 원래 요청과 어긋난다는 걸 설계 중
발견했다 — 그래서 도달 여부 무관, 레인 일치만으로 맞는 더 단순한
모델로 바꿨다). 브랜치 분리는 하지 않고 main에 직접 커밋했다(회사
사내용 저장소, 이 프로젝트의 기존 관례가 항상 main 직접 커밋이었고
git 이력으로 언제든 되돌릴 수 있어 충분하다고 판단).

- **`app/race.py`**: `LANE_COUNT=8`, `HAZARDS_PER_ROUND=10`,
  `OBSTACLE_CATALOG`(종류별 페널티), `lane_for`(시드+참가자+라운드 →
  고정 레인), `obstacle_layout`(시드+라운드 → 이 라운드 장애물 배치),
  `total_obstacle_penalty`(3라운드 합산, `OBSTACLE_PENALTY_CAP=0.4`로
  상한), `position_at(..., seed=)`(장애물에 맞는 순간부터 실제로 위치가
  내려갔다가 `progress_ratio=1.0`에서 정확히 target으로 선형 회복),
  `compute_effects`(이 틱에 누가 맞고 있는지).
- **`app/fairness.py`**: `_obstacle_adjusted_ranking`이 기본 HMAC 순위
  (0..1로 정규화)에 장애물 페널티를 더해 재정렬 — 이게 최종 `ranking`이
  되고, 나머지(`round_pass_ids`/`winners`)는 이 리스트를 그대로 자르던
  기존 구조를 그대로 재사용해 리플이 최소화됐다.
- **`app/main.py`**: `/api/verify/{draw_index}`·`/verify` 라우트 제거.
  `_run_race_phase`가 매 라운드 `obstacle_layout`을 한 번 계산해 매 틱
  `race_tick`에 `obstacles`(고정 배치)·`effects`(현재 피격자)로 실어
  보낸다. `draw.seed`는 리빌 전에도 서버 내부에서만 쓰이고(기존
  `_lock_round`와 같은 패턴), 클라이언트에는 절대 안 나간다.
- **`static/stage.js`**: 클라이언트가 `Math.random()`으로 장애물을
  뿌리고 직접 충돌 판정하던 코드(`maybeSpawnObstacle`/`applyKartEffect`/
  `kartEffectStateFor`(구버전)/`EFFECT_FADE_TAIL`)를 전부 제거하고,
  서버가 보낸 `obstacles`/`effects`를 그대로 렌더링하도록 바꿨다.
  `positions[pid]`에 이미 실제 감속이 반영돼 있어 "보이는 위치"를
  따로 흔드는 로직이 필요 없어졌다(카트끼리 추월하는 잔물결
  `jockeyOffsetFor`는 장애물과 무관한 별개 연출이라 그대로 둠).
- **제거**: `static/verify.html`·`verify.js`·`fairness.js`,
  `tests/test_js_fairness_parity.py`, 무대 커밋 배지(`#commit-badge`),
  운영 콘솔의 커밋 해시/시드 표시, `/verify` 링크. `app/fairness.py`의
  `recompute_from_reveal`/`verify_draw` 자체는 내부 정합성 점검용
  함수로 남겨뒀다(더 이상 HTTP로 노출되지 않을 뿐).
- **MC 멘트**(`app/mc.py`): "커밋 해시가 증거", "QR로 검증", "재계산으로
  확인" 등 `/verify`를 전제로 한 대사를 "시드 기반 완전 결정론" 서술로
  교체.
- **문서**: README·게임소개·게임구조의 공정성 서술을 "사전 봉인 + 사후
  검증(`/verify`)"에서 "시드 기반 완전 결정론"으로 다시 썼다(위 유의점에
  적어둔 대로). 게임소개.md의 "장애물은 순위에 영향 없다" 설명도
  "실제로 순위가 밀린다"로 정정했다 — 이건 참가자에게 보여주는 문서라
  틀린 채로 두면 안 되는 부분이었다.
- **테스트**: `tests/test_race.py`에 결정론·페널티 상한·"결승선에서 항상
  target과 정확히 일치"·"장애물이 실제로 최종 순위를 바꾼다"(20개 시드
  반복)를 검증하는 테스트 추가. 기존 `test_fairness.py`의 중첩 생존
  집합·결정론 테스트는 수정 없이 그대로 통과(설계가 "순위 리스트를
  한 번 더 정렬"하는 방식이라 하위 구조를 안 건드렸기 때문). pytest
  291개 통과.
- **실제 서버로 검증**: Playwright(헤드리스 Chromium)로 서버를 띄우고
  커밋 → 레이스 시작 → R1~R3 전체를 실제로 돌려 `race_tick`에
  `obstacles`/`effects`가 정상적으로 채워지는지, 무대 화면에 장애물
  스프라이트·피격 마크·MC 멘트가 정상 렌더링되는지, 콘솔 에러가
  없는지 확인했다.

### 12-5. ⑧ 후속 — 실제 음원 파일로 교체하는 옵션 ⏹ 구현 안 함(결론)

현재 BGM은 전부 WebAudio 합성이다(외부 파일 0개 → 오프라인·불안정
네트워크에서도 항상 재생된다는 게 장점). 사용자가 "필요하면 무료로
적용할 수 있는 링크나 방법"을 물어서 조사한 결과를 남긴다.

**상업적 이용까지 무료(출처 표기 조건 확인 필요)**:
- **Pixabay Music** (https://pixabay.com/music/) — Pixabay 라이선스,
  출처 표기 불필요. "racing / electronic / driving" 검색. 가장 무난.
- **Incompetech** (https://incompetech.com/music/royalty-free/) —
  Kevin MacLeod, CC BY 4.0. 출처 표기 필수. 레이싱용은 "Rocket" 계열.
- **Free Music Archive** (https://freemusicarchive.org/) — 곡마다
  라이선스가 다르므로 CC0/CC BY만 골라야 한다.
- **OpenGameArt** (https://opengameart.org/) — 게임용 루프가 많고
  CC0 필터가 있다.
- **Chosic** (https://www.chosic.com/free-music/all/) — 위 소스들을
  라이선스별로 모아둔 검색 포털.

**적용 방법(권장 구현)**: `static/bgm/{scene}.mp3`(scene = idle /
standby / anticipation / race1 / race2 / race3 / victory)를 두고,
`audio.js`의 `playScene()`이 해당 파일이 있으면 `HTMLAudioElement`
루프로 재생하고 없으면 지금의 합성 트랙으로 폴백하게 한다. 파일이
없어도 동작이 100% 유지되므로 저장소에 음원을 커밋하지 않아도 된다
(용량·라이선스 문제 회피). 행사 PC에서만 파일을 넣어 쓰는 운용이 가장
깔끔하다.

**결정**: 합성 BGM을 기본값으로 유지한다. 외부 파일은 "있으면 쓰는"
선택지로만 둔다 — 오프라인 보장이 이 프로젝트의 명시적 설계 원칙이고,
공모전 제출물에 라이선스가 걸린 바이너리를 넣지 않는 편이 안전하다.

### 12-6. 1라운드 카트 속도 완화 + 라운드별 레이스 시간 단축 ✅ 완료(2026-08-08)

**요청**: "1라운드에서 카트들의 속도가 너무 빨라. 각 라운드의 진행 시간을
줄여 줘."

**원인 분석**: `app/race.py: position_at`은
`target * progress_ratio ** pace_exponent`로 카트 위치를 계산하는데,
`pace_exponent`가 1보다 작은 카트(예전 하한 0.55)는 `progress_ratio`가
0에 가까울 때 기울기가 매우 가팔라(0.55일 때 진행률 5%만 지나도 목표
위치의 21%까지 도달) 출발 직후 화면을 순간이동하듯 가로지르는 것처럼
보였다. 특히 R1은 카메라가 선두를 추적하므로 이 효과가 도드라졌다.
단순히 라운드 길이만 줄이면 진행률(ratio)이 실시간 대비 더 빨리 흘러
체감 속도가 오히려 올라가므로, 속도 원인(페이스 곡선)과 길이 단축
두 가지를 함께 손봐야 의도한 결과가 난다.

**한 일**:
1. `app/race.py: pace_exponent` 하한을 0.55 → **0.8**로 올림
   (`0.8 + noise*0.9`, 범위 0.8~1.7). 초반 급가속(무한대에 가까운 초기
   기울기)을 없애면서도 추월 연출에 필요한 페이스 편차는 유지.
2. `app/director.py: _BASE_SEGMENTS`의 `race_r1`/`race_r2`/`race_r3`
   기준 초를 55/55/40 → **42/42/30**으로 축소(기준 합 300 → 264초).
   기본 총 시간 600초 기준 라운드 실제 길이가 약 13~15% 짧아지고,
   회수된 시간은 스케일링에 의해 나머지 구간(선택창·발표 등)에 고르게
   재배분된다.
3. `static/stage.js: CHASE_SCALE_BY_ROUND`(카메라 줌 배율)를 라운드
   길이가 줄어든 비율만큼 낮춤(1.15/1.45/1.6 → **1.0/1.26/1.36**).
   체감 속도 = 호 길이 × 줌 / 라운드 시간이므로, 시간이 줄어든 만큼
   줌도 같이 낮추지 않으면 오히려 더 빨라 보인다.

**테스트**: `tests/test_director.py`의 기본 600초 비율 검증값을 새
기준(264초 합)에 맞게 갱신. 전체 pytest 291개 통과(회귀 없음).

### 12-7. 무대/모바일 게임 설명 보강 ✅ 완료(2026-08-08)

**요청**: "게임에 대한 설명이 전체 화면과 모바일 모두에서 불충분해.
처음 게임을 하는 사람들이 더 쉽게 게임에 참여하게 하기 위한 보강을
해 줘."

**현황**: 게임 소개는 이미 있지만 노출 시점이 좁다.
- **무대(`static/stage.html`)**: `#overlay-waiting`(커밋 대기)과
  `#overlay-committed`(커밋 공개) 두 오버레이에만 소개/참여 방법이
  있다. 레이스가 시작되면(HUD만 남고 오버레이가 사라지면) 처음 온
  사람이 "지금 뭘 보고 있는 건지"(포지션 타워·부서 통과율·통과선·
  장애물 의미 등) 다시 확인할 방법이 없다.
- **모바일(`static/mobile.html`)**: `#intro-box`(`<details>`)가 상단에
  있어 처음엔 보이지만, 온보딩·게임 화면 콘텐츠가 쌓이면서 아래로
  밀려나고 별도로 다시 펼치기 전엔 눈에 안 띈다.

**할 일(제안, 착수 전 세부 방향은 자유롭게 조정 가능)**:
1. 무대 HUD에 "❓" 같은 상시 노출 버튼을 추가해, 레이스 중에도 언제든
   짧은 설명 패널(포지션 타워/통과선/부서 통과율/장애물이 무엇을
   의미하는지)을 다시 열어볼 수 있게 한다.
2. 모바일 `#intro-box`를 게임 화면(`#game-view`) 상단에도 접이식으로
   다시 노출하거나, 상단 고정 미니 버튼으로 항상 접근 가능하게 한다.
3. **[2026-08-08 갱신] 12-4가 완료됐다** — "장애물에 맞으면 실제로
   순위가 밀린다"는 문구를 쓰면 된다(게임소개.md §2·§4는 이미 이렇게
   고쳐뒀다). 무대/모바일 UI 쪽 설명 문구를 쓸 때도 같은 톤으로 맞출 것.
   **[2026-08-08 추가 갱신] 12-8(결승선 컷오프)도 완료됐다** — R1/R2
   설명에 "1등 통과 후 10초/5초 안에 못 들어오면 탈락"도 반영해야 한다.

**[2026-08-08 구현 결과]** 위 할 일 1·2번 그대로 구현했다.
- `static/stage.html`·`stage.js`: HUD에 "❓" 버튼(단축키 `H`, `Esc`로
  닫기)을 추가해 `#help-panel`을 토글한다. 내용은 기존
  `overlay-waiting`의 소개 문구를 재사용하되, "화면 보는 법"
  (POSITION/부서 통과율/장애물/결승선 카운트다운이 각각 뭘 뜻하는지)
  절을 새로 얹었다. `.round-transition`과 같은 화면 중앙 글래스 패널
  톤이지만 z-index를 더 높여(50) 다른 오버레이와 겹쳐도 항상 위에
  뜨게 했다.
- `static/mobile.html`·`mobile.js`: `#intro-box`를 복제하지 않고,
  `#game-view` 안에 "📖 게임 설명 다시 보기" 링크 버튼만 추가해 클릭
  시 `introBoxEl.open = true` + `scrollIntoView`로 기존 콘텐츠를 그
  자리에서 다시 펼친다(콘텐츠 중복 없음 → 나중에 문구가 어긋날 걱정이
  없다).
- 두 화면의 소개 문구 전체를 §12-4/§12-8에 맞춰 다시 점검해 "장애물은
  연출일 뿐" 같은 이제는 틀린 문장이 하나도 안 남게 했다.

**검증**: Playwright로 무대에서 ❓ 클릭 → 패널 표시 → `Esc`로 닫힘,
모바일에서 온보딩 완료 후 "설명 다시 보기" 클릭 → intro-box 재확장 +
스크롤을 확인. 콘솔 에러 0건. pytest 302개 통과(순수 프론트엔드 변경).

**이것으로 §12의 인수인계 항목(12-1~12-9)이 전부 완료됐다.**

### 12-8. 1·2라운드 결승선 컷오프(카운트다운) ✅ 완료(2026-08-08)

**요청**: "1 라운드의 경우, 결승선을 통과한 차가 발생할 경우, 그로부터
5초의 카운트다운을 세서 그 시간 제한 내 들어온 카트들만 다음 라운드로
진출하는 것으로 해 줘." 착수 전 확인 질문(순위 후보군 안에서 필터링
vs 순위 개념 없이 시간만으로 결정)에 사용자가 "기존대로 1라운드는
전체 화면에 100개 카트 중 몇 개가 들어왔는지 실시간으로 N/100 식으로
보여 달라. 첫 차 기준 카운트다운은 약 10초로 해 달라. 즉 2개 기준(순위
개념 + 시간 제한 개념)"으로 확정. 이어서 "2라운드의 경우엔 5초의 시간
제한을 추가해 줘 -- 이것도 2라운드 첫 차 결승선 통과 후 카운트다운"
으로 R2까지 확장.

**확정된 규칙**: R1/R2는 기존 순위 상위 후보군(R1 100명 / R2 F명)은
그대로 두고, 그 후보군 안에서 **1등이 결승선을 통과한 시점부터 정해진
시간(R1 10초 / R2 5초) 안에 결승선을 넘지 못하면 탈락**한다. 순위
조건과 시간 조건 둘 다 만족해야 하는 AND 조건. R3(결선)는 요청 범위
밖이라 무변경.

**구현**:
1. `app/race.py: crossing_ratio` -- 카트가 결승선(pass_line)에 처음
   도달하는 progress_ratio를 선형 스캔으로 구한다(장애물 dip 때문에
   곡선이 국소적으로 흔들릴 수 있어 이분탐색은 위험 판단). `steps=500`
   기본값이면 ~95초 라운드에서 0.2초 미만 오차. `obstacle_layout`에
   `lru_cache`를 걸어 카트마다 반복 호출되는 성능 문제를 미리 막음.
2. `app/fairness.py: R1_CUTOFF_WINDOW_SECONDS=10.0`,
   `R2_CUTOFF_WINDOW_SECONDS=5.0`, `_apply_cutoff_window`가 순위
   후보군 중 "첫 통과자 + 창" 밖으로 밀린 카트를 걸러낸다.
   **정원 미달 방지 안전장치**: 컷오프 결과가 최소 인원(R1은
   `finalist_count`, R2는 `draw_count`)보다 적으면 순위 순으로
   모자란 만큼 자동으로 채운다 -- "레이스가 너무 극단적으로 갈려서
   상품을 못 나눠준다"는 사고를 구조적으로 차단한다. `_compute_outcome`
   에 `race_r1_seconds`/`race_r2_seconds`(선택 인자, 없으면 컷오프
   자체를 건너뛰어 기존 동작과 100% 동일 -- 대부분의 기존 테스트가
   이 경로라 회귀가 없었다)를 추가하고, 결과에 `round_candidate_count`
   (컷오프 "전" 순위 기준 후보군 크기 -- 통과선 위치 계산과 무대의
   "N/후보수" 카운터가 쓴다)를 새로 담아 돌려준다. `build_snapshot`에도
   이 두 초 값을 포함시켜, 리빌 후 `recompute_from_reveal`이 커밋
   시점과 동일한 컷오프 결과를 재현하도록 함.
3. `app/models.py: DrawResult.round_candidate_count` 필드 신설.
4. `app/main.py`: `commit_draw`/`redraw`/`demo_start` 세 경로 모두
   `_race_cutoff_seconds_for_commit`으로 런북에서 race_r1/r2의 실제
   레이스 시간(스타트 라이트 리드인 제외, `_run_race_phase`가 쓰는
   것과 동일한 공식으로 통일)을 구해 `fairness.compute_draw`에 넘긴다.
   `_run_race_phase`의 통과선(`pass_line`) 계산도 `round_pass_ids`의
   실제(컷오프 후) 길이가 아니라 `round_candidate_count`(컷오프 전
   후보군 크기)를 쓰도록 바꿨다 -- 안 바꾸면 시간에 밀려 탈락한
   카트가 화면에서 아예 결승선에 못 미치는 것처럼 보여 "1등 통과 후
   카운트다운"이라는 연출의 전제 자체가 깨진다. `race_tick`에
   `candidate_count`/`cutoff_window_seconds`를 추가(둘 다 공개 정보로
   계산 가능해 리빌 전에 노출해도 안전).
5. `static/stage.js`: 무대 상단에 컷오프 패널 신설(`updateCutoffPanel`).
   "1등 결승 통과" 순간 자체는 서버가 알려주는 게 아니라 기존 통과선
   감지 로직(`crossedPassLine`, 원래 통과 사운드용으로 있던 것)을
   재사용해 클라이언트가 스스로 감지한다. 창이 열려 있는 동안은
   실시간으로 세던 통과 인원을, 창이 닫히면 그 순간 값으로 얼려서
   보여준다(마감 후에도 레이스는 끝까지 진행돼 karts가 결승선을 계속
   넘지만, 그건 컷오프에 못 든 카트들이라 살아있는 숫자에 넣으면 실제
   통과 인원보다 부풀려 보인다).

**테스트**: `tests/test_race.py`에 `crossing_ratio` 결정론/단조성/None
케이스 테스트, `tests/test_fairness.py`에 "타이밍 인자 없으면 컷오프
없음(회귀 없음)"·"실제로 후보를 탈락시킴"·"정원 미달 안전장치 작동"·
"중첩 생존 집합 불변식 유지"·"결정론"·"리빌 재계산 재현" 테스트 추가.
pytest 302개 통과.

**실서버 검증**: 웹소켓 원시 틱을 직접 관찰해 이론값(순위 상위 100명
후보 중 10초 창 통과자 ~16명)과 실제 서버 결과(`round_pass_ids[1]`
길이 16)가 일치함을 확인. Playwright로 무대 화면에서 카운트다운이
정상적으로 줄어들고("8.8s" → "0.0s") 카운터가 올라간 뒤("2/100" →
"11/100") 마감과 함께 정확히 얼어붙는 것을 스크린샷으로 확인.

### 12-9. MC 멘트 미입력 플레이스홀더 버그 + 남성 보이스 톤 재확인 ✅ 완료(2026-08-08)

**요청**: "게임하다보면 AI MC 텍스트에 {{team}} 뭐 이렇게 표시되는 게
있던데 그거 수정해 주고" + "AI MC 목소리 톤을 전에 말했던 것처럼
스포츠 캐스터와 같이 남자 톤으로 해 달라고 했었는데 그거 제대로
적용되었는지 확인해 주고."

**{team} 버그 원인**: `MCAgent.pick_line`이 태그 하나당 셔플백
하나만 관리해, 그 태그 풀에 `{team}`/`{winner_count}` 같은 플레이스홀더가
필요한 줄이 섞여 있으면 값을 안 준 호출에서도 뽑힐 수 있었다. 실제로
`close_call`(`tryShowLiveMcLine("close_call")`, team 없이 호출)과
`race_progress`, `final_announce` 등 여러 호출부가 이 문제를 갖고
있었다.

**수정**: `pick_line`이 이번 호출에 실제로 값이 들어온 플레이스홀더만
요구하는 줄로 후보를 먼저 거른 뒤 셔플백을 돈다. 같은 태그라도 호출마다
넘기는 파라미터 조합이 다를 수 있어 셔플백을 (태그, 파라미터 키
조합) 단위로 따로 관리한다. `_validate_llm_templates`도 같은 헬퍼
(`_required_fields`)로 정리하면서, 중괄호가 깨진 LLM 응답도 함께
거르도록 보강.

**보이스 톤 확인 결과**: 코드(`static/stage.js: KO_MALE_VOICE_HINTS`,
`MC_VOICE_PITCH_SHIFT=-0.28`)는 설계대로 정확히 동작 중 -- 남성 이름
힌트로 한국어 음성을 찾고, 못 찾으면 피치를 낮춰 최대한 중저음에
가깝게 보정한다. 이 로직은 `speak()` 하나로 모든 MC 발화가 지나가므로
빠짐없이 적용된다(재확인 완료, 코드 변경 없음).
다만 **개발 PC(Windows)에는 한국어 TTS 음성이 "Microsoft
Heami"(여성) 하나뿐**이라(Playwright로 `speechSynthesis.getVoices()`
직접 조회해 확인), 실제로는 "피치를 낮춘 여성 음성"으로 들린다.
**진짜 남성 음성이 필요하면 행사 PC의 Windows 설정(설정 → 시간 및
언어 → 음성)에서 남성 한국어 음성을 추가로 설치해야 한다** -- 이건
Windows OS 설정이라 코드로 해결할 수 있는 범위 밖이다.

**테스트**: `tests/test_mc.py`에 "close_call을 30번 호출해도 `{team}`이
한 번도 안 남는다" 테스트 추가 + 기존 셔플백 회전 테스트를 새 필터링
동작에 맞게 갱신. pytest 302개 통과(12-8과 같은 커밋에 포함).

---

## 13. 설계 변경 이력 — "왜 지금 이 구조인가"

> **이 절의 목적**: 기준 문서(`타추위-추첨프로그램_게임구조.md`)는 "지금
> 어떻게 동작하는가"만 담아야 읽기 쉽다. 그런데 시도했다가 접은 안들은
> 나중에 같은 제안이 다시 올라왔을 때 판단 근거가 되므로 버릴 수 없다.
> 그래서 **과거 방식과 폐기 사유는 전부 이 절로 모았다**(2026-08-08,
> 사용자 요청: "게임 소개에서의 이전 방식에 관한 설명은 이력을 나타내는
> 문서로 옮겨달라"). 기준 문서에서 이 절을 참조한다.

### 13-1. 사이버머니 갬블링(패리뮤추얼 베팅) 모드 — 폐기

한때 "예측 게임"과 "사이버머니 갬블링" 두 모드가 있었고, 갬블링은
맞힌 사람끼리 판돈을 나눠 갖는 패리뮤추얼 방식이었다. 실제로 돌려본
결과 네 가지 문제가 확인돼 모드 자체를 제거했다:

1. **제로섬이라 전체 잔액이 늘지 않는다** — 다 같이 열심히 해도 제자리
2. **맞혀도 배당이 눌린다** — 정답 후보가 넓어 승자가 많으면 1.5~3배
3. **어렵다** — 20초 안에 "대상 + 금액" 두 번을 결정해야 한다
4. **안 걸면 그 라운드가 통째로 무효** — 머뭇거린 사람이 소외된다

"소수가 크게 딴다"도 "다수가 조금씩 받는다"도 아닌 어정쩡한 상태였고
사내 행사 성격과도 맞지 않았다. 용어(베팅/판돈/배당)도 전부 예측/점수로
교체했다. → **다시 제안된다면**: 사내 행사에서 "잃는다"는 감각 자체가
문제였다는 점을 먼저 확인할 것.

### 13-2. 확신도 100점 배분 — 폐기

예전에는 참가자가 확신도 100점을 3라운드에 직접 배분해야 했다. "누구를
고를지"와 "얼마를 걸지"를 동시에 판단해야 해서, 선택 창이 20~30초만
열리는 라이브 이벤트에는 불필요한 마찰이었다(2026-08-07 사용자 피드백).

지금은 라운드마다 대상 하나만 고르면 되고, 동일한 배점
(`ROUND_BASE_POINTS`)을 놓고 겨룬다. **막판 역전 드라마는 확신도 없이도
유지된다** — `ROUND_WEIGHTS`(1.0/1.5/3.0)가 이미 결선에 3배 가중치를
주므로 개인이 몰아주지 않아도 R3 하나로 순위가 크게 뒤집힌다.
제거된 것: `PredictionCard.alloc`, `set_allocation`, `/api/predict/allocate`,
모바일 확신도 슬라이더 UI.

### 13-3. 정답/오답 2지선다 채점 — 순위 차등으로 교체

예전 채점은 맞으면 만점, 틀리면 0점이었다. R1에서 8할이 0점을 받고 첫
라운드에 폰을 내려놓는 것이 문제였다. 지금은 고른 대상이 몇 등이었는지에
따라 100/60/35/20/10%를 받고 순위 밖이어도 5%(참여 보상)가 들어간다.
**아무도 0점이 아니되** 1위 적중과 꼴찌 사이 20배 격차로 변별력은 유지된다.

배점 크기도 한 번 올렸다: 사용자 피드백("예측으로 얻는 포인트가 너무
적다")에 따라 `ROUND_BASE_POINTS`를 100 → **300**으로 상향(2026-08-07).

### 13-4. 결선 "부서당 1대" — 폐기

결선 진출을 부서당 1명씩으로 바꾸는 안(Phase T3)을 검토·구현했다가
되돌렸다. 등수가 낮은 사람이 진출하고 높은 사람이 탈락하는 장면이
그대로 화면에 나오는데, 이건 "레이스 결과가 곧 순위"라는 신뢰를 정면으로
깎는다. 지금은 부서와 무관하게 **전체 순위 상위 F명**이 결선에 오른다.
자세한 경위는 §10.1(b).

### 13-5. 업그레이드 상점 — 폐기

사이버머니로 카트 글로우를 사는 순수 코스메틱 상점이 있었으나, 갬블링
모드 제거와 함께 사라졌다. 관련 테스트 15개도 함께 제거됐다.

### 13-6. `/verify` 공개 검증 페이지 — 제거

브라우저가 서버와 별개로 커밋·순위를 재계산해 대조하는 페이지가 있었다
(`static/verify.html`, `static/fairness.js`). 사용자 요청으로 §12-4에서
화면을 제거했다. **판정 로직 자체는 여전히 시드 하나로 100% 재현
가능한 순수 함수**이며(`app/fairness.py: recompute_from_reveal`/
`verify_draw`), `tests/test_fairness.py`의 위변조 탐지 테스트가 이를 계속
확인한다. 공정성 서술도 "사전 봉인 + 사후 검증" → "시드 기반 완전
결정론"으로 함께 고쳐 썼다.

### 13-7. 레이스 구간 시간 — 110/110/80초 → 95/95/68초

레이스가 늘어진다는 피드백에 따라 §12-6에서 단축했다. 회수한 시간은
발표·선택 구간에 재배분했다. 결선은 여기에 더해 **1위 통과 후 5초
카운트다운으로 조기 종료**되므로(2026-08-08) 실제로는 더 짧게 끝난다.

### 13-8. 장애물 — 연출 전용 → 실제 순위 반영

처음에는 100% 클라이언트 연출이었고 "부딪혀도 아무 일도 안 일어난다"는
것이 문제였다. §12-4에서 시드 파생 배치 + 실제 페널티로 바꿔 순위에
반영되게 했다. 이때 **참가자 행동이 아니라 커밋 시점의 시드에서만**
파생된다는 원칙은 유지했으므로, "결과가 커밋 시점에 고정된다"는 신뢰
장치는 그대로다.

2026-08-08 추가 조정(사용자 요청): 장애물이 결승선 **이후**에도 놓이던
문제를 고쳐 **결승선까지의 비율**로 배치하고, 균일 난수 대신 균등 슬롯 +
슬롯 내 지터를 써서 한곳에 뭉치지 않게 했다. 화면에서는 자기 레인 안에서
좌우로 흔들리며 회전한다(움직임 파라미터도 전부 시드 파생).

### 13-9. 결과발표+다음 라운드 대기 25초 요청 — 기각(30초 유지)

사용자 요청: "각 라운드별로, 게임이 종료된 후 결과 발표 및 다음 라운드까지의
대기 시간은 25초 이내로 해 줘." 이 구간(`score_rX_select_rY`)은 결과 발표
겸 다음 라운드 예측 선택 창이라, `director.py`의
`MIN_SELECTION_WINDOW_SECONDS = 30.0`(폰으로 고를 최소 시간 보장)이 하드
플로어로 걸려 있어 25초로 낮추면 이 최소값과 직접 충돌한다. 사용자에게
확인한 결과 **30초를 유지하기로 결정**(2026-08-10) — 선택 UX가 25초 단축
보다 우선한다. 코드 변경 없음.
