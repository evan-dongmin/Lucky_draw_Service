# 진행 상황 (다음 세션 이어하기용)

> 마지막 갱신: 2026-08-08. 이 문서는 지금까지
> 뭘 했고, 다음에 뭘 이어서 해야 하는지를 빠르게 파악하기 위한 것이다.
> 설계 근거·심사 전략은 `타추위-추첨프로그램_공모전_전략노트.md`, 참가자용
> 쉬운 설명은 `타추위-추첨프로그램_게임소개.md`를 참고.

---

## 0. 지금 당장 알아야 할 것 (2026-08-08 갱신 핵심 요약)

- **✅✅✅✅✅ 작업계획서 §12의 인수인계 항목이 전부 완료·커밋·
  푸시됐다(2026-08-08, 같은 세션). 12-1~12-4, 12-6~12-9 전부 끝났고
  12-5만 "구현 안 함"으로 결론 난 상태다 — §12에 더 이상 남은 일이
  없다.**
  - **§12-4**: 장애물이 이제 시드에서 결정론적으로 파생돼 실제 순위·
    트랙 위치에 영향을 준다(레인 일치 시 무조건 맞음 — 선두든 꼴찌든
    동일). `/verify` 페이지·커밋 해시 배지·`static/fairness.js`는
    제거됐고, 공정성 서술은 "커밋-리빌 + 사후 검증"에서 **"시드 기반
    완전 결정론"**으로 README·게임소개·게임구조 문서에 함께 반영했다.
  - **§12-6**: R1 초반 급가속 완화 + 라운드별 레이스 시간 단축.
  - **§12-8(신규)**: R1/R2에 "1등이 결승선을 통과한 시점부터 정해진
    시간(R1 10초/R2 5초) 안에 못 들어오면 탈락"하는 결승선 컷오프를
    추가했다. 기존 순위 상위 후보군(R1 100명/R2 F명) 조건과 AND로
    결합되고, 정원 미달을 막는 안전장치가 있다. 무대 화면 상단에
    실시간 "N/후보수" 카운터 + 카운트다운 패널이 뜬다.
  - **§12-9(신규)**: AI MC 멘트에 `{team}` 같은 미입력 플레이스홀더가
    그대로 남는 버그를 고쳤다(`pick_line`이 이제 값이 있는 플레이스홀더만
    요구하는 줄로 후보를 거른다). 남성 보이스 톤 로직은 재확인 결과
    정상 동작 중이나, 개발 PC에는 한국어 음성이 여성(Heami) 하나뿐이라
    실제로는 "피치를 낮춘 여성 음성"으로 들린다 — 진짜 남성 음성이
    필요하면 행사 PC에 남성 한국어 TTS 음성을 추가 설치해야 한다.
  - **§12-7**: 무대 HUD에 "❓" 상시 도움말 버튼(단축키 `H`) + 모바일
    게임 화면에 "설명 다시 보기" 링크를 추가해, 레이스가 시작된 뒤에도
    처음 온 사람이 게임 설명을 언제든 다시 볼 수 있게 했다.
  - 자세한 구현 내역은 §1 맨 뒤 "2026-08-08" 항목들과 작업계획서
    §12-4/§12-6~§12-9 참고.
- **⚠️ 스타트 라이트 연출 길이와 `app/main.py`의 `RACE_COUNTDOWN_SECONDS`
  (5.2초)는 반드시 함께 움직여야 한다.** 연출이 더 길어지면 라이트가 켜져
  있는 동안 카트가 이미 달려나가는 것이 보인다(과거 실제 버그).
  `static/stage.js`의 `LIGHT_*` 상수 위 주석에 예산 계산이 있다.
- **레이스 연출이 전면 개편됐다(2026-08-07).** 트랙이 x 단조 증가 리본
  에서 **진짜 서킷 레이아웃**(월드 좌표계 + 헤어핀)으로 바뀌었고,
  카메라가 전체 조망 → 선두 추적으로 부드럽게 줌인하며, 라운드 시작 전
  스타팅 그리드 장면이 추가됐다. 체감 속도도 라운드별로 62/60/80px/s로
  정규화했다(예전 62/115/234 — R3가 4배 빨라 눈으로 못 따라갔다). **[2026-08-08
  후속]** §12-6로 라운드 길이를 줄이면서 `CHASE_SCALE_BY_ROUND`도 같은
  비율로 낮춰 이 60~90px/s 대는 그대로 유지된다 — 현재 실제 값은
  `static/stage.js`의 `CHASE_SCALE_BY_ROUND` 주석 참고.
  자세한 내용은 §1의 44~46번.

- **⚠️⚠️⚠️ 확신도 배분을 완전히 없앴다(2026-08-07, 사용자 피드백).**
  예전엔 확신도 100점을 3라운드에 직접 배분해야 했는데, "누구를 고를지"
  와 "얼마를 걸지"를 동시에 판단해야 해서 선택 창이 짧게 열리는 라이브
  이벤트엔 불필요한 마찰이라는 의견을 드렸고 사용자가 동의했다. 지금은
  **라운드마다 대상 하나만 고르면 끝**이고, 라운드마다 동일한 고정
  배점(`ROUND_BASE_POINTS=100`)을 걸고 겨룬다. "막판 역전" 드라마는
  `ROUND_WEIGHTS`(1.0/1.5/3.0)만으로 그대로 유지된다 — 개인이 몰아줄
  필요가 없어졌을 뿐. `PredictionCard.alloc`/`set_allocation`/
  `/api/predict/allocate`/모바일 확신도 입력 UI를 전부 제거했다.
  자세한 내용은 §1의 43번. pytest 282개(alloc 검증 테스트 9개 제거,
  나머지는 새 채점식 기준으로 재작성).
- **레이스 박진감·속도감 강화(2026-08-07, 사용자 피드백).** 장애물
  개수 4~5배 증가 + 지속시간 연장 + 충돌 감속 체감 강화, R1도 줌-추적
  카메라로 전환(뒤처진 카트는 화면 밖으로 자연스럽게 빠짐), 맵 확대,
  카트끼리 계속 추월하며 선두가 바뀌는 잔물결, 결승선을 트랙 끝쪽에
  고정(+결승선 이후 장애물 제거). 전부 연출 전용이라 레이스 판정은
  무변경. 자세한 내용은 §1의 38~41번.
- **⚠️ 모바일 "내 카트 현황" 신설(2026-08-07, 사용자 요청).** 레이스가
  실제로 달리는 동안 폰에서 내 카트 실시간 등수·통과선 통과 여부(진행
  중이면 몇 % 왔는지)·소속 팀 실시간 순위·포인트 순위를 볼 수 있다.
  모바일에 위치 데이터를 통째로 뿌리지 않는다는 기존 원칙은 유지하면서,
  `latest_race_tick` 캐시 하나로 폴링 시점에 한 명분만 계산해 돌려주는
  방식으로 풀었다. 자세한 내용은 §1의 42번.
- **⚠️⚠️ 참여형 게임이 "예측 게임" 하나로 통일됐다(2026-08-07, 사용자
  요청).** 사이버머니 갬블링/패리뮤추얼 베팅 모드를 **완전히 삭제**했고,
  채점 방식도 정답/오답 이진에서 **순위 차등**으로 바꿨으며, 명단 전원이
  자동 참가된다. 아래가 핵심이니 이것만은 반드시 읽을 것 —
  자세한 내용은 §1의 32~34번, 규칙 상세는 게임구조 문서 §4~5.
  1. **순위 차등 채점** — 고른 대상이 몇 등이었는지에 따라 배점의
     100/60/35/20/10/5%를 받는다. **아무도 0점이 아니다.**
  2. **전원 자동 참가** — 커밋 시점에 명단 전원 카드 생성. R1·R2 미선택
     시 **자기 소속 부서**, R3는 무작위로 자동 배정. 폰을 안 들어도
     경품 대상에 남는다.
  3. **결선(R3) 배점 3배** — 라운드 가중치가 1.0/1.5/3.0으로, 결선이
     앞 두 라운드 합보다도 크다. 막판 역전이 실제로 성립한다(확신도
     배분은 위 항목대로 이후 제거됨).
- **⚠️ 핵심 설계가 바뀌었다(사용자 명시적 요청)**: 이제 **예측
  게임이 켜진 세션에서는, 레이스 결선 1등이 아니라 그 최종 리더보드
  상위 N명이 실제 경품 당첨자다.** 레이스/커밋-리빌/`/verify`는 여전히
  100% 그대로(무변경) — "레이스가 조작 없이 진행됐는가"만 검증한다.
  "누가 상품을 받는가"는 그 위에 새로 얹은 별도 단계(`app/main.py`의
  `_compute_prize_winners`, `DrawResult.prize_winners`/`prize_basis`)로
  분리했다. 자세한 설계/이유/구방식과의 비교는 §1의 16번 항목과
  전략노트 §7 참고 — **문서 전체(README/게임소개/verify.html/admin
  화면)를 이 변경에 맞춰 다시 썼다.**
- 라운드마다 트랙 모양이 다르게(R1/R2/R3 각각 다른 곡선), 모바일에서
  이모지 응원을 무대 화면에 날리는 기능을 추가했다(§1의 15번 항목).
- 어제 발견한 시상대 버그와 같은 계열의 진짜 버그 4건을 전체 코드베이스
  감사로 추가로 찾아 고쳤다(WS 재연결 후 admin 카메라 버튼이 영구히
  죽는 문제 등 — §1의 14번 항목 참고).
- **테스트는 328개 전부 통과**(322 + 이번에 추가한 prize_winners 6개).
- **Phase T(팀 대항전) 착수**: 라운드별 팀 순위 차등 보상(T2)은 구현·
  커밋·푸시 완료. 결선 팀당 카트 1대(T3)는 시도했으나 Round 2 레이스
  시각화가 표현할 수 없는 결과(등수 역전 결선 진출)를 만들어내
  **사용자 확인 후 폐기**했다 — 관련 코드 변경은 전부 되돌려져 있다.
  자세한 경위는 §1의 19번 항목과 작업계획서 "10. Phase T" 참고.
- **실사용 피드백 11건 반영 완료**(§1의 21~31번). 특히 **모바일에서
  베팅이 아예 불가능하던 버그**(폴링 재렌더가 선택을 초기화)와 **MC가
  레이스 종료 후에도 계속 중계하던 문제**가 컸다. 업그레이드 상점은
  제거했고, 장애물은 8종·스핀아웃/정지/감속으로 대폭 강화했다.
- **게임 구조 기준 문서 신설**: `타추위-추첨프로그램_게임구조.md`.
  진행 순서·라운드별 규칙·보상표·바꾸기 쉬운 설정값 위치가 정리돼 있어,
  앞으로 변경 지시는 이 문서를 기준으로 주고받는다.
- **테스트는 현재 289개 전부 통과**(갬블링 관련 테스트 30여 개가
  모듈째 사라지고 차등 채점/자동 참가 테스트가 새로 들어왔다).

---

## 1. 지금까지 완료된 것 (누적, 시간순)

### ~2026-08-04 세션까지
1. **무대 화면 전체화면 개편** -- F1 스타트 라이트, 실시간 포지션 타워,
   WebAudio 효과음, AI MC 음성 해설, FINAL LAP/PHOTO FINISH 배너, 시상대+
   컨페티+팡파레. (`static/stage.html/css/js`, `static/audio.js`,
   `static/fx.js`)
2. Director 하한 미달 시 조용히 멈추던 버그 수정(`/api/racing/start`·
   `/api/demo/start` 사전 검증 + 400 반환).
3. 배포 준비: `Dockerfile`, `.dockerignore`, `render.yaml` 최초 작성(단,
   `.dockerignore` 버그로 이 시점엔 실제로는 빌드 불가 상태였음 — §0 참고).
4. 공모전 전략 문서 작성.
5. **사이버머니 갬블링(승인됨)** -- 패리뮤추얼 정산. `app/gambling.py`,
   `/api/bet/*`, `Session.prediction_mode`.
6. **팀 특수능력 → 개인 캐릭터/카트 선택으로 확장**. `app/characters.py`,
   `/api/character/*`, 모바일 온보딩 3단계.
7. MC 음성 생동감 개선(`MC_ENERGY` 상황별 속도/피치 프로파일).

### 2026-08-05까지 추가된 것 (이번 갱신에서 반영)
8. **갬블링 경제 확장(커밋 `826038d`)** — 라운드 통과 시 +40, 우승 부서
   소속 통과자 +60, 결선 최종 당첨자 +300 지급(`FINISH_REWARD`/
   `TEAM_WIN_REWARD`/`FINAL_WIN_REWARD`, `award_round_rewards`/
   `award_final_rewards`). **순수 코스메틱 업그레이드 상점** 추가 —
   개인 카트 글로우 3단계(`purchase_personal_upgrade`), 부서원 공동
   투자로 올리는 팀 단계(`contribute_team_upgrade`, 임계값 500마다
   레벨업, 최대 3단계). 레이스 계산에는 어떤 값도 넘기지 않는 시각
   전용 기능. **잔액은 이 확장 이후에도 여전히 0 미만으로 절대
   내려가지 않고(`place_bet`이 초과 베팅 차단, 정산 패배 시 추가
   차감 없음), 오직 보상/베팅승리로만 플러스 방향으로 움직인다** —
   `test_balance_never_goes_negative_even_on_full_loss` 등으로 보증됨.
9. **레이스 트랙 곡선화(커밋 `af0ada9`)** — 트랙이 가로 일직선이라
   박진감이 없다는 피드백에 따라, 서버(`app/race.py`)의 0..1 진행률
   스칼라는 그대로 두고 클라이언트(`static/stage.js`)에서만 Catmull-Rom
   스플라인 + arc-length LUT로 S자 곡선 좌표로 매핑. 카메라가 리더의
   (x,y)를 따라 커브를 팬, 카트 스프라이트가 진행 방향으로 회전, 오일
   슬릭·트래픽 콘·타이어 장애물이 상시 2~3개 등장해 카트가 지나가면
   흔들리는 연출(순위/진행률 값은 절대 불변 — 시각 전용). 원격 브랜치가
   그 사이 "GRAND PRIX 전체화면 개편"으로 크게 앞서 있어 곱선 로직을
   그 위에 재이식하는 rebase 작업이 필요했음(완료, 322 테스트 통과 +
   Playwright 실제 구동 검증 완료).
10. **총 진행 시간 5분 → 10분(300s → 600s) 기본값 전환** — 사용자 요청.
    `app/director.py`(`DEFAULT_TOTAL_SECONDS`), `app/main.py`(세션 생성/
    데모 시작 기본값), `app/models.py`(Session 기본값), `static/admin.html`
    (입력창 기본값) 전부 갱신. 런북은 비율 기반이라 각 구간이 정확히
    2배로 늘어남(레이스/선택창/발표 전부 비례 확장).
11. **장면별 배경음악(BGM) 엔진** — `static/audio.js`에 외부 음원 파일
    없이 오실레이터로 합성하는 루프형 BGM 5종 추가(`idle`/`anticipation`/
    `race`/`victory`/`roulette`). `playScene(name)`으로 부드럽게 전환,
    `setSceneIntensity(0..1)`로 레이스 진행률+접전도에 따라 템포/밝기
    상승. `showOverlay()`/레이싱 phase 핸들러/`race_tick`/WS reset에
    훅을 걸어 화면 전환마다 자동으로 맞는 장면이 재생되도록 연결.
    Playwright로 전 구간(대기→오프닝→선택→레이스→결과발표→결선→
    시상대) 실제 구동해 `SFX.getSceneName()`으로 장면 전환을 검증함.
12. **배포 버그 수정** — §0 참고. `.dockerignore`/`requirements.txt`
    (pydantic 명시 핀 추가)/`THIRD_PARTY_NOTICES.md` 정정.
13. **문서 전면 점검·갱신** — README/전략노트/작업계획서/이 문서를 실제
    코드 상태와 대조해 정정, 참가자용 쉬운 설명 문서
    (`타추위-추첨프로그램_게임소개.md`) 신설.

### 2026-08-06 추가된 것

14. **전체 코드베이스 버그 감사(백그라운드 에이전트) + 실제 버그 4건 수정**:
    - **WS 재연결 시 죽은 소켓 참조**(가장 심각) — WiFi가 한 번이라도
      순단되면 admin의 카메라 전환 버튼이 그 이후로 영구히 조용히
      죽는 버그. `static/shared.js`의 `connectWS`가 항상 같은 핸들을
      반환하도록 재작성.
    - **모바일이 세션 리셋을 못 받고, 네트워크 오류로 참여가 영구
      잠기는 문제** — 관리자가 리셋해도 참가자 폰이 예전 화면에
      고정되고, 페이지 새로고침 중 네트워크가 잠깐 불안정하면 로컬
      토큰이 지워지면서 "이미 다른 기기에서 참여 중" 409로 영구
      잠기던 문제.
    - `bannerHideTimer`가 영원히 null로 안 돌아가는 문제(어제 고친
      시상대 버그와 같은 계열, 뿌리 원인까지 정리).
    - 캐릭터 선택이 R1에서만 재조회되던 문제(라운드 사이 선택창에서
      캐릭터를 바꿔도 반영이 늦던 연출 버그).
15. **라운드별 다른 트랙 + 응원 이모지**:
    - 트랙 웨이포인트를 라운드별 3세트(`TRACK_DEFS`)로 확장 — R1은
      완만하게(250대가 한눈에 들어와야 함), R2는 중간, R3(결선·
      클로즈업)는 가장 굴곡지게. 매번 같은 맵이 반복되지 않는다.
    - 모바일에 이모지 버튼(🔥👏🎉💪😱⚡❤️😂) 추가 → WS로 서버에 전송
      → 서버가 허용목록+연결당 쿨다운(0.4초)으로 검증 후 stage 역할
      에만 릴레이 → 화면 하단에서 떠오르며 페이드아웃(순수 연출,
      순위 무관).
16. **⚠️ 핵심 설계 변경: 예측/갬블링 리더보드가 실제 경품 당첨자를
    결정하도록 전환**(사용자 명시적 요청 — §0 참고). 구현 요지:
    - `app/models.py`: `DrawResult`에 `prize_winners`/`prize_basis`
      필드 추가(리빌 전에는 `winners`처럼 `None`).
    - `app/main.py`: `_compute_prize_winners(session, draw)` —
      `predictions_enabled=False`면 레이스 결과 그대로(무변경 폴백),
      켜져 있으면 라운드 3 최종 채점 직후 예측/갬블링
      `leaderboard(N)`의 participant_id를 당첨자로 채택. 새 WS 브로드
      캐스트 `{"type": "prize_winners", "winners": [...], "basis": ...}`
      를 `revealed`·라운드3 최종 채점 이후에 보낸다(막판까지 순위가
      바뀔 수 있다는 게 핵심이라, 레이스 리빌 순간과 당첨자 확정
      순간을 의도적으로 분리). 룰렛 모드(`reveal_draw`)는 애초에
      예측 라운드가 안 도니 레이스 결과를 그대로 채택.
    - `static/stage.js`: 시상대는 `latest.revealed`뿐 아니라
      `latest.prize_winners`가 채워질 때까지 기다렸다가 뜬다.
      시상대에 "○○ 리더보드 기준" 안내 문구 추가(레이스 순위와 다를
      수 있음을 명시).
    - `static/admin.js`: 커밋 표시 영역에 **"실제 경품 당첨자"**를
      레이스 결과보다 먼저·크게 표시(둘이 다를 때는 레이스 결과도
      "참고"로 별도 표시) — 진행자가 실물 상품을 엉뚱한 사람에게
      주지 않도록.
    - **레이스/`fairness.py`/`/verify`/기존 322개 테스트는 전혀
      건드리지 않았다** — 회귀 없음, 순수 추가.
    - 신규 단위/통합 테스트 6개(`tests/test_prize_winners.py`):
      레이스 1등이 실제 당첨에서 탈락하고 레이스 꼴찌 쪽이 당첨되는
      "반전" 시나리오까지 검증.
    - 문서 전체(README §3, 게임소개.md, verify.html, 이 문서)를 새
      설계에 맞춰 재작성.

**테스트**: 328개 전부 통과(322 기존 + prize_winners 6개, 회귀 없음).

**커밋 상태**: 8~13은 이미 커밋·푸시됨. 14~16(오늘 작업분)은 통합
Playwright 검증까지 마친 뒤 작업단위로 커밋·푸시 예정(§2 참고).

### 2026-08-06 저녁 추가 세션 (Phase T 착수)

17. **업그레이드 상점 프론트엔드 병합**: 로컬에 커밋 안 된 채 남아있던
    코스메틱 업그레이드 상점 프론트(모바일 패널 + 무대 글로우 연출)를,
    그 사이 origin에 쌓인 14개 커밋(응원 이모지·라운드별 트랙·WS 재연결
    버그 수정 등)과 충돌 없이 병합·커밋·푸시함(`static/mobile.js`,
    `static/stage.js`의 `drawKart()` 시그니처 통합).
18. **Phase T2(라운드별 팀 순위 차등 보상) 구현·커밋·푸시 완료** —
    `TEAM_WIN_REWARD`(이진, 1위만) → `TEAM_RANK_REWARDS = [100, 70, 40]`
    (3단계 계단식)로 확장. `app/gambling.py`의 `award_round_rewards`가
    순위 정렬된 부서 목록을 받도록 변경, `app/main.py`가 통과율
    내림차순으로 부서 순위를 구성해 전달. 레이스 계산에는 관여하지
    않음. 328개 테스트 전부 통과.
19. **Phase T3(결선 팀당 카트 1대) 시도 후 폐기**: `app/fairness.py` +
    `static/fairness.js`에 실제로 구현했으나(부서별 R1 생존자 중
    전체 순위 최상위 1명만 결선 진출), 착수 중 설계 문서에 없던 충돌을
    발견함 — `static/stage.js`의 Round 2 레이스가 "통과선 하나로 앞/뒤
    가르기"(연속 구간 절단) 방식인데 반해, "부서당 1명"은 전체 순위
    어디서든(예: 71등이 유일 생존자인 부서 대표) 나올 수 있는 불연속
    집합이라 단일 통과선으로 표현이 불가능함을 발견(79개 테스트가
    한꺼번에 실패하며 잡아냄). Round 2를 "카트별 팀대표 배지" 방식으로
    새로 그리는 방안까지 설계했으나, 상황을 설명하는 과정에서
    **사용자가 재고 후 철회** — "낮은 등수가 결선에 가고 높은 등수가
    탈락하는" 결과 자체가 화면을 고쳐도 현장에서 신뢰를 깎아먹을 수
    있다고 판단. **최종 결정: 결선 진출은 등수 그대로 유지, 관련 변경은
    전부 되돌림(스태시 폐기, 커밋 없음)**. 팀 대항전 요구는 T2만으로
    충분하다고 결론. 상세 경위는 작업계획서 "10. Phase T" §10.1(b) 참고.
20. **T1(업그레이드 코스메틱/실반영 방향)·T4(투자 선택 메뉴 승격)는
    보류**: T1은 사용자 확인 결과 "코스메틱 유지형"으로 확정됐지만,
    이어서 "그럼 T4가 무슨 실익이냐"는 질문 → 마감 임박을 감안해 T4는
    이번 범위에서 제외하기로 함께 결정. 업그레이드 상점은 기존 그대로
    (코드 변경 없음).

**테스트**: 328개 전부 통과(변동 없음 — T2는 순증, T3는 되돌림).

**커밋 상태**: 17·18은 각각 개별 커밋으로 이미 푸시됨. 19·20은 코드
변경이 없으므로(전부 되돌림/보류) 문서 갱신만 커밋.

### 2026-08-06 심야 — 실사용 피드백 11건 일괄 반영

실제로 데모를 돌려본 뒤 나온 피드백을 백엔드/프런트/운영콘솔 3개 커밋으로
나눠 반영했다.

21. **AI MC가 레이스 종료 후에도 계속 중계하던 문제**. 원인은 멘트가
    네트워크 왕복(`/api/mc/line/{tag}`) 뒤에 재생되는 구조라, 요청과
    재생 사이에 장면이 바뀌면 흘러간 중계가 그대로 나갔던 것. 장면 전환
    마다 세대(`mcEpoch`)를 올리고 이전 세대 응답은 자막·음성 모두 버리게
    했다. 추가로 진행률 90% 이후에는 실황 멘트를 잠그고(`liveMcSuppressed`),
    `racing_complete`에서 대기 중인 멘트를 전부 취소한다.
22. **좌우 패널이 레이스를 가리던 문제** — 패널 폭 15.5→12.5rem, 글자
    축소, 배경 불투명도 완화(뒤 트랙이 비침).
23. **사이버머니 리더보드가 MC 자막에 가려지던 문제** — MC 바가 화면
    전체 폭을 차지하고 z-index(25)도 패널(20)보다 높아 우측 하단 패널을
    통째로 덮고 있었다. MC 바를 좌우 패널 사이 가운데 폭으로 제한하고,
    패널 높이를 MC 바 위쪽까지로 제한했다.
24. **모바일에서 베팅이 아예 불가능하던 치명적 버그**(가장 심각).
    베팅 창이 열린 동안 2.5초마다 도는 배당률 폴링이 카드 DOM을 통째로
    다시 그려서, 팀/카트를 고르고 금액을 입력하는 중이던 선택이 매번
    초기화됐다. 아직 서버에 보내지 않은 입력을 `pendingBetTarget/Amount`에
    보관해 재렌더 때 복원하고, 입력 중(포커스가 카드 안)에는 재렌더 자체를
    건너뛰도록 했다.
25. **결선이 너무 느리던 문제** — `race_r3` 기준 시간 70→40(600초 기준
    140초→80초). 회수한 시간은 카트가 많아 볼거리가 실제로 많은 R1/R2에
    재배분(각 45→55).
26. **라운드별 맵이 다 똑같아 보이던 문제** — 예전 `TRACK_DEFS`는 굴곡
    진폭(±0.2~0.34)보다 트랙 폭(halfWidthFrac 0.48~0.55)이 2~3배 커서
    셋 다 "굵고 뭉툭한 띠"로 뭉개져 사실상 구분이 안 됐다. R1=넓은 고속
    스피드웨이 / R2=폭 절반의 지그재그 시케인 / R3=가장 좁은 테크니컬
    서킷으로 성격을 갈랐고, 차선 수를 트랙 폭에 맞춰 자동 조정해 좁은
    코스에서 카트가 점처럼 작아지지 않게 했다.
27. **카트 업그레이드 상점 전면 제거** — 순위에 영향을 줄 수 없는 순수
    코스메틱이라 게임 선택지로서 의미가 없다는 사용자 판단. 백엔드
    (`purchase_personal_upgrade`/`contribute_team_upgrade`/`team_upgrade_level`,
    `/api/bet/upgrade/*`, `/api/bet/upgrades`)와 프런트(모바일 패널, 무대
    글로우, 레전드 별 표시)를 모두 삭제.
28. **시상대에 당첨자 최종 사이버머니 표시** — `DrawResult.prize_scores`
    추가(확신도 모드면 점수). 시상대 상위 3명과 그 외 명단 모두에 표시.
29. **모바일에 라운드별 보상 내역 표시** — `BetCard.rewards`에 항목별
    (결승선 통과 / 팀 순위 보너스 / 결선 당첨)로 기록해, 참가자가 "이번
    라운드에 왜 얼마를 받았는지" 폰에서 바로 볼 수 있게 했다.
30. **운영 콘솔에서 룰렛 모드 제거** — 실제 행사는 항상 레이싱 모드로
    진행하므로 모드 선택자와 "추첨 시작(리빌)" 버튼을 없앴다. 백엔드는
    그대로 두어 기존 테스트/저장 세션 호환성을 유지한다.
31. **장애물 대폭 강화** — 종류 3→8종(콘/기름/타이어/바나나/웅덩이/바위/
    폭탄/얼음), 크기 랜덤(0.75~1.45배), 스폰 간격 2.2초→0.42초, 동시
    개수 라운드별 7~12개. 충돌 시 **스핀아웃/정지/감속/미끄러짐** 효과가
    걸린다. **⚠️ 핵심 안전장치**: 효과는 전부 "보이는 위치"에만 적용되고,
    각자 시간이 지나 사라지는 것과 별개로 레이스 막판 12% 구간
    (`EFFECT_FADE_TAIL`)에서 강도에 0으로 수렴하는 페이드가 곱해진다 →
    결승선 통과 시점에는 보이는 위치와 서버가 정한 실제 위치가 정확히
    일치하므로, 화면과 발표 결과가 어긋날 수 없다.

**신규 문서**: `타추위-추첨프로그램_게임구조.md` — 게임 진행 순서·라운드별
규칙·보상표·자주 바꾸는 설정값 위치를 한눈에 정리한 **지시용 기준 문서**
(사용자 요청). 앞으로 변경 지시는 이 문서를 기준으로 주고받는다.

**테스트**: 319개 전부 통과(업그레이드 상점 제거로 관련 테스트 15개가
사라지고 보상 내역 테스트 4개가 추가돼 순감). 실제 서버를 띄워 갬블링
데모를 끝까지 돌려 `prize_scores`(예: `[1458, 1138, 1081]`)와
`card.rewards`가 실제로 채워지는 것까지 확인했고, 그 회차에서 레이스
당첨자(`P0010`, `P0025`)와 실제 경품 당첨자(`P0014`, `P0022`)가 다르게
나오는 반전 케이스도 함께 관측됐다.

### 2026-08-07 — 사운드 전면 보강 (BGM 라운드별 분리 + 극적 효과음)

**문제**: BGM이 레이스 내내 한 곡(`race`)뿐이라 라운드가 올라가도 같은
노래를 세 번 듣는 셈이었고, 화면에서 가장 극적인 순간인 **장애물 충돌**과
**결승선 통과**에는 소리가 아예 없었다. 대기 구간도 선택창과 같은
`anticipation`을 써서 "곧 출발한다"는 긴장이 없었다.

1. **레이스 BGM 라운드별 분리** — `race1`(D장조, 경쾌한 오프닝) /
   `race2`(A단조, 싱코페이션 베이스 + 오프비트 스탭으로 추격전) /
   `race3`(E단조, 4분 킥 + 16분 아르페지오 + 옥타브 리드, 4마디마다
   상승 필인). 기존 `setSceneIntensity(진행률+접전도)`에 따른 실시간
   템포/음량 상승은 그대로 유지.
2. **출발 대기 BGM `standby` 신설** — 심장박동 같은 2연타 킥 + 초침
   하이햇 + 마디마다 조여드는 저현. 오프닝/`r1_lock`/커밋 완료 대기
   화면에 연결.
3. **나머지 장면 확장** — `idle`/`anticipation`/`victory`/`roulette`을
   4마디 화음 진행으로 바꿔 오래 틀어놔도 한 화음만 웅웅대지 않게 하고,
   장면별 기본 음량을 분리(대기는 낮게, 결선/시상대는 높게). BGM에
   킥/스네어/하이햇 합성 파트 추가 — 화음만으로는 주행감이 안 났다.
   → 장면 총 8종: `idle`/`standby`/`anticipation`/`race1`/`race2`/
   `race3`/`victory`/`roulette`.
4. **장애물 충돌음 6종** — `wobble`(콘, 가벼운 플라스틱) / `slow`(기름·
   타이어, 둔탁한 고무 충격) / `slide`(웅덩이·얼음, 스키드) / `spin`
   (바나나, 코믹한 하강) / `stall`(바위, 묵직한 정면 충돌) / `explode`
   (폭탄, 저역 붐 + 파편). 선두가 바위/폭탄에 맞으면 화면 흔들림 추가.
5. **결승(통과)선 통과음** — 선두는 체커기 + 함성 + 링 이펙트, 뒤따르는
   카트는 짧은 블립. 그 외 FINAL LAP 종 + 함성, PHOTO FINISH·최종 발표
   직전 상승 스팅어, 라운드 통과 발표 함성.
6. **동시다발 이벤트 총량 제어** — 250대가 뛰는 R1에서는 한 프레임에
   수십 대가 동시에 충돌·통과할 수 있다. (a) `audio.js`의 종류별/전체
   게이트로 솎아내고, (b) 충돌음 볼륨을 순위로 스케일(선두 1.15 →
   하위권 0.28), (c) 통과음은 카트당 1회·라운드당 최대 24회로 제한,
   (d) 마스터 최종단에 리미터를 물려 피크만 눌러 클리핑을 막는다.
   라운드 전환 시 이미 통과선을 넘어 있던 카트는 소리 없이 기록만
   채워, 중간 접속/복구 직후 통과음이 쏟아지지 않게 했다.
7. **충돌 연출 재발동 버그 수정** — `applyKartEffect()`가 "새로 효과가
   걸렸을 때"만 `true`를 반환하도록 바꿔, 카트가 장애물에 붙어 있는
   동안 매 프레임 충돌음이 나는 것을 막았다.

**불변 원칙 유지**: 추가한 소리는 전부 연출 전용이며 순위/진행률 값에는
전혀 관여하지 않는다. 파이썬 테스트 319개 전부 통과, 가짜 WebAudio로
전 장면·전 효과음을 한 번씩 스케줄해보는 스모크 테스트도 통과.

### 2026-08-07 — 예측 게임 전면 개편 (베팅 제거 · 순위 차등 · 전원 참가)

**계기**: 사용자가 실제로 갬블링 모드를 돌려보고 "보상도 적고 어렵다"고
보고했다. 코드로 확인한 원인 네 가지:
(a) 패리뮤추얼은 걷힌 판돈만 재분배하므로 **전체 잔액 총량이 안 늘고**,
(b) 정답 후보가 넓어(R1은 8개 부서 중 2개) 승자가 많으면 **배당이
1.5~3배로 눌리고**, (c) 20초 안에 "대상 + 금액" **두 번을 결정**해야 하며,
(d) `lock_round`가 자동 베팅을 하지 않아 **안 걸면 라운드가 통째로 무효**가
된다. 그 결과 "소수가 크게 딴다"도 "다수가 조금씩 받는다"도 아니었다.

32. **갬블링/베팅 모드 완전 제거** (커밋 `cde7cf5`). `app/gambling.py`와
    테스트 3종 삭제, `Session.prediction_mode` 제거, `/api/bet/*` 제거,
    `prize_basis`를 `"confidence"|"gambling"` → `"prediction"`으로 통합.
    MC 태그 `gambling_*` → `prediction_result`/`prediction_champion`.
    무대 우측 "실시간 배당률" 패널은 **"실시간 선택 분포"**(막대 그래프)로
    바뀌었다 — 예전에는 갬블링 모드에서만 뜨던 패널이 이제 항상 뜬다.
    운영 콘솔에서 예측 게임 종류 선택자와 갬블링 승인 경고도 없앴다.

33. **순위 차등 채점 도입** (커밋 `e4baf3f`). 기존에는 정답 집합(R1 통과율
    상위 2개 / R2 1위 부서)에 들었는지만 봐서 **대다수가 0점**이었고,
    첫 라운드에 빗나간 사람이 폰을 내려놓는 구조였다. 이제
    `RANK_RATIOS = [1.0, 0.6, 0.35, 0.2, 0.1]` + `FLOOR_RATIO = 0.05`로
    **몇 등을 골랐는지에 비례해 계단식 지급**한다. R3도 결승 등수 기준
    같은 곡선을 쓴다. 1위 적중과 꼴찌 사이 20배 격차를 남겨 변별력은
    유지했다. 소수파 보너스는 **1위 정확 적중에만** 적용한다 — 차등
    구간까지 주면 "틀렸는데 아무도 안 골라서 더 받는" 모순이 생긴다.
    갬블링에 있던 성과 보상(통과/팀 순위/결선 당첨)은 예측 점수 단위로
    옮겨왔다(+8 / +20·14·8 / +60).

34. **전원 자동 참가 + 라운드별 기본 선택** (커밋 `e4baf3f`, 사용자 요청).
    `enroll_all()`이 커밋 시점에 **명단 전원**의 카드를 만든다. 미선택
    시 자동 배정 규칙이 라운드마다 다르다: **R1·R2는 자기 소속 부서**
    (대상이 부서라 가장 자연스럽고, "자기 팀 응원"이라는 서사와도 맞는다),
    **R3는 시드 파생 무작위**(결선 진출자 개인이 대상이라 "자기 팀"이
    없다). 효과는 **모바일 미참여자도 경품 대상에 남는다**는 것 —
    예전에는 join한 사람만 카드가 생겨 리더보드에 아예 없었다.
    참여 유인은 유지된다: 부서를 직접 고를 수 있고, 확신도를 몰아쓸 수
    있고, R3는 안 고르면 무작위다.
    ⚠️ 부작용: `_compute_prize_winners`의 "참여자가 적으면 당첨자 수가
    줄어든다"는 옛 동작은 실질적으로 사라졌다(전원에게 카드가 있으므로).

35. **R3 몰아주기(올인)** (커밋 `e4baf3f`, 사용자 요청). 확신도 하한을
    `MIN_ALLOC_ROUNDS = (1, 2)`에만 적용해 R3는 0~80으로 열고,
    `ROUND_WEIGHTS[3]`을 2.5 → **3.0**으로 올렸다. 앞 두 라운드를 모두
    1위로 맞힌 사람(45/45/10 → 113점)도 R3 올인 적중(10/10/80 → 240점)
    한 방에 뒤집힌다. 이 성질은 테스트로 고정했다
    (`test_round3_all_in_beats_two_early_round_wins`).

**테스트**: 289개 전부 통과. 갬블링 테스트 3파일이 통째로 빠지고,
`test_predictions.py`를 새 규칙 기준으로 전면 재작성했으며(순위 단조
감소·참여 보상 하한·소수파 보너스 적용 범위·올인 역전·성과 점수 항목별
내역), 폰을 아무도 안 들었을 때 전원이 점수를 쌓고 경품 대상이 되는지를
끝까지 돌려보는 통합 테스트를 추가했다
(`test_non_mobile_participants_still_score_and_stay_prize_eligible`).

### 2026-08-07 — 운영 콘솔 "샘플 250명 생성" 등 버튼 무응답 점검

**계기**: 사용자가 운영 콘솔에서 "샘플 250명 생성" 등 버튼이 안 눌린다고
보고. 로컬 서버 + 헤드리스 Chrome(puppeteer-core, 시스템 Chrome 직접
구동)으로 재현을 시도했으나 **정상 동작**했다 — 즉 코드 자체의 논리
오류는 아니었고, 실제 원인은 두 가지 잠재 취약점으로 좁혀졌다.

36. **`btn-sample` 핸들러에 try/catch 누락** (`static/admin.js`). 다른
    모든 버튼 핸들러는 실패 시 `alert(e.message)`로 사용자에게 알리는데
    이 핸들러만 예외 처리가 없어, `/api/roster/sample` 요청이 실패하면
    (네트워크 순단, Render 무료 플랜 콜드스타트 지연 등) 콘솔 로그에만
    에러가 남고 화면에는 **아무 반응도 없어 보였다** — 사용자가 "버튼이
    안 눌린다"고 느끼는 것과 정확히 일치하는 증상. 다른 핸들러와 동일한
    패턴으로 통일.
37. **정적 자원에 Cache-Control 미설정** (`app/main.py`). `StaticFiles`/
    `FileResponse`가 `Cache-Control`을 안 붙여서 브라우저가 자체
    휴리스틱으로 `admin.js`/HTML을 오래 캐싱할 수 있었다 — 배포 후에도
    브라우저가 강력 새로고침 없이 옛 버전을 계속 쓰면, HTML은 최신인데
    JS만 예전 버전이라 DOM 참조가 어긋나며 버튼이 조용히 먹통이 되는
    사고로 이어질 수 있다. `/static/*`·`/`·`/admin`·`/verify`·`/mobile`
    응답에 `Cache-Control: no-cache`를 미들웨어로 강제해 매 요청마다
    ETag로 재검증하도록 함(내용이 안 바뀌면 여전히 304로 빠르게 응답).

**검증**: 헤드리스 Chrome으로 샘플 생성 → MC 사전생성 → 세션 생성 →
커밋 → 초기화까지 전체 플로우를 재실행해 모두 정상 동작 확인. 파이썬
테스트 289개 전부 통과(로직 변경 없음, 회귀 없음).

### 2026-08-07 — 레이스 박진감·속도감 강화 + 모바일 현황판 + 스코어링 단순화

사용자 피드백 연쇄("레이싱에 박진감과 속도감이 많이 부족하다" →
"카트끼리 추월하며 선두가 계속 바뀌게" → "결승선을 트랙 끝쪽에" →
모바일 실시간 현황 요청 → 확신도 배분 대신 라운드당 1픽 제안)에 따라
연속으로 반영. 전부 연출/UI 레이어 변경이라 `app/fairness.py`·
`app/race.py`(레이스 결과 계산)는 무변경.

38. **장애물 대폭 증가 + 지속 + 감속 체감 강화** (`static/stage.js`).
    라운드별 동시 활성 상한 7/10/12 -> 35/45/50, 스폰 간격
    420~1120ms -> 120~340ms. 지속 기준을 "리더 기준 0.08" -> "시야
    꼬리 기준 0.2"로 바꿔 훨씬 오래 남는다. 충돌 lag를 0.005~0.026 ->
    0.03~0.09로 올려 부딪히면 눈에 띄게 뒤처졌다가 1~2초에 걸쳐
    따라잡는다.
39. **R1도 줌-추적 카메라로 전환 + 맵 확대** (`static/stage.js`). 예전엔
    R1(250대)만 카메라가 스케일 1로 고정돼 전혀 안 움직였다. 사용자가
    "뒤처진 카트는 안 보여도 된다"고 명시적으로 허용해, R1도 리더를
    따라 확대·이동하는 카메라로 바꿨다(광각은 수동 선택지로 남김).
    트랙이 쓰는 화면 대역을 넓혀(0.14~0.86H -> 0.05~0.95H) 실제 이동
    거리(호 길이)를 늘렸다 -- Catmull-Rom은 아핀 변환 불변이라 자기교차
    위험 없이 안전하게 확대된다. 화면 밖 후미 카트는 그리지도 충돌
    판정도 안 해서(컬링) 장애물을 늘려도 프레임이 안 무거워진다.
40. **카트끼리 계속 추월하며 선두가 바뀌는 잔물결** (`static/stage.js:
    jockeyOffsetFor`). 카트마다 다른 주기로 흔드는 사인파를 "보이는
    위치"에만 더한다 -- 실제 순위(POSITION 패널·통과 판정)는 전혀 안
    바뀌고 결승선 앞 12%에서 0으로 수렴. 개발 중 실제로 버그 하나를
    발견: 장애물 lag + 잔물결이 동시에 걸리면 R3처럼 트랙이 굴곡지고
    카메라가 많이 확대된 상태에서 카트가(리더까지) 화면 밖으로 사라져
    보일 수 있었다(헤드리스 Chrome으로 재현). 카메라가 리더의 "실제"
    위치가 아니라 리더 본인의 효과까지 반영한 "보이는" 위치를 따라가게
    바꿔 리더는 구조적으로 프레임 밖에 날 수 없게 고쳤다.
41. **결승선을 트랙 끝쪽에 고정 + 결승선 이후 장애물 제거**
    (`static/stage.js: warpProgress, FINISH_VISUAL_FRACTION=0.92`).
    원본 통과선(pass_line) 값이 라운드·인원수마다 제각각이라 결승선이
    트랙 중간쯤 그려지던 것을, 항상 진행률 92% 지점에 오도록 구간별
    선형 워프. 카트가 실제로 통과선을 넘는 시점과 화면에서 체커기
    줄을 넘는 시점은 항상 정확히 일치(통과 판정 자체는 무변경, "어디에
    그릴지"만 재배치). 장애물 스폰 자체도 pass_line 이전으로 상한을
    걸어 결승선 너머엔 애초에 생기지 않는다.
42. **모바일 "내 카트 현황" 신설** (`app/main.py: latest_race_tick,
    _my_race_status, _my_department_rank`, `app/predictions.py:
    PredictionEngine.rank_of`, `static/mobile.js`). 레이스 진행 중 폰에
    실시간 등수·통과 여부(진행 중이면 통과선까지 몇 % 왔는지)·소속 팀
    실시간 순위·포인트 순위를 표시. "모바일 250대에 위치 데이터를
    안 뿌린다"는 기존 원칙(§0 참고)은 그대로 지키면서, 최신 틱 하나만
    캐시해 폴링 시점에 그 사람 한 명분만 계산해 돌려주는 방식으로
    풀었다. 기존 폴링 패턴(선택 창 열려 있는 동안 refreshMe)을 레이스
    진행 중(phase가 "race_"로 시작할 때)에도 켜지도록 확장.
43. **예측 게임 스코어링 단순화 — 확신도 배분 완전 제거**
    (`app/predictions.py`, `app/main.py`, `static/mobile.js`). §0의
    설명대로 확신도 100점 배분(및 `MIN_ALLOC`/`TOTAL_ALLOC`/
    `set_allocation`/`/api/predict/allocate`/모바일 확신도 입력 UI)을
    전부 제거하고, 라운드마다 고정 배점(`ROUND_BASE_POINTS=100`)을
    거는 방식으로 교체. `ROUND_WEIGHTS`(1.0/1.5/3.0)만으로 "결선이
    막판 역전을 만든다"는 드라마가 그대로 유지됨을 신규 테스트
    (`test_round3_weight_lets_a_late_comeback_beat_an_early_leader`)로
    고정. 모바일은 대상 버튼을 누르는 즉시 확정되고 별도 저장 단계가
    없어졌다.

**테스트**: 282개 전부 통과(alloc 검증 테스트 9개 제거, 나머지는 새
채점식 기준으로 재작성 + rank_of/모바일 현황 통합 테스트 2개 추가).
헤드리스 Chrome으로 250명/40명/20명 세션을 여러 차례 실제로 돌려
R1~R3 확인 -- 결승선 왼쪽에만 장애물이 있고 오른쪽은 깨끗함, 실제
선두 교체(POSITION 패널 기준) 확인, 모바일 "내 카트 현황" 카드와
단순화된 예측 카드(확신도 UI 없음, 클릭 한 번으로 확정) 스크린샷
확인, `scripts/load_test.py`도 allocate 스테이지 제거 후 재검증(30명
성공 30/30).

### 2026-08-07 — 레이스 연출 전면 개편 + 포인트 상향 + MC/BGM 강화

사용자가 한 번에 지시한 11건 중 8건을 처리했다. 남은 3건과 추가 논의
2건은 **작업계획서 §12**에 결정 근거까지 정리해 뒀다.

44. **포인트 체계 상향(①)** — "예측으로 얻는 포인트가 너무 적다".
    `ROUND_BASE_POINTS` 100 → **300**(1위 적중이 R1 300 / R2 450 /
    R3 900점). 성과 점수도 같은 비율로 올렸다(통과 8→20, 팀 1~3위
    20/14/8→45/30/18, 결선 당첨 60→150). 게임구조.md §5-1에 **라운드별
    실제 지급 점수표**를, §5-4에 "한 사람이 한 라운드에 받는 점수" 합산
    예시(최상 1,995 / 평범 192 / 미참여 82점, 약 24배 격차)를 신설.

45. **맵을 진짜 서킷으로 재설계(⑪) + 결승선(②) + 장애물 분포(④) +
    카메라 줌(⑥) + 스타팅 그리드(⑦)** — 한 커밋으로 묶어 처리.
    - 트랙이 "x가 단조 증가하는 리본 + 위아래 물결"이라 아무리 진폭을
      키워도 구불구불한 띠로만 보였다. 이제 웨이포인트를 (x,y) 2차원
      으로 찍는 **진짜 서킷**이고, 화면의 1.25~2.0배인 **월드** 위에
      그려져 카메라가 따라간다. 노면 밖 잔디/런오프도 추가.
    - 자기교차 방지 규칙 2가지(구간 간격 > 트랙 폭, 헤어핀 반경 >
      halfWidth)를 주석에 명시했다. 어기면 리본이 뒤집힌다.
    - 카트 크기를 "차선 폭의 82%"가 아니라 **트랙 폭 기준**으로 먼저
      정하도록 뒤집었다(예전엔 차선을 36개까지 쪼개면 카트가 9px 점이
      되어 장애물보다 작았다).
    - 위험/안전 구역 표시가 "웨이포인트 x 단조 증가" 전제에 의존한
      화면 가로 그라데이션이라, 되돌아오는 서킷에서 경계가 엉뚱한
      가운데에 생겼다 → 호 길이 기준 리본 칠하기로 교체.
    - 장애물이 트랙 밖으로 나가던 문제: laneCenter 난수 + weave 진폭이
      합쳐 트랙 폭을 넘었다 → 자기 반지름만큼 여유를 두고 클램프.

46. **추가 피드백 3건 즉시 반영** — 같은 날 사용자가 실제로 돌려보고
    보고한 것들.
    - **속도 정규화**: 체감 속도 = (호 길이 × 줌) / 라운드 시간인데,
      R2·R3에 구간을 3~4개 넣는 바람에 기본 설정(600초)에서
      62/115/234px/s로 벌어졌다(R3가 R1의 4배). 세 코스를 모두
      2구간+헤어핀으로 통일하고 월드를 줄여 **62/60/80px/s**로 맞췄다.
      라운드별 개성은 길이가 아니라 "단위 거리당 커브 수"로 낸다.
    - **카메라 흔들림**: 카메라가 리더의 *연출 보정된* 위치(장애물 lag
      포함)를 따라가 충돌마다 화면이 덜컥거렸다 → 실제 위치를 따라가고
      지수 평활(camState)을 걸었다.
    - **결승선 = 통과 기준**: `FINISH_VISUAL_FRACTION` 0.92 → **0.965**.
      선 뒤로 트랙이 8% 더 이어져 "맵 끝이 목표"처럼 보이던 문제를
      없애고, 결승선 자체도 넓은 체커기 밴드 + 금색 판정선 + 게이트
      기둥으로 크게 그렸다.
    - **스타트 라이트 중 이미 출발하던 버그**: `_run_race_phase`에
      리드인(`RACE_COUNTDOWN_SECONDS=2.8초`)을 넣어 그 동안 ratio를 0으로
      고정. 구간 전체 길이는 그대로라 런북에 영향 없고, 짧은 구간에서는
      duration의 25%로 제한한다. `race_tick`에 `countdown` 플래그 추가.
    - **결승선 앞 장애물 무더기**: 스폰 위치를 `min(..., finishLimit, ...)`
      로 **잘라내고** 있어서, 리더가 결승선 근처에 오면 이후 스폰이 전부
      한 지점으로 clamp돼 수십 개가 겹쳤다 → 남은 구간이 있을 때만 그
      안에서 무작위로 고르고, 없으면 스폰하지 않도록 수정.

47. **AI MC 남성 캐스터 톤(③) + BGM 박진감 강화(⑧)**
    - MC: Web Speech API가 성별을 표준 노출하지 않으므로 한국어 남성
      음성 이름 힌트 목록으로 우선 선택하고, 폴백 환경에서도 중저음이
      되도록 피치 −0.28 / 속도 +0.06 전역 보정. 피치 하한도 0.75→0.55로
      열어야 보정이 실제로 먹는다.
    - BGM: 단일 오실레이터만 쓰던 게 "얇은 소리"의 원인이었다.
      `sceneSuperSaw()`(디튠 톱니 3개 + 필터 엔벨로프)와
      `scenePunchKick()`(서브 + 어택 클릭)을 새로 만들어 세 레이스
      트랙을 다시 썼다. 템포 126/136/146 → 150/160/172, 킥은 4분
      four-on-the-floor, 하이햇·베이스는 8분 → 16분.

**테스트**: 282개 전부 통과(연출 레이어 변경이라 백엔드 로직 무변경,
리드인만 `_run_race_phase` 수정). 헤드리스 Chrome으로 세 라운드 트랙
레이아웃 전체 조망, 250명 실제 레이스(스타팅 그리드 → 전체 조망 → 줌인
→ 결승선 통과), BGM 7개 장면 재생을 각각 스크린샷·콘솔 에러로 검증.
리드인은 별도 스크립트로 "카운트다운 틱 전부 ratio 0, 마지막 틱 정확히
1.0"을 직접 확인했다.

### 2026-08-07 (심야) — 작업계획서 §12 인수인계 항목 소진 + 추가 3건

인수인계로 남겨둔 §12-1/12-2/12-3을 모두 구현했다. 각 항목에 미리
적어둔 "결정"을 그대로 따랐고, 백엔드 스키마 변경은 §12-3의 인자 추가
(`score_round(character_by_pid=...)`)와 `round_revealed` 필드 추가뿐이다.

1. **§12-1 모바일 점수 내역** — 라운드 카드마다 소계를 항상 표기(미채점은
   회색 `-`로 둬서 "0점이라서"인지 "아직 안 끝나서"인지 구분되게 함) +
   예측/통과/팀/결선 4항목 x 3라운드 표와 항목별 총계. 합계가 서버
   `card.score`와 어긋나면 서버 값을 신뢰하고 그 사실을 한 줄로 알린다.
2. **§12-2 무대 라운드 전환기 패널** — `round_revealed`에
   `survivors_by_department`(팀별 생존 카트 수)와 R2 종료 시 `finalists`
   (등수 순 결선 진출자)를 추가하고, 선택 창 동안 화면 중앙에 패널을
   띄운다. R1 선택 구간에는 아직 탈락자가 없으므로 **팀별 참가 카트 수**를
   보여준다(사용자 추가 요청).
3. **§12-3 카트 능력** — `app/characters.py`에 `AbilityEffect` 8종을 정의해
   **예측 점수 규칙만** 비튼다. 레이스 순위는 그대로다(능력은 사람이 고르는
   값이라 순위에 영향을 주면 공정성이 깨진다). 미선택자는 중립 1.0배.
   배수 폭은 주력 ±30% 이내로 제한했다.

**같은 세션에서 사용자가 추가로 요청한 3건**:

4. **폰에도 후보 판단 지표** — `/api/predict/me`에 `candidate_stats` 추가.
   R1은 팀별 참가 카트 수, R2는 생존 수 + 직전 통과율/순위, R3는 결선
   진출자의 직전 등수와 소속 팀. 무대에만 있던 정보를 폰에서도 볼 수 있게
   해 정보 격차를 없앴다. 라운드별 "무엇을 보고 골라야 하는지" 힌트도 추가.
5. **레이스 시작 전 게임 소개/참여 방법** — 무대 대기·커밋 화면에
   "이런 게임입니다"(4줄) + "참여 방법"(3단계) + QR을 좌우 배치. 모바일은
   온보딩 위에 접이식 소개 박스를 두고 참여를 마치면 자동으로 접는다.
6. **스타트 카운트다운을 실제 F1처럼** — 5열 x 2단 갠트리, 즉각 점등,
   5등 점등 후 **불규칙한 정적(0.7~1.25초 랜덤)**, 일제 소등. 점등음은
   등마다 반음씩 올라가고, 소등음은 출발 혼 + 타이어 스크리치 + 일제 발진
   굉음 + 함성을 겹쳤다.
   > ⚠️ **연출 길이와 `app/main.py`의 `RACE_COUNTDOWN_SECONDS`(2.8 -> 5.2초)는
   > 반드시 함께 움직여야 한다.** 연출이 더 길어지면 라이트가 켜져 있는
   > 동안 카트가 이미 달려나가는 것이 보인다(과거 실제 버그).
   > `static/stage.js`의 `LIGHT_*` 상수 위 주석에 예산 계산이 있다.

**테스트**: 291개 통과(282 -> +9). 신규 테스트는 능력 배수 7종(고른 사람만
적용 / 해당 라운드에만 / shield가 참여 보상에만 발동 / 성과 점수 항목별 /
모르는 id는 중립 / 재채점 멱등 / 로스터-효과 동기화)과 `round_revealed`
전환기 데이터, 라운드별 `candidate_stats` 형태 검증이다. 실제 서버를 띄워
무대/모바일 페이지와 `/api/character/roster`, 데모 세션의
`/api/predict/me` 응답까지 확인했다.

**남은 §12 항목**: 12-4(장애물이 실제 순위에 영향 + `/verify` 제거)는
사용자가 명시적으로 뒤로 미룬 항목이고, 되돌리기 어려운 삭제와 공정성
논증 변경이 포함되어 **착수 전 확인이 필요**하다. 12-5(실제 음원 파일)는
"합성 BGM 유지"로 결론이 나 구현 대상이 아니다.

---

### 2026-08-08 — 실제 플레이 피드백 3건 (§12-6 라운드 속도/시간 + §12-4 장애물 실제 반영)

사용자가 실제로 데모를 플레이해보고 세 가지를 요청했다: ① R1 카트
속도가 너무 빠르다/라운드 시간을 줄여달라, ② 장애물에 맞아도 카트
위치가 실제로 안 바뀐다, ③ 무대·모바일의 게임 설명이 처음 하는
사람에게 불충분하다. ①②는 이 세션에서 구현까지 끝냈고, ③(§12-7)은
작업계획서에 다음 항목으로만 추가해뒀다(미착수).

**§12-6(완료)** — `app/race.py: pace_exponent` 하한을 0.55→0.8로 올려
R1 초반 급가속(진행률 5%만 지나도 목표의 21%까지 도달하던 문제)을
없애고, `app/director.py`의 race_r1/r2/r3 기준 시간을 55/55/40→
42/42/30초로 줄였다(기본 600초 기준 라운드 실제 길이가 13~15% 단축).
줄어든 라운드 길이만큼 `static/stage.js: CHASE_SCALE_BY_ROUND`(카메라
줌)도 비례해 낮춰, 라운드가 짧아진 만큼 체감 속도가 오히려 빨라지는
부작용을 막았다. 자세한 내용은 작업계획서 §12-6.

**§12-4(완료)** — 장애물을 시드 결정론 기반의 실제 게임 요소로 바꿨다.
- **범위 확정 과정이 중요하다**: 착수 전 사용자에게 "시각 효과만 강화"
  vs "실제 순위에도 영향(12-4 전체 실행)" 두 선택지를 물었고, 사용자가
  후자를 명확히 선택했다. 이전 세션(2026-08-07)에서 §12-4를 보류하며
  남겨둔 "공정성 논증이 '커밋-리빌+사후검증'에서 '시드 기반 완전
  결정론'으로 바뀐다"는 유의점을 그대로 실행했다.
- **설계가 초안과 달라진 지점**: 작업계획서 §12-4 초안은 "트랙 진행률
  도달 여부"까지 판정에 넣는 모델(`drag_at`)을 제안했는데, 구현 중에
  이러면 순위가 높은(target_position이 큰) 카트만 장애물 구간에 도달해
  더 많이 맞고 순위 낮은 카트는 덜 맞는 구조적 편향이 생긴다는 걸
  발견했다. 사용자의 "중간 순위뿐 아니라 전체 카트에 모두 적용"이라는
  기존 요청과 맞지 않아서, **레인 일치만으로 맞는지 판정**(도달 여부
  무관)하는 더 단순한 모델로 바꿨다 — 선두든 꼴찌든 완전히 동일한
  확률로 장애물에 노출된다.
- `app/race.py`에 `obstacle_layout`/`lane_for`/`total_obstacle_penalty`
  (시드 결정론 배치·레인·페널티)를 추가하고, `position_at(..., seed=)`가
  장애물에 맞는 순간부터 실제로 위치를 내렸다가 결승선에서 정확히
  목표 위치로 회복하도록 만들었다(연출 오프셋이 아니라 서버 권위 값).
- `app/fairness.py: _obstacle_adjusted_ranking`이 기본 HMAC 순위에
  장애물 페널티를 더해 최종 순위를 만든다 — 기존 "순위 리스트를
  자르기만 하는" 구조를 그대로 재사용해 리플이 작았다.
- `/api/verify/{draw_index}`·`/verify` 페이지, `static/verify.html`·
  `verify.js`·`fairness.js`, `tests/test_js_fairness_parity.py`,
  무대·운영 콘솔의 커밋 해시 노출을 전부 제거했다(사용자 요청: "verify
  같은 기능은 필요 없다. 커밋 해시도 보여줄 필요 없다"). `fairness.py`의
  `recompute_from_reveal`/`verify_draw`는 내부 정합성 점검용으로 남겨둠.
- `static/stage.js`의 장애물 렌더링을 클라이언트 랜덤 스폰+자체 충돌
  판정에서, 서버가 매 틱 보내는 `obstacles`/`effects`를 그대로 그리는
  방식으로 전면 교체했다(코드가 오히려 더 단순해짐).
- **README·게임구조·게임소개 문서를 전부 새 공정성 서술로 다시
  썼다.** 특히 게임소개.md는 "장애물은 연출용, 순위에 영향 없음"이라고
  써뒀던 걸 "실제로 순위가 밀린다"로 정정했다 — 참가자에게 보여주는
  문서라 틀린 채로 둘 수 없었다. 기획안.md은 역사 기록이라 원문은
  안 건드리고 상단에 변경 사실만 짧게 추기했다.
- **테스트**: `tests/test_race.py`에 장애물 결정론·상한·"결승선에서
  항상 목표와 정확히 일치"·"20개 시드 중 최소 한 번은 실제로 순위가
  바뀜" 등 12개 테스트 추가. 기존 `test_fairness.py`의 중첩 생존 집합/
  결정론 테스트는 수정 없이 그대로 통과했다(설계상 리플이 격리됨).
  pytest 291개 통과.
- **실서버 검증**: Playwright(헤드리스 Chromium)를 이 세션에서 처음
  설치해(`npm install playwright` + `npx playwright install chromium`)
  실제 FastAPI 서버를 띄우고 커밋 → 스타트 라이트 → R1~R3 레이스 →
  시상 축하 구간까지 전체 플로우를 실행, 장애물 스프라이트·피격 마크·
  MC 멘트가 정상 렌더링되고 콘솔 에러가 0건임을 확인했다. (주의:
  서버를 재시작할 때 git-bash의 `pkill`이 Windows 네이티브 프로세스를
  못 잡아 포트가 안 풀리는 경우가 있었다 — `Get-NetTCPConnection`/
  `Stop-Process`(PowerShell)로 잡아야 확실하다.)

**커밋**: §12-6/§12-4 백엔드·프론트엔드·문서를 논리 단위로 나눠 4개
커밋으로 나눠 푸시했다(속도 완화, 장애물 백엔드, 장애물 프론트엔드,
문서 — 마지막 문서 커밋은 다음에 이어짐).

**남은 §12 항목**: 12-7(게임 설명 보강)만 남았다(미착수). 12-4가
끝나서 더 이상 착수를 막는 의존성이 없다.

---

### 2026-08-08 (계속) — 결승선 컷오프(§12-8) + MC 버그 수정(§12-9)

같은 세션에서 사용자가 이어서 요청한 항목들:

48. **§12-8 결승선 컷오프**. "1 라운드의 경우, 결승선을 통과한 차가
    발생할 경우, 그로부터 5초의 카운트다운을 세서 그 시간 제한 내
    들어온 카트들만 다음 라운드로 진출하는 것으로 해 줘"로 시작해,
    착수 전 확인 질문("순위 후보군 안에서 필터링" vs "순위 없이 시간만")
    에 사용자가 **"기존대로 1라운드는 전체 화면에 100개 카트 중 몇
    개가 들어왔는지 실시간으로 N/100 식으로 보여 달라. 첫 차 기준
    카운트다운은 약 10초로. 즉 2개 기준(순위 개념 + 시간 제한 개념)"**
    으로 확정. 이어서 "2라운드의 경우엔 5초의 시간 제한을 추가해 줘"로
    R2까지 확장 요청.
    - `app/race.py: crossing_ratio`(카트가 결승선에 처음 도달하는
      시점을 선형 스캔으로 계산, 장애물 dip 때문에 이분탐색은 위험해서
      피함) + `obstacle_layout`에 `lru_cache`(반복 호출 성능 확보).
    - `app/fairness.py: _apply_cutoff_window`(순위 후보군 중 "첫 통과자
      + 창" 밖으로 밀린 카트를 걸러내되, **정원 미달을 막는 최소 인원
      안전장치**를 반드시 포함 — 컷오프가 아무리 타이트해도 R2/최종
      당첨자 정원은 순위 순으로 채워서 항상 보장한다). 통과선 위치는
      컷오프 "전" 순위 기준 후보군 크기(`round_candidate_count`, 신규
      `DrawResult` 필드)로 계산해, 시간에 밀려 탈락한 카트도 화면에서는
      결승선까지 도달하는 모습이 보이게 했다(안 그러면 "1등 통과 후
      카운트다운"이라는 연출의 전제가 깨진다).
    - `app/main.py`: 커밋 시점에 런북에서 race_r1/r2의 실제 레이스
      시간을 미리 구해(`_race_cutoff_seconds_for_commit`) fairness에
      넘긴다. race_tick에 candidate_count/cutoff_window_seconds를
      실어 보내 클라이언트가 "1등 통과" 순간을 스스로 감지(기존 통과선
      감지 로직 재사용)해 카운트다운·실시간 카운터를 그리게 했다.
    - `static/stage.js`: 무대 상단에 컷오프 패널 신설. 창이 열려 있는
      동안은 실시간 카운트, 닫히면 그 순간 값으로 얼려서 보여준다
      (이후에도 레이스가 끝까지 진행되며 karts가 결승선을 계속 넘지만
      그건 컷오프에 못 든 카트들이라 살아있는 숫자에 넣으면 실제
      통과 인원보다 부풀려 보인다).
    - 실서버 검증: 웹소켓 원시 틱으로 이론값(상위 100명 후보 중 10초
      창 통과자 ~16명)과 실제 서버 결과(`round_pass_ids[1]` 길이 16)가
      일치함을 확인. Playwright로 무대 화면 카운트다운·카운터·마감
      상태 전환을 스크린샷으로 확인.
49. **§12-9 MC 버그 2건**. "AI MC 텍스트에 {{team}} 뭐 이렇게 표시되는
    게 있던데 그거 수정해 줘" + "AI MC 목소리 톤을 스포츠 캐스터처럼
    남자 톤으로 해 달라고 했었는데 그거 제대로 적용됐는지 확인해 줘."
    - **원인**: `MCAgent.pick_line`이 태그 하나당 셔플백 하나만 관리해,
      `{team}`/`{winner_count}` 같은 플레이스홀더가 필요한 줄이 그
      값을 안 주는 호출(`close_call`, `race_progress`, `final_announce`
      등 실제로 여러 곳)에서도 뽑힐 수 있었다.
    - **수정**: 호출 시점에 실제로 값이 있는 플레이스홀더만 요구하는
      줄로 후보를 먼저 거르고 나서 셔플백을 돈다(태그+파라미터 조합별로
      셔플백 분리).
    - **보이스 톤 재확인**: 코드는 정확히 설계대로 동작 중(남성 이름
      힌트 매칭 → 실패 시 피치 하향 보정). 다만 이 개발 PC의 Windows에는
      한국어 음성이 "Microsoft Heami"(여성) 하나뿐이라(Playwright로
      `speechSynthesis.getVoices()` 직접 조회해 확인) 실제로는 "피치를
      낮춘 여성 음성"으로 들린다 — 진짜 남성 음성이 필요하면 **행사
      PC의 Windows 설정에 남성 한국어 TTS 음성을 추가로 설치**해야
      한다(코드로 해결되는 범위가 아님).

**테스트**: pytest 302개 통과(12-8 신규 테스트 8개, 12-9 신규 테스트
1개 포함, 기존 테스트 전부 무변경 통과).

**커밋**: 백엔드(§12-8+§12-9 묶음), 프론트엔드(컷오프 패널 UI), 문서
3개 커밋으로 나눠 푸시.

50. **§12-7 게임 설명 보강(마지막 남은 항목)**. "게임에 대한 설명이
    전체 화면과 모바일 모두에서 불충분해. 처음 게임을 하는 사람들이
    더 쉽게 게임에 참여하게 하기 위한 보강을 해 줘"에 이어 "그것도
    마저 해 줘"로 착수 지시.
    - `static/stage.html`·`stage.js`: HUD에 "❓" 상시 버튼(단축키 `H`,
      `Esc`로 닫기)을 추가해 `#help-panel`을 토글한다. 기존 커밋 대기
      오버레이의 소개 문구를 재사용하고 "화면 보는 법"(POSITION/부서
      통과율/장애물/결승선 카운트다운 설명) 절을 새로 얹었다.
      `.round-transition`과 같은 화면 중앙 글래스 패널 톤이되, z-index
      50으로 다른 오버레이보다 항상 위에 뜨게 했다.
    - `static/mobile.html`·`mobile.js`: 기존 `#intro-box`를 복제하지
      않고, `#game-view` 안에 "📖 게임 설명 다시 보기" 링크만 추가해
      클릭하면 그 콘텐츠를 다시 펼치고 스크롤한다(콘텐츠 중복 없음).
    - 두 화면의 소개 문구를 §12-4(장애물 실제 반영)·§12-8(결승선
      컷오프)에 맞춰 다시 점검해 이제는 틀린 문장이 안 남게 했다.
    - Playwright 검증: 무대에서 ❓ 클릭 → 패널 표시 → `Esc`로 닫힘,
      모바일에서 온보딩 완료 후 "설명 다시 보기" 클릭 → intro-box
      재확장 + 스크롤 확인. 콘솔 에러 0건.

**테스트**: pytest 302개 통과(순수 프론트엔드 변경, 코드 회귀 없음).

**커밋**: 프론트엔드 1개 커밋으로 푸시(뒤이어 이 문서·작업계획서 갱신
커밋).

**§12 인수인계 항목 전부 완료**: 12-1~12-4, 12-6~12-9 전부 끝났고
12-5만 "구현 안 함"으로 결론이 나 있다. §12에 더 이상 남은 일이 없다.

### 2026-08-08 (야간) — 결선 카운트다운 · 장애물 배치/움직임 · 설명 정리

사용자 지시 4건을 반영했다. 모두 작업 단위로 나눠 커밋·푸시했다.

1. **장애물이 결승선 앞 구간에만, 고른 간격으로, 그리고 움직인다**
   - 예전에는 절대 진행률 0.12~0.80에 균일 난수로 뿌려서, 결승선이 트랙
     중간쯤인 R1(통과선 약 0.55)에서는 절반 가까이가 **결승선 너머**에
     놓였다. 이제 위치를 "결승선까지의 비율"로 잡고 결승선을 곱한다.
   - 균일 난수 → **균등 슬롯 + 슬롯 내 지터**로 바꿔 뭉침/공백을 없앴다.
   - 좌우 흔들림·회전·부유를 추가했다. 파라미터는 전부 시드 파생이라
     관전 화면이 몇 대든 똑같이 움직인다. 흔들림 폭은 **자기 레인 안**
     으로 제한했다(옆 레인 침범 시 "내 레인이 아닌데 왜 맞았지"가 된다).
   - ⚠️ **이 변경의 핵심 제약은 순환 참조 방지였다.** 순위 계산에 쓰이는
     페널티 합은 레인 일치로만 정해져 결승선을 필요로 하지 않는다.
     그래서 위치 무관 명세(`race._hazard_specs`)와 절대 위치
     (`race.obstacle_layout`)를 분리했다. 위치가 필요한 건 dip 타이밍과
     화면 렌더뿐이고 그쪽은 결승선 확정 후에만 호출되므로 순방향 체인이
     유지된다. **"결승선을 어떻게 바꿔도 페널티 합은 불변"을 테스트로
     못 박았다** — 여기가 깨지면 결정론이 무너진다.
2. **결선(R3) 5초 카운트다운 + 즉시 결과 발표**
   - 1등이 결승선을 넘는 순간부터 5초를 재고, 창이 닫히면 구간 시간이
     남아 있어도 레이스를 끝내고 곧바로 결과 발표로 넘어간다.
   - **당첨자는 여전히 순위로 정해진다.** 결선 결승선은 애초에 "정확히
     N대가 넘도록" 놓여서 창을 좁혀도 당첨자가 바뀔 수 없다(경품 수는
     항상 채워져야 하고, 늦게 들어온 카트를 떨어뜨려도 그 자리를 채울
     사람은 더 뒤에 있는 카트뿐이다). R3의 창은 **"언제 끝낼지"를 정하는
     연출 규칙**이며, 이 점을 코드 주석과 기준 문서에 명시했다.
   - 무대는 R1/R2와 같은 컷오프 패널을 쓰되 문구를 "결과 발표까지"로
     바꾸고, 창이 닫히면 체커기 배너 + 함성 + 컨페티로 마무리한다.
3. **모바일에 라운드별 예측 대상 명확화** — "탈락 대상을 거른다"는
   뭉뚱그린 설명 대신, 첫 화면에 **라운드별 예측 대상 표**를 넣었다
   (R1·R2 = 부서 하나 → 통과율 1위 / 결선 = 카트(개인) 하나 → 1등,
   배점 3배). 예측 카드에도 대상 배지와 라운드별 질문을 항상 띄운다.
4. **기준 문서에서 "이전 방식" 서술을 이력 문서로 이관** — `게임구조.md`는
   "지금 어떻게 동작하는가"만 담도록 정리하고, 시도했다가 접은 안과 그
   사유는 **작업계획서 §13 "설계 변경 이력"** 으로 모았다(베팅 모드 폐지,
   확신도 배분 폐지, 2지선다 채점 교체, 결선 부서당 1대 폐지, 업그레이드
   상점 폐지, `/verify` 제거, 레이스 시간 단축, 장애물 정책 변경).
   `게임구조.md` 머리말에 "여기에는 예전 얘기를 쓰지 않는다"를 명시했다.

**테스트**: 305개 통과(299 → +6). 신규는 장애물 배치 3종(결승선 앞쪽 배치 /
간격 균등 / 결승선을 바꿔도 페널티 불변)과 결선 조기 종료 1종(가짜 시계로
"1위 통과 + 5초"를 재현해 마지막 틱에만 `race_over`가 붙는지, 진행률 1.0
전에 멈추는지, 종료 시점이 첫 통과 + 창 길이와 일치하는지)이다.

### 2026-08-08 (심야) — 자체 감사로 찾은 결함 5건 수정

사용자 지시("이외에 더 개선할 것이 있으면 알아서 찾아서")에 따라 코드베이스를
감사했다. 새 기능을 얹는 대신 **실제로 깨져 있던 것**을 찾아 고쳤다.

1. **참가자 폰에 최종 당첨 결과가 전혀 안 떴다** (가장 큰 구멍)
   서버는 `prize_winners`를 브로드캐스트하는데 `mobile.js`가 그 메시지를
   아예 처리하지 않았다. 참가자에게 "내가 됐나?"는 행사에서 가장 궁금한
   한 가지인데, 무대 시상대에서 이름을 찾는 방법밖에 없었다.
   → `/api/predict/me`에 `prize`를 추가하고 폰 맨 위에 결과 패널을 띄운다.
   **폴링 응답에도 실어 보내** 발표 순간 화면이 꺼져 있었거나 새로고침한
   사람도 놓치지 않게 했다(WS 이벤트만으로 처리하면 영영 못 본다).
2. **레이스가 끝나도 "내 카트 현황"이 안 사라졌다**
   `latest_race_tick`을 세션 리셋 때만 비워서, 시상식 내내(그리고 행사가
   끝난 뒤로도) 마지막 틱이 그대로 보였다. 결선이 1위 통과 + 5초로 조기
   종료되면서부터는 진행률 1.0 전 스냅샷이 얼어붙어 **아직 달리는 것처럼**
   보인다. → 런북 완료 시 캐시를 비운다.
3. **순수 레이싱 세션의 폰은 아무것도 못 봤다**
   `/api/predict/me`가 `predictions_enabled=False`면 곧바로 돌아가면서
   prize/race_status를 전부 뺐고, 모바일도 그 모드에서 폴링을 꺼버렸다.
   정작 이 모드에서 `basis="race"`로 당첨자가 정해지는데도 폰에서는 알
   수 없었다. → 예측과 무관한 정보는 이 경로에서도 내려주고, 레이싱
   구간에는 계속 폴링한다.
4. **basis 키 불일치** — 폰 안내 문구 표가 `confidence`를 키로 쓰는데
   서버가 내보내는 값은 `race`/`prediction`뿐이라 근거 설명이 조용히
   비었다. 무대·폰이 같은 키만 쓰는지 검사하는 테스트를 넣었다 --
   한쪽만 바꿔도 화면이 조용히 비는 종류라 사람 눈으로는 안 잡힌다.
5. **끊긴 상호참조 2건** — `fairness.py`의 `canonical_json` 설명이 삭제된
   `verify.js`를 가리키고 있었고(규칙을 지켜야 하는 진짜 이유 = 직렬화가
   달라지면 재계산이 위변조로 오탐된다,로 다시 씀), `main.py`가 존재하지
   않는 상수명 `START_LIGHTS_BUDGET_MS`를 참조하고 있었다(실제 이름으로 정정).

**테스트**: 310개 통과(305 → +5). 기존 테스트 하나
(`test_mobile_race_status_helpers_after_race`)는 "런북이 끝나도 캐시가
남아 있다"는 **버그를 검증하고 있었으므로**, 결선 구간만 돌려 진행 중
상태를 보도록 재작성했다.

---

## 2. 다음에 이어서 할 일 (08/07 마감까지, 우선순위순)

### 우선순위 높음 (마감 전 필수)

1. **실제 화면 눈으로 확인**(가장 급함). 백엔드 값은 실서버 데모로
   검증했지만(§1의 31번 끝 참고), 아래는 아직 **사람 눈으로** 안 봤다:
   - ①모바일에서 확신도를 배분하고(특히 **R3에 80 몰아주기**) 라운드별
     대상을 골라 **실제로 예측이 반영되는지**(32~35번). 라운드가 끝나면
     폰에 **점수 내역**(예측/통과/팀 순위)이 항목별로 뜨는지도 함께.
   - ②라운드마다 트랙이 확실히 달라 보이는지(26번)
   - ③장애물 충돌 연출이 과하지 않은지, 그리고 **결승선에서 카트가
     제자리로 정확히 수렴하는지**(31번 안전장치)
   - ④좌우 패널/MC 바가 더 이상 서로 가리지 않는지(22·23번)
   - ⑤MC가 레이스 종료 후 조용해지는지(21번)
   - ⑥시상대에 최종 **점수**가 뜨는지(28번), 무대 우측에 **실시간 선택
     분포** 막대가 뜨는지(32번)
   - ⑦**폰을 안 든 참가자도 리더보드에 올라와 점수가 쌓이는지**(34번) —
     데모를 봇 없이(`with_bots: false`) 돌리면 바로 확인된다.
2. `docker build` 재확인 — 배포 경로가 여전히 정상인지.

### 여유 있으면 (전략노트 §0 기존 판단 유지 — 마감 임박이라 새 기능보다 안정성 우선)

- 예측 게임 최종 결과 화면에 "예측왕" 발표 연출 강화(컨페티 등).
- 관리자 콘솔 런북 미리보기(구간별 시간 배분 표).
- 능력별 카트 스프라이트 색감/파티클 차별화, 능력별 다른 효과음.
- 봇은 캐릭터를 고르지 않는다 -- 의도된 동작(부서 기반 폴백을 그대로
  보여주기 위함)이지 버그 아님.
- T4(라운드 시작 전 투자 선택 메뉴)는 **폐기**. 업그레이드 상점을
  제거(§1의 27번)한 데 이어 베팅도 없어졌고(32번), 지금은 확신도 배분
  — 특히 R3 몰아주기(35번) — 이 그 역할을 하고 있어 별도 메뉴를 만들
  실익이 없다. T3(결선 팀당 1대)는 Round 2 레이스 시각화 근본
  재설계 없이는 재추진 불가 — 재검토하려면 먼저 "통과선" 개념 자체를
  다시 설계해야 함.

### 사용자 액션이 필요한 것 (내가 대신 못 함)

- ~~실제 Render 배포 실행~~ **완료**: https://taachuwi-grandprix.onrender.com/
  (README/전략노트 반영됨). 무료 플랜이라 트래픽 없으면 슬립되므로,
  행사/심사 직전 한 번 미리 접속해 깨워둘 것.
- 실제 기기·행사장 Wi-Fi로 최소 1회 리허설.
- BGM/효과음/MC 음성을 실제 사용할 브라우저에서 들어보고 만족스러운지
  확인(안 되면 🔊/🎙 버튼으로 개별적으로 끌 수 있음).

---

## 3. 알아두면 좋은 것

- **게임 규칙·설정값을 바꿀 때는 `타추위-추첨프로그램_게임구조.md`를
  먼저 보라.** 진행 순서, 라운드별 규칙, 보상표, 그리고 "무엇을 바꾸려면
  어느 파일의 어느 상수를 고쳐야 하는지" 치트시트가 §9에 정리돼 있다.
- **연출과 결과를 절대 섞지 말 것**(이 프로젝트의 제1원칙). 장애물 충돌,
  캐릭터 능력, 응원 이모지, 카트 색 등 참가자가 하는 어떤 행동도
  `fairness.py`/`race.py`의 순위 계산에 값을 넘기면 안 된다. 장애물
  효과가 "보이는 위치"에만 적용되고 결승선 전에 0으로 수렴하도록
  설계된 이유가 이것이다(`EFFECT_FADE_TAIL`).
- **참여형 게임은 예측 게임 하나뿐이다.** `Session.prediction_mode`와
  `app/gambling.py`, `/api/bet/*`는 전부 삭제됐다. 세션 설정에 남은
  스위치는 `predictions_enabled`(켜기/끄기)뿐이다.
- **점수 규칙을 바꿀 때는 `app/predictions.py` 상단 상수만 보면 된다** --
  `RANK_RATIOS`(순위별 지급 비율), `FLOOR_RATIO`(참여 보상),
  `ROUND_WEIGHTS`(라운드 배점), `MIN_ALLOC`/`MIN_ALLOC_ROUNDS`(확신도
  하한이 걸리는 라운드), `FINISH_POINTS`/`TEAM_RANK_POINTS`/
  `FINAL_WIN_POINTS`(성과 점수).
- **자동 배정 규칙이 라운드마다 다르다** -- `_default_target()` 참고.
  R1·R2는 자기 부서, R3는 시드 파생 무작위. 부서 매핑은 커밋된
  스냅샷의 `departments`를 뒤집어 만들기 때문에(`_department_by_pid`),
  자동 배정값이 후보 목록에 없는 사태가 생기지 않는다.
- **캐릭터 선택은 예측 게임과 독립적** -- 예측 게임이 꺼져 있어도 모바일
  온보딩(부서→이름→캐릭터)까지는 항상 가능하다.
- **스냅샷 파일**: `data/prediction_snapshot.json`,
  `data/character_choices.json` (둘 다 `.gitignore` 등록됨). 서버 재시작
  시 자동 복원. `data/gambling_snapshot.json`은 더 이상 쓰이지 않으므로
  남아 있으면 지워도 된다.
- **실행**: `python run.py`. 테스트: `pytest -q`. 배포 이미지 빌드:
  `docker build -t lucky-draw-test .`(이제 성공함).
- **BGM 디버그**: 브라우저 콘솔에서 `SFX.getSceneName()`으로 현재 재생
  중인 배경음악 장면(idle/standby/anticipation/race1/race2/race3/
  victory/roulette)을 확인할 수 있다. `SFX.playScene("race3")`로 직접
  틀어보거나 `SFX.hit("explode")`, `SFX.finishCross(0)`으로 효과음만
  따로 들어볼 수도 있다.
- **Playwright 검증 환경**: Chromium을 직접 다운받을 수 없는 사내망
  환경(SSL 인터셉션)이라, `playwright` npm 패키지만 설치하고
  `executablePath`로 로컬에 이미 설치된 시스템 Chrome/Edge를 직접
  구동하는 방식을 쓴다(스크래치패드에 재사용 가능한 드라이버 스크립트
  패턴 있음). 저장소에는 스크린샷/영상을 커밋하지 않음(용량 문제 +
  요청받지 않음).
