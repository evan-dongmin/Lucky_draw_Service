# 게임 구조 설명서 (지시용 기준 문서)

> 마지막 갱신: 2026-08-07. **이 문서는 "지금 프로그램이 실제로 어떻게
> 동작하는가"를 한눈에 파악하고, 그걸 기준으로 변경 지시를 내리기 위한
> 문서다.** 각 항목 끝에 `[파일:함수]`로 실제 코드 위치를 적어뒀으니
> "여기 이 숫자를 바꿔줘" 식으로 바로 지시할 수 있다.
>
> 참가자용 쉬운 설명은 `타추위-추첨프로그램_게임소개.md`,
> 개발 이력·다음 할 일은 `타추위-추첨프로그램_진행상황.md` 참고.

---

## 0. 30초 요약

1. 진행자가 명단을 올리고 **커밋**을 만든다 → 이 순간 레이스 결과가
   시드로 완전히 고정된다(나중에 `/verify`에서 누구나 재계산 검증 가능).
2. 무대 화면에서 **3라운드 카트 레이스**가 자동으로 진행된다.
   레이스 결과 자체는 1번에서 이미 정해져 있고, 화면은 그걸 보여줄 뿐이다.
3. 참가자는 폰으로 **매 라운드 "누가 살아남을지" 예측**한다. 잃는 것은
   없고, 순위가 가까울수록 점수가 크다(빗나가도 0점이 아니다).
4. **최종 경품 당첨자는 레이스 1등이 아니라, 예측 리더보드 상위 N명이다.**
   ← 이게 이 프로그램의 핵심 반전 구조다.
5. **폰을 안 들어도 명단에 있으면 자동 참가된다** — 1·2라운드는 자기
   부서가 기본 선택으로 들어가므로 아무것도 안 해도 경품 대상이다.

---

## 1. 두 개의 층 (가장 중요한 개념)

이 프로그램은 **완전히 분리된 두 개의 층**으로 되어 있다. 변경 지시를
내릴 때 "어느 층 얘기인지"만 분명히 하면 사고가 안 난다.

| | **A층: 레이스 (고정·검증 가능)** | **B층: 예측 게임 (실시간·변동)** |
|---|---|---|
| 무엇 | 누가 각 라운드를 통과하는가 | 참가자가 그걸 얼마나 잘 맞혔는가 |
| 언제 정해지나 | **커밋하는 순간 전부 확정** | 행사 내내 계속 변함 |
| 정하는 것 | 카트 순위, 라운드 통과자 | **실제 경품 당첨자** |
| 코드 | `app/fairness.py`, `app/race.py` | `app/predictions.py` |
| 조작 가능성 | 불가능(커밋-리빌 + HMAC) | 해당 없음(참가자 실력/운) |

**절대 규칙**: B층(예측·응원 등 참가자가 하는 어떤 행동도)이
A층 계산에 영향을 주면 안 된다. 영향을 주는 순간 "결과가 커밋 시점에
고정된다"는 이 프로젝트의 핵심 신뢰 장치가 깨진다.
장애물 충돌 연출조차 "보이는 위치"만 바꾸고 실제 순위는 건드리지 않는
이유가 이것이다(§8 참고).

---

## 2. 전체 진행 순서 (런북)

진행자가 "레이스 시작"을 누르면 아래 9구간이 **자동으로** 순서대로
흘러간다. 사람이 중간에 개입할 필요가 없다.
`[app/director.py: _BASE_SEGMENTS]`

기준 총 시간 **600초(10분)** 일 때:

| # | 구간 | 시간 | 화면에서 벌어지는 일 |
|---|---|---|---|
| 1 | `opening` | 20초 | 오프닝, 커밋 해시 공개, QR 안내 |
| 2 | `r1_lock` | 20초 | 1라운드 예측 마감 카운트다운 |
| 3 | `race_r1` | **110초** | 🏁 **1라운드 레이스** (전원 → 100명) |
| 4 | `score_r1_select_r2` | 60초 | R1 결과 발표 + 점수 지급 + R2 예측 창 |
| 5 | `race_r2` | **110초** | 🏁 **2라운드 레이스** (100명 → 결선 진출자) |
| 6 | `score_r2_select_r3` | 60초 | R2 결과 발표 + 점수 지급 + R3 예측 창 |
| 7 | `race_r3` | **80초** | 🏁 **결선 레이스** (결선 진출자 → 최종 순위) |
| 8 | `final_announce` | 70초 | 리빌 + 최종 채점 + 🏆 **경품 당첨자 발표** |
| 9 | `verify` | 70초 | 검증 안내(/verify로 누구나 재계산) |

**시간 비율을 바꾸고 싶으면**: `_BASE_SEGMENTS`의 숫자만 고치면 된다.
합계가 300이 되도록 맞추면 총 시간에 비례해 자동 스케일된다.
선택창(4·6번)은 최소 30초가 보장된다(`MIN_SELECTION_WINDOW_SECONDS`).

---

## 3. 라운드별 상세

### 라운드 1 — 대규모 서바이벌

| 항목 | 값 |
|---|---|
| 출전 카트 | 전원 (최대 250대) |
| 통과 인원 | **상위 100명** `[app/fairness.py: R1_PASS_COUNT]` |
| 예측 대상 | **부서(팀)** — "어느 부서가 통과율이 높을까?" |
| 채점 | 고른 부서의 **통과율 순위별 차등**(§5) `[app/main.py: _ranked_targets]` |
| 미선택 시 | **자기 소속 부서** 자동 선택 `[predictions.py: _default_target]` |
| 트랙 | 넓고 완만한 고속 스피드웨이 `[static/stage.js: TRACK_DEFS[1]]` |
| 카메라 | 광각(250대가 한 화면에) |

### 라운드 2 — 결선 진출전

| 항목 | 값 |
|---|---|
| 출전 카트 | R1 통과자 100명 |
| 통과 인원 | **결선 진출자 F명** (당첨 인원수에 따라 5~10명) `[fairness.py: resolve_finalist_count]` |
| 예측 대상 | **부서(팀)** |
| 채점 | 그 라운드 통과율 순위별 차등(R1과 동일 곡선, 배점 1.5배) |
| 미선택 시 | **자기 소속 부서** 자동 선택 |
| 트랙 | 폭이 절반인 **지그재그 시케인** `[TRACK_DEFS[2]]` |

> **참고**: 결선 진출은 부서와 무관하게 **전체 순위 상위 F명**이다.
> "부서당 1명씩"으로 바꾸는 안을 검토했다가, 등수 낮은 사람이 진출하고
> 높은 사람이 탈락하는 장면이 신뢰를 깎는다고 판단해 폐기했다
> (작업계획서 §10.1(b) 참고).

### 라운드 3 — 결선

| 항목 | 값 |
|---|---|
| 출전 카트 | 결선 진출자 F명 (5~10대) |
| 통과 인원 | **레이스 1위~N위** (N = 경품 개수) |
| 예측 대상 | **개별 카트** (부서가 아니라 사람) |
| 채점 | 고른 카트의 **결승 등수별 차등**, 배점 **3.0배**(가장 큼) |
| 미선택 시 | **무작위 배정**(결선 진출자 개인이라 "자기 팀"이 없다) |
| 트랙 | 가장 좁고 굴곡 심한 **테크니컬 서킷** `[TRACK_DEFS[3]]` |
| 카메라 | 클로즈업 |

**⚠️ 여기서 나온 레이스 순위는 경품 당첨자가 아니다.** 아래 §6 참고.

---

## 4. 참가자가 폰에서 하는 것

접속 → 부서 선택 → 이름 선택 → 캐릭터(카트) 선택 → 게임 화면.
`[static/mobile.js]`

참여형 게임은 **예측 게임 하나뿐이다** `[app/predictions.py]`. 예전에 있던
"사이버머니 갬블링(패리뮤추얼 베팅)" 모드는 제거했다 — 이유는 §5-4.

### 라운드당 1픽 — 손해 없음

- 각 라운드마다 승리 예측 대상을 **하나만** 고른다(부서 R1·R2, 결선
  카트 R3). 확신도를 나눠 거는 단계는 없다 — 예전엔 100점을 3라운드에
  직접 배분해야 했지만, 선택 창이 짧게 열리는 라이브 이벤트에는
  불필요한 마찰이라 판단해 없앴다(2026-08-07, 사용자 피드백).
- **틀려도 차감이 전혀 없다.** 순위가 가까울수록 점수가 크고, 순위표
  밖이어도 참여 보상이 들어간다(§5-1)
- 라운드마다 동일한 배점(`ROUND_BASE_POINTS`)을 걸고 겨루고, 라운드
  가중치(R1 1.0 / R2 1.5 / R3 3.0)가 이미 "결선이 가장 크게 흔든다"는
  드라마를 만들어준다 — 개인이 뭔가를 몰아줄 필요가 없다(§5-2)

### 미선택 시 어떻게 되나 ⭐

폰을 안 들었거나 시간 안에 못 고른 사람도 **자동으로 채워진다**
`[predictions.py: _default_target]`:

| 라운드 | 자동 배정값 | 이유 |
|---|---|---|
| R1·R2 | **자기 소속 부서** | 대상이 부서라서 가장 자연스러운 기본값. "자기 팀 응원"이라는 서사와도 맞고, 폰 없이도 납득 가능한 결과가 나온다 |
| R3 | **시드 파생 무작위** | 대상이 결선 진출자 개인이라 "자기 팀"에 해당하는 것이 없다 |

> 소속 부서가 그 라운드 후보에 없으면(부서 병합 등) 무작위로 폴백한다.

### 전원 자동 참가 ⭐

**커밋하는 순간 명단 전원에게 예측 카드가 만들어진다**
`[predictions.py: enroll_all]`. 예전에는 모바일로 참여(join)한 사람만
카드가 생겨서, 폰을 안 든 사람은 리더보드에 아예 없었고 따라서 경품
대상에서 통째로 빠졌다. 지금은 **명단에 있기만 하면 경품 가능성을 일단
확보**한다.

폰을 들 유인은 그대로 남는다:
- 자기 부서 대신 **실제로 잘할 것 같은 부서**를 고를 수 있다
- R3는 어차피 무작위라, 여기서 직접 고르는 것만으로도 큰 차이가 난다

---

## 5. 점수가 쌓이는 방식 ⭐

참가자 폰에 라운드별로 항목이 쪼개져 표시된다
`[mobile.js: renderRewardBreakdown]`.

### 5-1. 예측 점수 — 순위 차등 (핵심)

고른 대상이 **그 라운드 결과 순위에서 몇 등이었는지**에 따라 배점의
일정 비율을 받는다 `[predictions.py: RANK_RATIOS, rank_ratio]`.

| 내가 고른 대상의 순위 | 지급 비율 |
|---|---|
| 🥇 1위 | **100%** |
| 2위 | 60% |
| 3위 | 35% |
| 4위 | 20% |
| 5위 | 10% |
| 6위 이하 | **5%** (참여 보상) |

점수 = `ROUND_BASE_POINTS(100) × 라운드 가중치 × 순위 비율`
라운드 가중치는 **R1 1.0 / R2 1.5 / R3 3.0** `[ROUND_WEIGHTS]`.

> **소수파 보너스**: 1위를 정확히 맞혔는데 그걸 고른 사람이 적을수록
> 최대 2배까지 곱해진다. **1위 적중에만** 붙는다 — 차등 구간까지 주면
> "틀렸는데 아무도 안 골라서 더 받는" 모순이 생기기 때문이다.

**설계 의도**: 예전 방식(정답/오답 2지선다)은 R1에서 8할이 0점을 받고
첫 라운드에 폰을 내려놨다. 지금은 누구도 0점이 아니되, 1위 적중과 꼴찌
사이에 **20배** 격차가 있어 리더보드 변별력은 유지된다.

### 5-2. 결선(R3) 배점 3배 — 역전 장치

R3는 라운드 가중치가 3.0으로 가장 크다(R1의 3배, R2의 2배). 확신도를
개인이 몰아줄 필요 없이, **배점 자체가 이미 "결선이 가장 크게 흔든다"는
구조**다 — 그래서 앞 두 라운드를 모두 1위로 맞힌 사람도 결선 하나로
뒤집힐 수 있다.

예시(소수파 보너스는 제외한 계산):

| | R1 | R2 | R3 | 합계 |
|---|---|---|---|---|
| **역전형** — 앞 두 라운드 2위(빗나감), R3만 1위 적중 | `100×1.0×0.6` = 60 | `100×1.5×0.6` = 90 | `100×3.0×1.0` = **300** | **450점** |
| **선두형** — 앞 두 라운드 1위 적중, R3 2위(빗나감) | `100×1.0×1.0` = 100 | `100×1.5×1.0` = 150 | `100×3.0×0.6` = 180 | **430점** |

즉
**막판까지 리더보드가 확정되지 않는다** — 이게 이 게임의 클라이맥스다.
`[tests/test_predictions.py: test_round3_weight_lets_a_late_comeback_beat_an_early_leader]`

### 5-3. 성과 점수 — 팀 대항전의 실체

예측과 **무관하게** 자동으로 쌓인다 `[predictions.py: score_round]`.

| 항목 | 점수 | 조건 | 상수 |
|---|---|---|---|
| 🏁 결승선 통과 | **+8** | 그 라운드를 통과한 개인 전원 | `FINISH_POINTS` |
| 🥇 우리 팀 1위 | **+20** | 그 라운드 통과율 1위 부서 소속 통과자 | `TEAM_RANK_POINTS[0]` |
| 🥈 우리 팀 2위 | **+14** | 2위 부서 소속 통과자 | `TEAM_RANK_POINTS[1]` |
| 🥉 우리 팀 3위 | **+8** | 3위 부서 소속 통과자 | `TEAM_RANK_POINTS[2]` |
| 🏆 결선 당첨 | **+60** | 결선 레이스 최종 당첨자 | `FINAL_WIN_POINTS` |

우리 부서가 그 라운드에 많이 살아남을수록 부서원 전체 점수가 오르고,
그게 곧 경품 당첨 확률로 이어진다.

> 4위 이하 부서는 팀 보너스가 없다(개인 통과 점수 +8은 그대로 받는다).
> 등수를 늘리거나 점수를 바꾸려면 `TEAM_RANK_POINTS` 리스트만 고치면 된다.

### 5-4. 왜 베팅을 없앴나 (기록)

기존 갬블링 모드는 패리뮤추얼(맞힌 사람끼리 판돈을 나눠 갖는) 방식이었다.
실제로 돌려본 결과 네 가지 문제가 확인됐다:

1. **제로섬이라 전체 잔액이 늘지 않는다** — 다 같이 열심히 해도 제자리
2. **맞혀도 배당이 눌린다** — 정답 후보가 넓어 승자가 많으면 1.5~3배
3. **어렵다** — 20초 안에 "대상 + 금액" 두 번을 결정해야 한다
4. **안 걸면 그 라운드가 통째로 무효** — 머뭇거린 사람이 소외된다

"소수가 크게 딴다"도 "다수가 조금씩 받는다"도 아닌 상태였다. 사내 행사
성격과도 맞지 않아, 모드 자체를 제거하고 용어(베팅/판돈/배당)도 전부
예측/점수로 교체했다.

---

## 6. 최종 경품 당첨자가 정해지는 방식 ⭐

**가장 중요하고, 가장 오해하기 쉬운 부분.**

```
레이스 결선 1등  ≠  경품 당첨자
```

- **예측 게임이 켜져 있으면**: 최종 리더보드(점수) **상위 N명이 실제
  경품 당첨자**다. `prize_basis="prediction"`
- **꺼져 있으면**: 레이스 결과를 그대로 쓴다. `prize_basis="race"`
- `[app/main.py: _compute_prize_winners]`

즉 **레이스에서 1라운드에 탈락했어도 예측을 잘했으면 당첨될 수 있고,
결선 1등이어도 예측을 못 했으면 당첨에서 빠질 수 있다.**

> **모수는 명단 전체다.** 전원 자동 참가(§4) 덕분에 모바일 미참여자도
> 리더보드에 올라와 있고, 자기 부서 자동 선택으로 점수도 쌓인다. 다만
> 직접 고른 사람이 통계적으로 위에 있게 된다 —
> **참여는 유리하게, 미참여는 불리하되 배제되지 않게** 설계했다.

발표 시점도 일부러 분리해 뒀다:
1. `revealed` — 레이스 결과 공개 (여기선 아직 당첨자 미확정)
2. R3 최종 채점 (리더보드가 여기서 마지막으로 뒤집힌다)
3. `prize_winners` — **경품 당첨자 확정 발표** ← 시상대는 이때 뜬다

시상대에는 당첨자 이름과 함께 **최종 점수**가 표시된다
`[static/stage.js: buildPodium]`.

> 진행자용: **관리자 콘솔에는 "실제 경품 당첨자"가 레이스 결과보다 크게,
> 먼저 표시된다.** 상품을 나눠줄 때는 반드시 그 목록을 봐야 한다.

---

## 7. 화면 구성

### 무대 화면 (`/`) — 프로젝터용

```
┌──────────────────────────────────────────────────────┐
│ 🏁 타추위 GRAND PRIX  [ROUND 2]  구간명      ⏱ 11.0s │ ← 상단 HUD
├────────────┬────────────────────────┬────────────────┤
│ POSITION   │                        │ 부서 통과율    │
│ 1 P0052    │      🏎  레이스        │ 개발팀  ▓▓░ 60%│
│ 2 P0010    │        트랙            │ 영업팀  ▓░░ 30%│
│ 3 P0144    │                        ├────────────────┤
│  ...       │   (장애물·카트·연석)   │ 실시간 선택분포│
│ (실시간)   │                        ├────────────────┤
│            │                        │ 예측           │
│            │                        │ 리더보드       │
├────────────┴────────────────────────┴────────────────┤
│           [팀 레전드: 부서색 + 능력 이모지]           │
│ 🎙 AI MC  "결승선이 눈앞입니다!"                      │ ← 좌우 패널 사이에만
└──────────────────────────────────────────────────────┘
```

- 좌우 패널은 반투명이라 뒤 트랙이 비친다. 화면이 1100px보다 좁으면
  MC 바가 전체 폭을 쓰고, 900px 미만이면 좌우 패널이 사라진다.
- **AI MC**: 상황별 멘트를 자막 + 음성으로. 🎙 버튼으로 끌 수 있다
  (끄면 자막만). 레이스가 끝나면 대기 중이던 멘트는 자동 취소된다.
- **효과음/BGM**: 🔊 버튼으로 on/off. 장면별 BGM 7종이 자동 전환된다 --
  대기(`idle`) · 출발 대기(`standby`) · 선택창(`anticipation`) ·
  **라운드별 레이스 BGM 3종**(`race1`/`race2`/`race3`, 라운드가 올라갈수록
  빠르고 무거워진다) · 결과/시상대(`victory`). (`roulette` 씬은
  `audio.js`에 코드만 남아 있고, 콘솔에서 룰렛 모드를 없앤 뒤로는 어떤
  화면에서도 재생되지 않는다.)
  효과음은 스타트 라이트·추월·**장애물 충돌 6종**(콘/기름·타이어/미끄러짐/
  스핀/충돌/폭발) · **결승선 통과**(선두는 체커기+함성) · 마지막 랩 종 ·
  발표 직전 상승음 · 팡파레/컨페티까지 상황별로 다르다.

### 참가자 화면 (`/mobile`)
내 점수 · **내 카트 현황**(레이스 진행 중 실시간 등수, 통과선 통과
여부 — 진행 중이면 통과선까지 몇 % 왔는지 진행률 바로 표시 —, 소속 팀
실시간 순위, 포인트 순위) · 라운드별 예측 카드(대상 선택, 클릭 한 번으로 확정) ·
**라운드별 점수 내역**(예측 점수 / 통과 / 팀 순위 / 결선 당첨으로
쪼개져 표시) · 리더보드 · 카트 변경 · 응원 이모지 버튼

**"내 카트 현황"은 위치 데이터를 통째로 뿌리지 않는다.** race_tick(무대
전용, 위치 데이터가 커서 모바일 250대에는 안 보냄)의 최신 틱 하나만
서버가 캐시해 두고, 폰이 레이스 진행 중 2초 간격으로 폴링할 때마다 그
사람 한 명분만 계산해서 돌려준다. `[app/main.py: latest_race_tick,
_my_race_status, _my_department_rank]`, `[app/predictions.py:
PredictionEngine.rank_of]`

### 운영 콘솔 (`/admin`)
명단 업로드 → 세션 생성 → 커밋 → 레이스 시작 → 카메라 전환 ·
**실제 경품 당첨자 표시** · 비상 조기 종료

### 검증 페이지 (`/verify`)
시드+명단만으로 브라우저에서 결과를 **처음부터 재계산**해 서버 값과 대조.
서버를 안 믿어도 되는 구조. `[static/fairness.js]`

---

## 8. 레이스 연출 (전부 시각 전용)

**이 항목의 모든 것은 순위에 영향이 없다.** 순위는 §1의 A층에서 이미
확정돼 있고, 아래는 그걸 재미있게 보여주기 위한 장치일 뿐이다.

### 장애물 `[static/stage.js: OBSTACLE_DEFS]`

8종이 트랙 위를 떠다니며, 카트가 닿으면 효과가 걸린다:

| 장애물 | 효과 | 체감 |
|---|---|---|
| 🚧 콘 | wobble | 살짝 흔들림 |
| 🛢️ 기름 | slow | 감속 + 미끄러짐 |
| 🛞 타이어 | slow | 감속 |
| 🍌 바나나 | spin | **스핀아웃**(한 바퀴 회전) |
| 💧 웅덩이 | slide | 옆으로 크게 미끄러짐 |
| 🪨 바위 | stall | **정지에 가깝게 느려짐** |
| 💣 폭탄 | stall | **정지 + 강한 스핀** |
| 🧊 얼음 | slide | 가장 크게 미끄러짐 |

- 크기는 종류별 기본값에 랜덤(0.75~1.45배)을 곱해 매번 다르다
- 동시 등장 개수: R1 35개 / R2 45개 / R3 50개
  `[OBSTACLE_MAX_ACTIVE_BY_ROUND]`(사용자 요청으로 대폭 증가 — 예전엔
  7/10/12개였다)
- 스폰 간격: 약 0.12~0.34초(예전 0.42~1.12초보다 훨씬 촘촘함)
- 부딪히면 눈에 띄게 뒤처졌다가 1~2초에 걸쳐 따라잡는다(감속 lag가
  종류별 0.03~0.09, 예전보다 3~4배 커짐). 화면에 보이는 위치에만
  적용되고 통과 판정에 쓰는 실제 값은 절대 안 바뀐다.
- **결승선 이후에는 장애물이 생기지 않는다** — 스폰 위치가 통과선을
  넘지 못하도록 상한이 걸려 있다.

**안전장치**: 모든 효과는 시간이 지나면 사라지고, 게다가 레이스 막판
12% 구간에서는 강도에 0으로 수렴하는 페이드가 곱해진다
`[EFFECT_FADE_TAIL]`. 따라서 **결승선 통과 시점에는 보이는 위치와 실제
위치가 정확히 일치**한다 — 화면과 발표 결과가 어긋날 수 없다.

### 카메라·맵·결승선 `[static/stage.js]`
- **모든 라운드에서 카메라가 리더를 따라 확대·이동**한다(R1도 포함 —
  예전엔 R1만 화면 전체가 고정된 광각이라 카메라가 안 움직였다).
  뒤처진 카트는 화면 밖으로 자연스럽게 빠진다(사용자 요청: 뒤처진
  카트는 안 보여도 됨) — 컬링 덕분에 장애물을 늘려도 프레임이 안
  무거워진다.
- 트랙이 쓰는 화면 대역을 넓혀 실제 이동 거리(호 길이)를 늘렸다 —
  같은 진행률이라도 더 멀리, 더 빠르게 도는 것처럼 보인다.
- **결승선은 항상 트랙 시각적 끝(진행률 92% 지점)에 그려진다**
  (`warpProgress`, `FINISH_VISUAL_FRACTION`) — 라운드·인원수에 따라
  원본 통과선 값이 달라도(예: 100/250명 통과 시 원본은 ~0.68) 화면에는
  항상 끝 가까이 보이도록 워프한다. 통과 판정 자체는 안 바뀌고 "어디에
  그릴지"만 재배치하는 것이라, 카트가 통과선을 넘는 시점과 화면에서
  체커기 줄을 넘는 시점은 항상 정확히 일치한다.
- 근접한 순위끼리는 화면에서 계속 앞서거니 뒤서거니 하며 선두가 자주
  바뀌어 보이는 잔물결(`jockeyOffsetFor`)이 더해져 있다 — 역시 순수
  연출이고 실제 순위(POSITION 패널·통과 판정)는 전혀 안 바뀐다.

### 그 외 연출
- 카트 스프라이트: 부서 색상 탑다운 카트 + 개인이 고른 캐릭터 능력 이모지
- F1 스타트 라이트, FINAL LAP 배너, PHOTO FINISH, 추월 이펙트
- 응원 이모지: 폰에서 누르면 무대 화면 하단에 떠오른다(0.4초 쿨다운)

---

## 9. 자주 바꾸게 되는 값 (지시용 치트시트)

| 바꾸고 싶은 것 | 파일 | 이름 |
|---|---|---|
| 구간별 시간 배분 | `app/director.py` | `_BASE_SEGMENTS` |
| 총 진행 시간 기본값 | `app/director.py` | `DEFAULT_TOTAL_SECONDS` |
| R1 통과 인원 | `app/fairness.py` | `R1_PASS_COUNT` |
| 결선 진출 인원 공식 | `app/fairness.py` | `resolve_finalist_count` |
| **순위별 지급 비율** | `app/predictions.py` | `RANK_RATIOS`, `FLOOR_RATIO` |
| **라운드별 배점 가중치** | `app/predictions.py` | `ROUND_WEIGHTS` |
| **라운드당 고정 배점** | `app/predictions.py` | `ROUND_BASE_POINTS` |
| 통과·팀·결선 성과 점수 | `app/predictions.py` | `FINISH_POINTS`, `TEAM_RANK_POINTS`, `FINAL_WIN_POINTS` |
| 미선택 시 자동 배정 규칙 | `app/predictions.py` | `_default_target` |
| 라운드별 트랙 모양 | `static/stage.js` | `TRACK_DEFS` |
| 장애물 종류·효과 | `static/stage.js` | `OBSTACLE_DEFS` |
| 장애물 개수 | `static/stage.js` | `OBSTACLE_MAX_ACTIVE_BY_ROUND` |
| MC 멘트 | `app/mc.py` | 태그별 멘트 풀 |
| 부서 그룹 수(5~8) | `app/departments.py` | `MIN_GROUPS`, `MAX_GROUPS` |

---

## 10. 실행 방법

```bash
python run.py          # 서버 (http://127.0.0.1:8000)
pytest -q              # 테스트 282개
docker build -t lucky-draw-test .   # 배포 이미지
```

| 주소 | 화면 |
|---|---|
| `/` | 무대(프로젝터) |
| `/admin` | 운영 콘솔 |
| `/mobile` | 참가자 폰 |
| `/verify` | 공정성 검증 |

무대 화면에서 **"데모로 체험하기"** 버튼을 누르면 봇이 자동으로 채워져
전체 흐름을 바로 볼 수 있다.
