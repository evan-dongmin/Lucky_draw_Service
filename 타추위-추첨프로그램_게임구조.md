# 게임 구조 설명서 (지시용 기준 문서)

> 마지막 갱신: 2026-08-08. **이 문서는 "지금 프로그램이 실제로 어떻게
> 동작하는가"를 한눈에 파악하고, 그걸 기준으로 변경 지시를 내리기 위한
> 문서다.** 각 항목 끝에 `[파일:함수]`로 실제 코드 위치를 적어뒀으니
> "여기 이 숫자를 바꿔줘" 식으로 바로 지시할 수 있다.
>
> 참가자용 쉬운 설명은 `타추위-추첨프로그램_게임소개.md`,
> 개발 이력·다음 할 일은 `타추위-추첨프로그램_진행상황.md` 참고.
>
> **이 문서에는 "예전엔 이랬다"를 쓰지 않는다.** 시도했다가 접은 안과
> 그 사유는 전부 `타추위-추첨프로그램_작업계획서.md` §13 "설계 변경
> 이력"에 있다. 여기는 **지금 어떻게 동작하는가**만 다룬다.

---

## 0. 30초 요약

1. 진행자가 명단을 올리고 **커밋**을 만든다 → 이 순간 레이스 결과(장애물
   충돌까지 포함)가 시드 하나로 완전히 결정론적으로 고정된다. `/verify`
   같은 공개 검증 페이지는 없다(2026-08-08 §12-4로 제거) — 대신
   `app/fairness.py: recompute_from_reveal`/`verify_draw`가 같은 계산이
   항상 재현되는지 내부적으로 보장하고, 이건 pytest로 계속 확인된다.
2. 무대 화면에서 **3라운드 카트 레이스**가 자동으로 진행된다.
   레이스 결과 자체는 1번에서 이미 정해져 있고, 화면은 그걸 보여줄 뿐이다.
   R1·R2는 순위 상위 후보라도 **1등 결승 통과 후 정해진 시간(10초/5초)
   안에 못 들어오면 탈락**한다. 결선은 **1등이 결승선을 넘으면 5초 뒤
   레이스가 끝나고 곧바로 결과 발표**로 넘어간다(§3 참고).
3. 참가자는 폰으로 **매 라운드 "누가 살아남을지" 예측**한다. 잃는 것은
   없고, 순위가 가까울수록 점수가 크다(빗나가도 0점이 아니다).
4. **최종 경품 당첨자는 레이스 1등이 아니라, 예측 리더보드 상위 N명이다.**
   ← 이게 이 프로그램의 핵심 반전 구조다.
5. **폰을 안 들어도 명단에 있으면 자동 참가된다** — 1·2라운드는 자기
   부서가 기본 선택으로 들어가므로 아무것도 안 해도 경품 대상이다.

---

## 1. 두 개의 층 (가장 중요한 개념)

이 프로그램은 **완전히 분리된 두 개의 층**으로 되어 있다. 변경 지시를
내릴 때 "어느 층 얘기인지"만 분명히 하면 사고가 안 난다.

| | **A층: 레이스 (고정·결정론적)** | **B층: 예측 게임 (실시간·변동)** |
|---|---|---|
| 무엇 | 누가 각 라운드를 통과하는가 (장애물 충돌 포함) | 참가자가 그걸 얼마나 잘 맞혔는가 |
| 언제 정해지나 | **커밋하는 순간 전부 확정** | 행사 내내 계속 변함 |
| 정하는 것 | 카트 순위, 라운드 통과자 | **실제 경품 당첨자** |
| 코드 | `app/fairness.py`, `app/race.py` | `app/predictions.py` |
| 조작 가능성 | 불가능(시드 + HMAC 순수 함수) | 해당 없음(참가자 실력/운) |

**절대 규칙**: B층(예측·응원 등 참가자가 하는 어떤 행동도)이
A층 계산에 영향을 주면 안 된다. 영향을 주는 순간 "결과가 커밋 시점에
고정된다"는 이 프로젝트의 핵심 신뢰 장치가 깨진다.
**장애물은 이 규칙의 예외가 아니다** — 장애물이 순위를 바꾸는 건
맞지만(2026-08-08 §12-4), 그 페널티는 **참가자 행동이 아니라 커밋
시점의 시드에서만** 결정론적으로 파생된다(§8 참고). B층(참가자의
예측·응원)이 A층에 영향을 주지 않는다는 원칙은 그대로 유지된다.

---

## 2. 전체 진행 순서 (런북)

진행자가 "레이스 시작"을 누르면 아래 9구간이 **자동으로** 순서대로
흘러간다. 사람이 중간에 개입할 필요가 없다.
`[app/director.py: _BASE_SEGMENTS]`

기준 총 시간 **600초(10분)** 일 때:

| # | 구간 | 시간 | 화면에서 벌어지는 일 |
|---|---|---|---|
| 1 | `opening` | 약 23초 | 오프닝, "출발 준비" 화면, QR 안내 |
| 2 | `r1_lock` | 약 23초 | 1라운드 예측 마감 카운트다운 |
| 3 | `race_r1` | **약 95초** | 🏁 **1라운드 레이스** (전원 → 100명) |
| 4 | `score_r1_select_r2` | 약 68초 | R1 결과 발표 + 점수 지급 + R2 예측 창 |
| 5 | `race_r2` | **약 95초** | 🏁 **2라운드 레이스** (100명 → 결선 진출자) |
| 6 | `score_r2_select_r3` | 약 68초 | R2 결과 발표 + 점수 지급 + R3 예측 창 |
| 7 | `race_r3` | **약 68초** | 🏁 **결선 레이스** (결선 진출자 → 최종 순위) |
| 8 | `final_announce` | 약 80초 | 리빌 + 최종 채점 + 🏆 **경품 당첨자 발표** |
| 9 | `verify` | 약 80초 | 시상 축하 여운 구간(내부 구간명은 `verify`이지만 공개 검증 페이지는 없다 -- §7·§8 참고) |

**시간 비율을 바꾸고 싶으면**: `_BASE_SEGMENTS`의 숫자만 고치면 된다.
합계가 264가 되도록 맞추면 총 시간에 비례해 자동 스케일된다.
선택창(4·6번)은 최소 30초가 보장된다(`MIN_SELECTION_WINDOW_SECONDS`).

---

## 3. 라운드별 상세

### 라운드 1 — 대규모 서바이벌

| 항목 | 값 |
|---|---|
| 출전 카트 | 전원 (최대 250대) |
| 통과 인원 | **상위 100명 후보 중 결승선 컷오프(아래 참고) 통과자만** `[app/fairness.py: R1_PASS_COUNT, R1_CUTOFF_WINDOW_SECONDS]` |
| 예측 대상 | **부서(팀)** — "어느 부서가 통과율이 높을까?" |
| 채점 | 고른 부서의 **통과율 순위별 차등**(§5) `[app/main.py: _ranked_targets]` |
| 미선택 시 | **자기 소속 부서** 자동 선택 `[predictions.py: _default_target]` |
| 트랙 | 넓고 완만한 고속 스피드웨이 `[static/stage.js: TRACK_DEFS[1]]` |
| 카메라 | 광각(250대가 한 화면에) |

### 라운드 2 — 결선 진출전

| 항목 | 값 |
|---|---|
| 출전 카트 | R1 통과자(컷오프 반영, 보통 100명보다 적음) |
| 통과 인원 | **상위 F명 후보 중 결승선 컷오프 통과자만** (F = 5~10명) `[fairness.py: resolve_finalist_count, R2_CUTOFF_WINDOW_SECONDS]` |
| 예측 대상 | **부서(팀)** |
| 채점 | 그 라운드 통과율 순위별 차등(R1과 동일 곡선, 배점 1.5배) |
| 미선택 시 | **자기 소속 부서** 자동 선택 |
| 트랙 | 폭이 절반인 **지그재그 시케인** `[TRACK_DEFS[2]]` |

> **참고**: 결선 진출 후보는 부서와 무관하게 **전체 순위 상위 F명**이다.
> ("부서당 1명씩" 안을 검토했다가 접은 경위는 작업계획서 §13 참고.)

### 결승선 컷오프 — R1/R2 전용 (작업계획서 §12-8, 2026-08-08, 사용자 요청)

R1·R2는 "순위 상위 후보"만으로 통과가 정해지지 않는다. **그 후보군
안에서도, 1등이 결승선을 통과한 시점부터 정해진 시간(R1 10초 / R2
5초) 안에 결승선을 넘지 못하면 탈락한다.** 순위 조건과 시간 조건
**둘 다** 만족해야 통과한다.

- `app/race.py: crossing_ratio`가 후보 카트마다 결승선(통과선)에 처음
  도달하는 시점(progress_ratio)을 구하고, `app/fairness.py:
  _apply_cutoff_window`가 그중 "1등 통과 + 창(window)" 밖으로 밀린
  카트를 걸러낸다. 전부 시드에서 결정론적으로 계산되므로(§4 참고),
  라이브 레이스는 이미 정해진 결과가 흘러가는 것뿐이다 -- 장애물(§8)과
  같은 원칙이다.
- **정원 미달 방지**: 컷오프가 아무리 타이트해도 R2가 열릴 최소 인원
  (`finalist_count`)과 최종 당첨자 수(`draw_count`)는 항상 보장된다 --
  순위 순으로 모자란 만큼 자동으로 채워 넣는다. "레이스가 너무
  극단적으로 갈려서 상품을 못 나눠준다"는 사고는 구조적으로 안 난다.
- **결승선(통과선) 위치는 컷오프 전 후보군 크기로 그린다** -- 시간에
  밀려 탈락한 카트도 화면에서는 결승선까지 도달하는 모습이 그대로
  보인다(그래야 "저 카트, 순위는 됐는데 시간이 모자랐다"는 게 눈으로
  보인다). 무대 화면 상단에 실시간 "N/후보수" 카운터와 카운트다운이
  뜬다(`static/stage.js: updateCutoffPanel`).
### 결선 카운트다운 — R3 전용 (2026-08-08, 사용자 요청)

결선에도 같은 5초 카운트다운이 붙지만 **의미가 다르다**. R1·R2에서는
창이 "누가 통과하는가"를 가르고 레이스는 구간 시간을 다 쓰는 반면,
**결선에서는 창이 닫히는 순간 레이스가 끝나고 곧바로 결과 발표로
넘어간다** `[app/main.py: _run_race_phase, fairness.R3_CUTOFF_WINDOW_SECONDS]`.
이미 승부가 갈린 뒤 뒤처진 카트가 들어오는 것을 계속 보지 않아도 된다.

- **당첨자 자체는 순위로 정해진다.** 결선 결승선은 애초에 "정확히 N대가
  넘도록" 놓이기 때문에 창을 좁힌다고 당첨자가 바뀔 수 없다 -- 경품 수는
  항상 채워져야 하고, 늦게 들어온 카트를 떨어뜨려도 그 자리를 채울 사람은
  더 뒤에 있는 카트뿐이다. 즉 R3의 창은 **"언제 끝낼지"를 정하는 연출
  규칙**이다.
- 무대에는 R1/R2와 같은 패널이 뜨되 문구가 다르다("결과 발표까지").
  창이 닫히면 체커기 배너 + 함성 + 컨페티로 마무리한다.

### 라운드 3 — 결선

| 항목 | 값 |
|---|---|
| 출전 카트 | 결선 진출자 F명 (5~10대) |
| 통과 인원 | **레이스 1위~N위** (N = 경품 개수) |
| 종료 조건 | **1위 결승 통과 + 5초**(아래 "결선 카운트다운") — 구간 시간이 남아도 끝난다 |
| 예측 대상 | **개별 카트** (부서가 아니라 사람) |
| 채점 | 고른 카트의 **결승 등수별 차등**, 배점 **3.0배**(가장 큼) |
| 미선택 시 | **무작위 배정**(결선 진출자 개인이라 "자기 팀"이 없다) |
| 트랙 | 가장 좁고 굴곡 심한 **테크니컬 서킷** `[TRACK_DEFS[3]]` |
| 카메라 | 클로즈업 |

**⚠️ 여기서 나온 레이스 순위는 경품 당첨자가 아니다.** 아래 §6 참고.

---

## 4. 참가자가 폰에서 하는 것

접속 → **게임 소개 확인**(접이식) → 부서 선택 → 이름 선택 → 캐릭터(카트)
선택 → 게임 화면. `[static/mobile.js]`

### 카트 능력 = 예측 점수 특성 `[app/characters.py: ABILITY_EFFECTS]`

카트 선택은 아바타 고르기가 아니라 **실제 점수에 영향을 주는 선택**이다.
단, **레이스 순위에는 전혀 관여하지 않는다** — 능력은 사람이 고르는
값이라 순위에 영향을 주면 "능력 잘 고른 사람이 유리한 추첨"이 되어
공정성이 깨진다. 대신 예측 게임의 점수 규칙만 비튼다.

| 카트 | 특성 | 성향 |
|---|---|---|
| 🔥 니트로 부스트 | 1라운드 예측 점수 +25% | 초반 집중형 |
| 🛡️ 배리어 실드 | 예측이 순위 밖일 때 참여 점수 2배 | 안정 지향 |
| ⚡ 스파크 러시 | 1위 정확 적중 시 +15% | 하이리스크 |
| 🌪️ 슬립스트림 | 고른 대상이 2~3위일 때 +30% | 차점 노림 |
| 💎 럭키 드래프트 | 소수파 보너스 배수 +0.3 | 역배 전문 |
| 🌊 타이달 웨이브 | 우리 팀 순위 보상 +50% | 팀플레이형 |
| ⭐ 스타더스트 | 결승선 통과 보상 2배 | 생존 보상형 |
| 🚀 로켓 대시 | 결선(3라운드) 예측 점수 +20% | 막판 역전형 |

- **미선택자는 중립 1.0배**다. 화면에는 부서 기반 폴백 아이콘이 붙지만
  점수 배수는 없다 — 안 고른 사람이 우연히 유리해지면 취지가 뒤집힌다.
- 배수 폭은 주력 ±30% 이내로 제한한다. 그 이상이면 "무엇을 맞혔는가"보다
  "무엇을 골랐는가"가 중요해져 예측 게임이 아니게 된다. 2배가 붙은
  두 특성은 원래 금액이 작은 항목(참여 보상·통과 보상)에만 적용된다.
- 라운드 사이에 카트를 바꾸면 **그다음 라운드부터** 새 능력이 적용된다
  (이미 채점된 라운드는 불변).

### 실시간 선택 통계 `[predictions.py: live_stats, main.py: _live_stats_full]`

선택 창이 열려 있는 동안 **표가 어디로 몰리는지**가 폰과 무대에 함께
뜬다. 비율만으로는 "3명 중 2명(67%)"과 "200명 중 134명(67%)"이 구분되지
않으므로 인원수까지 낸다.

| 값 | 뜻 |
|---|---|
| `counts` | 후보별 선택 인원 |
| `chosen` / `eligible` | 직접 고른 사람 / 카드가 있는 전체 인원 |
| `minority_bonus` | **그 대상이 1위가 됐을 때** 예측 점수에 곱해질 소수파 배수 |

- 소수파 배수는 `score_round`의 `1 + (1 - share)`와 **같은 식**이다.
  화면에 보여준 숫자와 실제 채점이 어긋나면 안 되므로 테스트로 고정돼
  있다(`test_live_stats_minority_bonus_matches_actual_scoring`).
- 아직 아무도 안 고른 후보도 0명으로 함께 내려보낸다 — 안 그러면 배수가
  가장 큰(2.0배) "진짜 소수파"가 목록에서 사라진다.
- **자동 배정은 집계에서 뺀다.** score_round의 분포 계산과 같은 규칙이라야
  창이 닫히는 순간의 화면 값과 실제 채점 분포가 일치한다.
- **소수파 판정은 서버가 한다** (`MINORITY_SHARE_RATIO = 0.6`): 균등
  배분(1/후보수)의 60% 미만인 후보만 소수파다. 절대 배수로 자르면 후보가
  많을수록 전부 소수파가 된다 -- 후보 8개가 고루 갈리면 배수가 전부
  1.87쯤이라 여덟 팀 모두에 💎가 붙어 표시가 무의미해진다(데모에서 실제로
  그렇게 나온 것을 잡아 고쳤다). 규칙이 한 곳에 있어야 무대와 폰의 표시도
  저절로 일치한다.
- 무대는 표가 가장 몰린 곳을 금색, 소수파를 💎로 표시한다.

### 최종 당첨 결과 `[app/main.py: _my_prize_result, mobile.js: renderPrize]`

당첨자가 확정되면 폰 맨 위에 결과 패널이 뜬다 -- 당첨이면 금색으로
"🏆 당첨되셨습니다! (N위)", 아니면 최종 포인트 순위와 당첨 커트라인을
알려준다. **예측 게임 on/off와 무관하게** 뜬다(꺼진 세션은 레이스 순위가
곧 당첨자다).

WS 이벤트뿐 아니라 **폴링 응답(`/api/predict/me`의 `prize`)에도 같은 값이
실려 온다** -- 발표 순간 화면이 꺼져 있었거나 새로고침한 사람도 결과를
놓치지 않아야 하기 때문이다.

### 고를 때 참고하는 지표 `[app/main.py: _candidate_stats]`

라운드마다 판단 근거가 폰과 무대에 함께 표시된다. 무대에만 있던 정보를
폰에서도 볼 수 있게 해 정보 격차를 없앤 것.

| 라운드 | 폰(후보 버튼) | 무대(중앙 패널) |
|---|---|---|
| R1 | 팀별 참가 카트 수 | 팀별 참가 카트 수 막대 |
| R2 | 생존 카트 수 + 직전 통과율/순위 | 팀별 생존 카트 수 막대 |
| R3 | 직전 등수 + 소속 팀 | 결선 진출자 등수표(상위 3명 강조) |

참여형 게임은 **예측 게임 하나뿐이다** `[app/predictions.py]`. 돈을 걸거나
잃는 요소는 없다.

### 라운드당 1픽 — 손해 없음

- 각 라운드마다 승리 예측 대상을 **하나만** 고른다(부서 R1·R2, 결선
  카트 R3). 확신도·금액을 나눠 거는 단계는 없다 — 라운드마다 "누구를
  고를지"만 정하면 끝이다.
- **틀려도 차감이 전혀 없다.** 순위가 가까울수록 점수가 크고, 순위표
  밖이어도 참여 보상이 들어간다(§5-1)
- 라운드마다 동일한 배점(`ROUND_BASE_POINTS`)을 걸고 겨루고, 라운드
  가중치(R1 1.0 / R2 1.5 / R3 3.0)가 이미 "결선이 가장 크게 흔든다"는
  드라마를 만들어준다 — 개인이 뭔가를 몰아줄 필요가 없다(§5-2)

### 미선택 시 어떻게 되나 ⭐

폰을 안 들었거나 시간 안에 못 고른 사람도 **자동으로 채워진다**
`[predictions.py: _default_target]`:

| 라운드 | 자동 배정값 | 이유 |
|---|---|---|
| R1·R2 | **자기 소속 부서** | 대상이 부서라서 가장 자연스러운 기본값. "자기 팀 응원"이라는 서사와도 맞고, 폰 없이도 납득 가능한 결과가 나온다 |
| R3 | **시드 파생 무작위** | 대상이 결선 진출자 개인이라 "자기 팀"에 해당하는 것이 없다 |

> 소속 부서가 그 라운드 후보에 없으면(부서 병합 등) 무작위로 폴백한다.

### 전원 자동 참가 ⭐

**커밋하는 순간 명단 전원에게 예측 카드가 만들어진다**
`[predictions.py: enroll_all]`. **명단에 있기만 하면 경품 가능성을 일단
확보**한다 -- 폰을 안 든 사람도 리더보드에 올라가 있다.

폰을 들 유인은 그대로 남는다:
- 자기 부서 대신 **실제로 잘할 것 같은 부서**를 고를 수 있다
- R3는 어차피 무작위라, 여기서 직접 고르는 것만으로도 큰 차이가 난다

---

## 5. 점수가 쌓이는 방식 ⭐

참가자 폰에 라운드별로 항목이 쪼개져 표시된다
`[mobile.js: renderRewardBreakdown]`.

### 5-1. 예측 점수 — 순위 차등 (핵심)

고른 대상이 **그 라운드 결과 순위에서 몇 등이었는지**에 따라 배점의
일정 비율을 받는다 `[predictions.py: RANK_RATIOS, rank_ratio]`.

| 내가 고른 대상의 순위 | 지급 비율 |
|---|---|
| 🥇 1위 | **100%** |
| 2위 | 60% |
| 3위 | 35% |
| 4위 | 20% |
| 5위 | 10% |
| 6위 이하 | **5%** (참여 보상) |

점수 = `ROUND_BASE_POINTS(300) × 라운드 가중치 × 순위 비율`
라운드 가중치는 **R1 1.0 / R2 1.5 / R3 3.0** `[ROUND_WEIGHTS]`.

**실제 지급 점수표** (소수파 보너스 제외, 소수점 버림):

| 내가 고른 대상의 순위 | R1 (×1.0) | R2 (×1.5) | R3 (×3.0) |
|---|---|---|---|
| 🥇 1위 | **300** | **450** | **900** |
| 2위 | 180 | 270 | 540 |
| 3위 | 105 | 157 | 315 |
| 4위 | 60 | 90 | 180 |
| 5위 | 30 | 45 | 90 |
| 6위 이하(참여 보상) | 15 | 22 | 45 |

> **소수파 보너스**: 1위를 정확히 맞혔는데 그걸 고른 사람이 적을수록
> 최대 2배까지 곱해진다(즉 R3 1위 단독 적중이면 최대 1,800점). **1위
> 적중에만** 붙는다 — 차등 구간까지 주면 "틀렸는데 아무도 안 골라서
> 더 받는" 모순이 생기기 때문이다.

**설계 의도**: 누구도 0점이 아니되, 1위 적중과 꼴찌 사이에 **20배**
격차가 있어 리더보드 변별력은 유지된다. 첫 라운드에 빗나간 사람이 폰을
내려놓지 않게 하면서도 "잘 맞힌 사람이 이긴다"가 성립하는 지점이다.

> 예측 점수 전체를 비례해서 올리거나 내리려면 `ROUND_BASE_POINTS`
> 상수 하나만 고치면 된다.

### 5-2. 결선(R3) 배점 3배 — 역전 장치

R3는 라운드 가중치가 3.0으로 가장 크다(R1의 3배, R2의 2배). 확신도를
개인이 몰아줄 필요 없이, **배점 자체가 이미 "결선이 가장 크게 흔든다"는
구조**다 — 그래서 앞 두 라운드를 모두 1위로 맞힌 사람도 결선 하나로
뒤집힐 수 있다.

예시(소수파 보너스는 제외한 계산):

| | R1 | R2 | R3 | 합계 |
|---|---|---|---|---|
| **역전형** — 앞 두 라운드 2위(빗나감), R3만 1위 적중 | `300×1.0×0.6` = 180 | `300×1.5×0.6` = 270 | `300×3.0×1.0` = **900** | **1,350점** |
| **선두형** — 앞 두 라운드 1위 적중, R3 2위(빗나감) | `300×1.0×1.0` = 300 | `300×1.5×1.0` = 450 | `300×3.0×0.6` = 540 | **1,290점** |

즉
**막판까지 리더보드가 확정되지 않는다** — 이게 이 게임의 클라이맥스다.
`[tests/test_predictions.py: test_round3_weight_lets_a_late_comeback_beat_an_early_leader]`

### 5-3. 성과 점수 — 팀 대항전의 실체

예측과 **무관하게** 자동으로 쌓인다 `[predictions.py: score_round]`.

| 항목 | 점수 | 조건 | 상수 |
|---|---|---|---|
| 🏁 결승선 통과 | **+20** | 그 라운드를 통과한 개인 전원 | `FINISH_POINTS` |
| 🥇 우리 팀 1위 | **+45** | 그 라운드 통과율 1위 부서 소속 통과자 | `TEAM_RANK_POINTS[0]` |
| 🥈 우리 팀 2위 | **+30** | 2위 부서 소속 통과자 | `TEAM_RANK_POINTS[1]` |
| 🥉 우리 팀 3위 | **+18** | 3위 부서 소속 통과자 | `TEAM_RANK_POINTS[2]` |
| 🏆 결선 당첨 | **+150** | 결선 레이스 최종 당첨자 | `FINAL_WIN_POINTS` |

우리 부서가 그 라운드에 많이 살아남을수록 부서원 전체 점수가 오르고,
그게 곧 경품 당첨 확률로 이어진다.

### 5-4. 한 사람이 한 라운드에 받을 수 있는 점수 (합산 예시)

라운드 점수 = **예측 점수 + 성과 점수**. 폰에는 이 항목들이 쪼개져서
소계·총계와 함께 표시된다 `[mobile.js: renderRewardBreakdown]`.

| 시나리오 | R1 | R2 | R3 | 누적 |
|---|---|---|---|---|
| **최상**(매 라운드 1위 적중 + 통과 + 팀 1위 + 결선 당첨) | 300+20+45 = 365 | 450+20+45 = 515 | 900+20+45+150 = 1,115 | **1,995점** |
| **평범**(자기 부서 자동 선택, 3위권 + R1만 통과) | 105+20 = 125 | 22 | 45 | **192점** |
| **미참여**(폰 안 듦, 전부 자동 배정 + 전부 탈락) | 15 | 22 | 45 | **82점** |

> 최상과 미참여 사이가 약 **24배**다. "참여는 확실히 유리하되, 아무도
> 0점으로 방치되지 않는다"는 설계가 숫자로 그대로 드러난다.

> 4위 이하 부서는 팀 보너스가 없다(개인 통과 점수 +8은 그대로 받는다).
> 등수를 늘리거나 점수를 바꾸려면 `TEAM_RANK_POINTS` 리스트만 고치면 된다.

> 지금 구조에 이르기까지 무엇을 시도했다가 왜 접었는지(베팅 모드 폐지,
> 확신도 배분 폐지, 결선 팀당 1대 폐지 등)는 이 문서가 아니라
> `타추위-추첨프로그램_작업계획서.md` §13 "설계 변경 이력"에 있다.
> 이 문서는 **지금 어떻게 동작하는가**만 다룬다.

---

## 6. 최종 경품 당첨자가 정해지는 방식 ⭐

**가장 중요하고, 가장 오해하기 쉬운 부분.**

```
레이스 결선 1등  ≠  경품 당첨자
```

- **예측 게임이 켜져 있으면**: 최종 리더보드(점수) **상위 N명이 실제
  경품 당첨자**다. `prize_basis="prediction"`
- **꺼져 있으면**: 레이스 결과를 그대로 쓴다. `prize_basis="race"`
- `[app/main.py: _compute_prize_winners]`

즉 **레이스에서 1라운드에 탈락했어도 예측을 잘했으면 당첨될 수 있고,
결선 1등이어도 예측을 못 했으면 당첨에서 빠질 수 있다.**

> **모수는 명단 전체다.** 전원 자동 참가(§4) 덕분에 모바일 미참여자도
> 리더보드에 올라와 있고, 자기 부서 자동 선택으로 점수도 쌓인다. 다만
> 직접 고른 사람이 통계적으로 위에 있게 된다 —
> **참여는 유리하게, 미참여는 불리하되 배제되지 않게** 설계했다.

발표 시점도 일부러 분리해 뒀다:
1. `revealed` — 레이스 결과 공개 (여기선 아직 당첨자 미확정)
2. R3 최종 채점 (리더보드가 여기서 마지막으로 뒤집힌다)
3. `prize_winners` — **경품 당첨자 확정 발표** ← 시상대는 이때 뜬다

시상대에는 당첨자 이름과 함께 **최종 점수**가 표시된다
`[static/stage.js: buildPodium]`.

> 진행자용: **관리자 콘솔에는 "실제 경품 당첨자"가 레이스 결과보다 크게,
> 먼저 표시된다.** 상품을 나눠줄 때는 반드시 그 목록을 봐야 한다.

---

## 7. 화면 구성

### 무대 화면 (`/`) — 프로젝터용

```
┌──────────────────────────────────────────────────────┐
│ 🏁 타추위 GRAND PRIX  [ROUND 2]  구간명      ⏱ 11.0s │ ← 상단 HUD
├────────────┬────────────────────────┬────────────────┤
│ POSITION   │                        │ 부서 통과율    │
│ 1 P0052    │      🏎  레이스        │ 개발팀  ▓▓░ 60%│
│ 2 P0010    │        트랙            │ 영업팀  ▓░░ 30%│
│ 3 P0144    │                        ├────────────────┤
│  ...       │   (장애물·카트·연석)   │ 실시간 선택분포│
│ (실시간)   │                        ├────────────────┤
│            │                        │ 예측           │
│            │                        │ 리더보드       │
├────────────┴────────────────────────┴────────────────┤
│           [팀 레전드: 부서색 + 능력 이모지]           │
│ 🎙 AI MC  "결승선이 눈앞입니다!"                      │ ← 좌우 패널 사이에만
└──────────────────────────────────────────────────────┘
```

- 좌우 패널은 반투명이라 뒤 트랙이 비친다. 화면이 1100px보다 좁으면
  MC 바가 전체 폭을 쓰고, 900px 미만이면 좌우 패널이 사라진다.
- **AI MC**: 상황별 멘트를 자막 + 음성으로. 🎙 버튼으로 끌 수 있다
  (끄면 자막만). 레이스가 끝나면 대기 중이던 멘트는 자동 취소된다.
- **효과음/BGM**: 🔊 버튼으로 on/off. 장면별 BGM 7종이 자동 전환된다 --
  대기(`idle`) · 출발 대기(`standby`) · 선택창(`anticipation`) ·
  **라운드별 레이스 BGM 3종**(`race1`/`race2`/`race3`, 라운드가 올라갈수록
  빠르고 무거워진다) · 결과/시상대(`victory`). (`roulette` 씬은
  `audio.js`에 코드만 남아 있고, 콘솔에서 룰렛 모드를 없앤 뒤로는 어떤
  화면에서도 재생되지 않는다.)
  효과음은 스타트 라이트·추월·**장애물 충돌 6종**(콘/기름·타이어/미끄러짐/
  스핀/충돌/폭발) · **결승선 통과**(선두는 체커기+함성) · 마지막 랩 종 ·
  발표 직전 상승음 · 팡파레/컨페티까지 상황별로 다르다.

### 참가자 화면 (`/mobile`)
내 점수 · **내 카트 현황**(레이스 진행 중 실시간 등수, 통과선 통과
여부 — 진행 중이면 통과선까지 몇 % 왔는지 진행률 바로 표시 —, 소속 팀
실시간 순위, 포인트 순위) · 라운드별 예측 카드(대상 선택, 클릭 한 번으로 확정) ·
**라운드별 점수 내역**(예측 점수 / 통과 / 팀 순위 / 결선 당첨으로
쪼개져 표시) · 리더보드 · 카트 변경 · 응원 이모지 버튼

**"내 카트 현황"은 위치 데이터를 통째로 뿌리지 않는다.** race_tick(무대
전용, 위치 데이터가 커서 모바일 250대에는 안 보냄)의 최신 틱 하나만
서버가 캐시해 두고, 폰이 레이스 진행 중 2초 간격으로 폴링할 때마다 그
사람 한 명분만 계산해서 돌려준다. `[app/main.py: latest_race_tick,
_my_race_status, _my_department_rank]`, `[app/predictions.py:
PredictionEngine.rank_of]`

### 운영 콘솔 (`/admin`)
명단 업로드 → 세션 생성 → 커밋 → 레이스 시작 → 카메라 전환 ·
**실제 경품 당첨자 표시** · 비상 조기 종료. 커밋 해시·시드는 화면에
노출하지 않는다(2026-08-08 §12-4, 사용자 요청).

---

## 8. 레이스 연출

**장애물을 뺀 나머지는 전부 순위에 영향이 없다.** 트랙 모양·카메라·
결승선 그래픽·스타팅 그리드·스타트 라이트는 §1의 A층에서 이미 확정된
결과를 재미있게 보여주기 위한 장치일 뿐이다. **장애물만 예외다** —
2026-08-08 §12-4로 장애물이 순위 결정 자체의 일부가 됐다.

### 장애물 — 이제 실제 판정이다 `[app/race.py: obstacle_layout, lane_for, total_obstacle_penalty]`

8종은 그대로지만, 배치·충돌·페널티가 전부 **커밋 시드에서 결정론적으로
파생**된다(연출 파일 `static/stage.js: OBSTACLE_VISUAL`은 이제 "어떻게
보여줄지"만 담당하고 배치·판정에는 관여하지 않는다):

| 장애물 | 효과 | 페널티(0~1 스케일) |
|---|---|---|
| 🚧 콘 | wobble | 0.006 |
| 🛢️ 기름 | slow | 0.012 |
| 🛞 타이어 | slow | 0.010 |
| 🍌 바나나 | spin | 0.018 |
| 💧 웅덩이 | slide | 0.014 |
| 🪨 바위 | stall | 0.022 |
| 💣 폭탄 | stall | 0.030 |
| 🧊 얼음 | slide | 0.016 |

- **배치**: 라운드마다 정확히 **10개**(`HAZARDS_PER_ROUND`)가 고정 레인
  0~7(`LANE_COUNT=8`) 위에 (시드, 라운드, 순번) 해시로 결정론적으로
  놓인다. 화면 표시 차선 수(5~28, 참가자 수·화면 크기에 따라 변함)와는
  **완전히 다른 축**이다 — 헷갈리지 말 것.
- **결승선 앞쪽에만, 고른 간격으로** `[HAZARD_SPAN, HAZARD_JITTER]`:
  위치를 절대 진행률이 아니라 **결승선까지의 비율**(8%~92%)로 잡고
  결승선 값을 곱한다. 라운드마다 결승선 위치가 달라도 장애물은 항상
  결승선 앞 구간에만 깔린다. 균일 난수 대신 **균등 슬롯 + 슬롯 내
  지터**를 써서 한곳에 뭉치고 다른 구간이 텅 비는 일이 없다.
- **움직인다** `[HAZARD_DRIFT_LANES]`: 좌우로 흔들리며 회전하고 살짝
  떠다닌다. 움직임 파라미터(진폭·주기·위상·회전속도)도 전부 시드 파생
  이라 관전 화면이 몇 대든 똑같이 움직인다. 좌우 흔들림은 **자기 레인
  안(0.38 레인폭)으로 제한**된다 — 옆 레인을 침범하면 "저건 내 레인이
  아닌데 왜 맞았지"로 보이기 때문이다.

> **왜 위치와 페널티를 분리했나**: 순위 계산에 쓰이는 페널티 합은
> **레인 일치로만** 정해져 결승선을 전혀 필요로 하지 않는다. 만약 배치가
> 페널티에 영향을 주면 "순위 → 통과자 수 → 결승선 → 장애물 위치 →
> 페널티 → 순위"로 순환 참조가 생긴다. 그래서 위치 무관 명세
> (`_hazard_specs`: 레인·종류·페널티·배치 비율)와 절대 위치
> (`obstacle_layout`: 결승선을 곱한 값)를 분리했다. 위치가 필요한 건 dip
> 타이밍과 화면 렌더뿐이고, 그쪽은 결승선이 확정된 뒤에만 호출된다
> (R1 결승선 → R1 통과자 → R2 결승선 → …) — 순방향 체인이라 순환이 없다.
- **카트별 레인**도 (시드, 참가자, 라운드) 해시로 고정된다(`lane_for`).
- **판정**: 카트 레인이 장애물 레인과 **일치하면 무조건 맞는다**(트랙
  진행률 도달 여부는 안 본다). 사용자가 "중간 순위뿐 아니라 전체
  카트에 모두 적용되도록"이라고 명시적으로 요청해서, 선두든 꼴찌든
  똑같은 확률로 맞는다 — 도달 여부로 거르면 구조적으로 선두권만
  유리해지기 때문이다.
- **실제 순위 반영**: `app/fairness.py: _obstacle_adjusted_ranking`이
  기본 HMAC 순위(0=1등..1=꼴찌로 정규화)에 그 카트가 3라운드 동안 맞은
  장애물 페널티 합(`total_obstacle_penalty`, 최대 `OBSTACLE_PENALTY_CAP=0.4`)을
  더해 다시 정렬한다. 순환 참조가 없다 — 장애물 배치·판정은 순위와
  무관하게 시드에서만 파생되므로 "기본 순위 → 페널티 → 최종 순위" 한
  번의 패스로 끝난다.
- **실제 위치 반영**: `position_at`에 `seed`를 넘기면(레이스 진행
  중에는 항상 넘긴다) 장애물에 맞는 순간부터 트랙 위치가 **실제로**
  내려간다 — 클라이언트가 흉내 내는 오프셋이 아니라 서버가 계산해
  `race_tick.positions`로 내려주는 진짜 값이다. 다만 맞은 지점부터
  `(1 - at_ratio)` 구간에 걸쳐 선형으로 회복하도록 설계해
  `progress_ratio=1.0`에서는 항상 정확히 목표 위치(=장애물 페널티가
  이미 반영된 최종 순위의 자리)와 일치한다 — 화면과 발표 결과가 어긋날
  수 없다는 원칙은 그대로 유지된다.
- 무대 화면은 서버가 매 틱 함께 보내는 `obstacles`(이 라운드 고정
  배치)와 `effects`(지금 이 순간 누가 맞고 있는지, 세기 포함)를 그대로
  그린다 — 클라이언트가 충돌을 직접 재현하지 않는다.

**검증**: `tests/test_race.py`에 결정론(같은 시드·참가자 → 같은 배치·
페널티)과 "결승선에서는 항상 목표 위치와 정확히 일치"를 확인하는
테스트가 있고, `tests/test_fairness.py`류에는 장애물 페널티가 실제로
순수 HMAC 순위와 다른 결과를 낳는지 확인하는 테스트가 있다.

### 트랙 레이아웃 — 진짜 서킷 `[static/stage.js: TRACK_DEFS]`

2026-08-07에 전면 재설계했다. 예전에는 "x가 왼쪽에서 오른쪽으로 단조
증가하는 리본 + 위아래 물결"이라, 진폭을 아무리 키워도 구불구불한 띠로만
보였다. 지금은 웨이포인트를 **(x, y) 2차원으로 직접 찍는 진짜 서킷**이고,
직선 구간(leg)을 헤어핀으로 이어 붙인다.

- 트랙은 화면이 아니라 **월드**(화면의 1.25~2.0배) 위에 그려지고 카메라가
  따라간다. 덕분에 코스를 크게 잡아도 카트는 화면에서 충분히 커 보인다.
- 세 라운드 모두 **2구간 + 헤어핀 1개**로 통일했다. 라운드별 개성은
  코스 *길이*가 아니라 **단위 거리당 커브 수**로 낸다 — R1은 완만한
  롱 스트레이트, R2는 S자 시케인, R3는 연속 시케인으로 가장 촘촘하다.
- **설계 규칙(어기면 리본이 스스로 뒤집힌다)**: ① 나란한 구간 사이
  간격 > 트랙 폭 + 연석 여유, ② 헤어핀 곡률 반경 > halfWidth(권장 1.5배).
- 노면 밖에 잔디/런오프를 깔아 코스 경계가 분명하다.

> **왜 구간 수를 줄였나**: 처음엔 R2 3구간 / R3 4구간으로 만들었는데,
> 체감 속도 = (호 길이 × 줌 배율) / 라운드 시간이라 호 길이가 길어진
> 만큼 화면 이동이 빨라졌다. 기본 설정(600초)에서 R1 62 / R2 115 /
> R3 234px/s로 벌어져 "눈으로 못 따라간다"는 피드백을 받았다. 지금은
> **62 / 60 / 80px/s**로 정규화돼 있다. 코스를 더 복잡하게 만들고 싶으면
> 구간을 늘리지 말고 구간 안의 커브를 늘려야 한다.

### 카메라 `[static/stage.js: computeCamera]`
- 자동 모드에서 **진행률 10%까지는 월드 전체 조망**, 42%까지 easeInOut
  으로 **선두 추적 줌**으로 전환한다(`cameraChaseFactor`). 사용자 요청:
  "처음엔 전체를 보여주다가 진행하면서 선두에 집중".
- 카메라는 리더의 **실제** 위치를 따라간다 — 장애물 lag·잔물결 같은 연출
  오프셋은 빼고 본다. 예전엔 연출 보정된 위치를 따라가서 리더가 장애물에
  맞을 때마다 화면이 덜컥거렸다. 여기에 지수 평활(`camState`,
  `CAMERA_SMOOTHING`)까지 걸어 충돌과 무관하게 일정하게 따라간다.
- 뒤처진 카트는 화면 밖으로 자연스럽게 빠진다(컬링). 단 **전체 조망
  구간에서는 컬링하지 않는다** — 줌이 들어갈수록 컬링 범위를 좁힌다.

### 결승선 — **이 선을 넘는 것이 통과/승리 기준**
- 라운드·인원수에 따라 원본 통과선 값이 제각각이라(예: 250명 중 100명
  통과면 ~0.68), 화면에는 항상 트랙 끝 가까이 보이도록 워프한다
  (`warpProgress`, `FINISH_VISUAL_FRACTION = 0.965`).
- 워프는 0→0, passLine→0.965, 1→1을 지나는 단조 증가 함수라, **카트가
  통과선을 넘는 시점과 화면에서 체커기 줄을 넘는 시점이 항상 정확히
  일치**한다. 통과 판정 자체는 안 바뀌고 "어디에 그릴지"만 재배치한다.
- 0.92였을 때는 선 뒤로 트랙이 8% 더 이어져 "맵 끝에 도달하는 게 목표"
  처럼 보였다. 0.965로 올려 감속용 런아웃만 짧게 남겼고, 결승선도 넓은
  체커기 밴드 + 금색 판정선 + 게이트 기둥으로 크게 그린다.
- 근접한 순위끼리는 화면에서 앞서거니 뒤서거니 하는 잔물결
  (`jockeyOffsetFor`)이 더해져 있다 — 순수 연출이고 실제 순위(POSITION
  패널·통과 판정)는 전혀 안 바뀐다.

### 스타팅 그리드 `[static/stage.js: drawStartingGrid]`
1라운드 출발 직전 대기 구간(`opening`/`r1_lock`)에 출발선 뒤로 카트들이
3열 스태거드로 정렬한 장면을 보여준다. 첫 `race_tick`이 오면 자동 종료.

### 스타트 라이트 `[static/stage.js: runStartLights]`
실제 그랑프리 절차를 그대로 따른다 — 5열 × 2단 갠트리에 **① 엔진 점화 →
② 빨간 등 5개 순차 점등(등마다 반음씩 올라가는 "빰") → ③ 불규칙한
정적(0.7~1.25초 랜덤) → ④ 일제 소등 = 출발("빠~~")**. ③의 랜덤 대기가
F1 스타트에서 가장 긴장되는 순간이라 의도적으로 고정하지 않았다.

또한 레이스 구간이 시작돼도 **라이트 시퀀스가 끝날 때까지는 카트가
출발선에 정지해 있다** — `_run_race_phase`가 리드인
(`RACE_COUNTDOWN_SECONDS = 5.2초`) 동안 진행률을 0으로 고정한다. 구간
전체 길이는 그대로라 런북 시간 배분에는 영향이 없다.

> ⚠️ **연출 길이(`static/stage.js`의 `LIGHT_*` 상수)와
> `RACE_COUNTDOWN_SECONDS`는 반드시 함께 바꿔야 한다.** 연출이 더 길면
> 라이트가 켜져 있는 동안 카트가 이미 달려나가는 것이 보인다.

### 그 외 연출
- 카트 스프라이트: 부서 색상 탑다운 카트 + 개인이 고른 캐릭터 능력 이모지
- F1 스타트 라이트, FINAL LAP 배너, PHOTO FINISH, 추월 이펙트
- 응원 이모지: 폰에서 누르면 무대 화면 하단에 떠오른다(0.4초 쿨다운)

---

## 9. 자주 바꾸게 되는 값 (지시용 치트시트)

| 바꾸고 싶은 것 | 파일 | 이름 |
|---|---|---|
| 구간별 시간 배분 | `app/director.py` | `_BASE_SEGMENTS` |
| 총 진행 시간 기본값 | `app/director.py` | `DEFAULT_TOTAL_SECONDS` |
| R1 통과 인원 | `app/fairness.py` | `R1_PASS_COUNT` |
| 결선 진출 인원 공식 | `app/fairness.py` | `resolve_finalist_count` |
| **순위별 지급 비율** | `app/predictions.py` | `RANK_RATIOS`, `FLOOR_RATIO` |
| **라운드별 배점 가중치** | `app/predictions.py` | `ROUND_WEIGHTS` |
| **라운드당 고정 배점** | `app/predictions.py` | `ROUND_BASE_POINTS` |
| 통과·팀·결선 성과 점수 | `app/predictions.py` | `FINISH_POINTS`, `TEAM_RANK_POINTS`, `FINAL_WIN_POINTS` |
| 미선택 시 자동 배정 규칙 | `app/predictions.py` | `_default_target` |
| 라운드별 트랙 모양 | `static/stage.js` | `TRACK_DEFS` |
| 장애물 종류·페널티(실제 순위에 반영됨) | `app/race.py` | `OBSTACLE_CATALOG` |
| 장애물 연출(이모지·스핀·좌우 흔들림) | `static/stage.js` | `OBSTACLE_VISUAL` |
| 라운드당 장애물 개수·레인 수 | `app/race.py` | `HAZARDS_PER_ROUND`, `LANE_COUNT` |
| 결승선 컷오프 창 길이(R1 10초/R2 5초) | `app/fairness.py` | `R1_CUTOFF_WINDOW_SECONDS`, `R2_CUTOFF_WINDOW_SECONDS` |
| MC 멘트 | `app/mc.py` | 태그별 멘트 풀 |
| 부서 그룹 수(5~8) | `app/departments.py` | `MIN_GROUPS`, `MAX_GROUPS` |

---

## 10. 실행 방법

```bash
python run.py          # 서버 (http://127.0.0.1:8000)
pytest -q              # 테스트 302개
docker build -t lucky-draw-test .   # 배포 이미지
```

| 주소 | 화면 |
|---|---|
| `/` | 무대(프로젝터) |
| `/admin` | 운영 콘솔 |
| `/mobile` | 참가자 폰 |

무대 화면에서 **"데모로 체험하기"** 버튼을 누르면 봇이 자동으로 채워져
전체 흐름을 바로 볼 수 있다.
