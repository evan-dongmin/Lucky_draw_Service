# 타추위 추첨 프로그램 기획안
### AI 에이전트 기반 경품 추첨 서비스 — 아이디어 우선순위 및 개발 전략

- 작성일: 2026-07-28
- 제출 마감: 2026-08-07(금) — **실작업 가능 기간 약 10일**
- 참고 문서: `타추위-추첨프로그램 설명.txt`

> **[2026-08-08 추기]** 이 문서는 최초 설계 배경·의사결정 기록이라
> 원문을 그대로 남겨둔다. 다만 아래 "커밋-리빌 해시 공개 + `/verify`
> 사후 검증" 부분은 **작업계획서 §12-4(사용자 요청)로 이후 변경됐다**:
> `/verify` 공개 검증 페이지는 제거됐고(커밋 해시도 화면에 노출하지
> 않음), 대신 장애물 충돌까지 포함한 레이스 전체가 시드 하나로 완전히
> 결정론적임을 내부 재계산 테스트(`app/fairness.py:
> recompute_from_reveal`/`verify_draw`, pytest)로 계속 보장하는 방식으로
> 바뀌었다. 현재 상태 기준 문서는
> `타추위-추첨프로그램_게임구조.md`(§0·§7·§8)를 참고할 것.

---

## 1. 요구사항 요약

브리핑에서 도출한 필수 조건을 먼저 정리한다. 아이디어를 고르기 전에 "무엇을 반드시 만족해야 하는가"를 명확히 해두지 않으면, 화려한 연출에 시간을 쓰다가 정작 심사 기준을 놓치기 쉽다.

| 구분 | 요구사항 |
|---|---|
| **진행 시간** | **추첨 전체를 5분 내에 완료 (결정적 제약 — 모든 연출 설계의 기준)** |
| 규모 | 참가자 약 250명. 명단은 **실명 + 부서 + 사번**으로 행사 전 확정 등록 (부서는 팀 편성과 예측 대상의 기준이므로 필수 항목) |
| 운영 기능 | 당첨 인원수 설정, 중복 당첨 허용/제외 토글, 초기화·재추첨 |
| 실행 환경 | PC 또는 대형 스크린에서 원활히 구동 (설치 부담 최소화) |
| **화면 구성(사용자 지정)** | **① 전체 관람용 PC/대형 스크린 화면 ② 참가자 개인 모바일 화면 (QR 접속)** |
| **참여 방식(사용자 지정)** | 참가자가 모바일로 사이버머니를 받아 라운드별 팀/카트에 걸고, 결과에 따라 증감 |
| 안정성 | 행사 현장에서 오류 없이 즉시 활용 가능한 완성도 |
| 공정성 | 추첨 결과를 참가자 스스로 신뢰/검증할 수 있는 구조 |
| 제출물 | 실행 가능한 서비스(배포 주소), 소스 저장소 + 오픈소스 라이선스 명시, 소개 및 사용 방법 문서 |
| 심사 기준 | ① 재미와 긴장감 ② 안정성 및 완성도 ③ 사용 편의성 ④ 공정성 (팀장님들이 심사) |
| 추가 제약(사용자 지정) | **서비스는 AI 에이전트 기반으로 동작해야 함** |

---

## 2. 핵심 설계 원칙 — "재미"와 "공정성"은 충돌하지 않는다

가장 먼저 짚어야 할 설계 이슈가 있다. AI 에이전트들이 레이싱을 하거나 대결을 벌여서 그 결과가 곧 당첨자가 되는 구조를 그대로 만들면, "그 경주 시뮬레이션이 진짜 공정했는지 어떻게 아느냐"는 의심을 피하기 어렵다. 250명이 지켜보는 현장에서 이런 의심은 치명적이다.

그래서 **추첨 로직(공정성 담당)과 연출 로직(재미 담당)을 완전히 분리**하는 것을 전체 아키텍처의 최우선 원칙으로 삼는다.

1. **추첨(Draw) 단계** — 참가자 명단과 당첨 인원수가 확정되면, 검증 가능한 난수로 당첨자를 먼저 결정한다. 카지노/추첨 업계에서 쓰는 **커밋-리빌(commit-reveal)** 방식을 적용한다.
   - 커밋 대상은 시드값만이 아니라 **시드 + 참가자 명단 스냅샷 + 추첨 파라미터(당첨 인원수, 중복 허용 여부)를 합친 값의 해시(SHA-256)**로 한다. 시드만 커밋하면 "추첨 직전에 명단이나 인원수를 바꿔치기했다"는 의심에는 답할 수 없기 때문이다.
   - 추첨 시작 전 이 해시를 화면에 미리 공개하고, 추첨 후 원본(시드·명단·파라미터)을 공개하여 누구나 "해시 재계산 = 화면에 미리 떴던 값"임을 즉석에서 검증할 수 있게 한다.
   - **재추첨·다음 경품 라운드는 각각 새로운 커밋-리빌 사이클**로 진행한다. 라운드마다 "커밋 → 추첨 → 리빌"이 반복되므로, 재추첨 기능이 공정성 구조를 훼손하지 않는다.
   - 이 장치만으로 "공정성" 심사 기준을 정량적·시연 가능한 형태로 만족시킬 수 있다.
2. **연출(Presentation) 단계** — 레이싱/토너먼트/룰렛 등 AI 에이전트 기반 시뮬레이션은 이미 정해진 결과를 향해 "얼마나 극적으로 도달하는가"를 담당한다. 슬롯머신이 임의로 릴을 돌리다가 정해진 자리에 정확히 멈추는 것과 같은 원리다. 이렇게 하면 시뮬레이션은 마음껏 화려하고 예측 불가능하게 만들어도 결과의 공정성과는 무관해진다.

이 원칙 덕분에 "AI 에이전트가 재미를 담당하고, 검증 가능한 난수가 공정성을 담당한다"는 구조가 명확해지고, 뒤에 나오는 모든 아이디어에 동일하게 적용할 수 있다.

---

## 3. 후보 아이디어 비교

5개 후보를 재미·긴장감, 안정성(10일 내 완성 가능성), 사용 편의성, 공정성 구조, AI 에이전트 적합도 5개 축으로 평가했다 (각 5점 만점, 총 25점).

| # | 아이디어 | 재미·긴장감 | 안정성/완성 가능성 | 편의성 | 공정성 구조 | 에이전트 적합도 | 총점 |
|---|---|:-:|:-:|:-:|:-:|:-:|:-:|
| A | **AI 레이싱 추첨** (사용자 제안, 스코프 조정) | 5 | 3 | 4 | 4 | 5 | **21** |
| B | AI MC 해설형 룰렛 추첨 (룰렛으로 확정) | 4 | 5 | 5 | 5 | 3 | **22** |
| C | 토너먼트 서바이벌 대결 | 4 | 4 | 4 | 5 | 4 | **21** |
| D | LLM 실시간 스토리텔링 던전 | 5 | 2 | 3 | 3 | 4 | 17 |
| E | 심플 룰렛/슬롯 (안전망 MVP) | 2 | 5 | 5 | 5 | 2 | 19 |
| F | **참여형 예측 게임 계층** (사용자 제안, A 위에 얹는 가산 계층) | 5 | 3 | 4 | 5 | 4 | **21** |

### A. AI 레이싱 추첨 (사용자 아이디어)
카트 레이싱으로 당첨자를 가린다. 배속(빨리감기), 부스터를 통한 역전 이벤트, 1인칭 시점 등 카메라 전환을 지원하고, 각 카트는 개별 에이전트가 제어한다. **가장 몰입감 있고 차별화된 아이디어이며, "AI 에이전트 기반"이라는 제약을 가장 직관적으로 만족**시킨다. 다만 250대를 한 화면에 동시에 보여주면 누가 누군지 식별이 안 되고 트랙 위가 혼잡해져 오히려 몰입을 해친다는 현실적 문제가 있다. → 4절에서 이를 해소하는 스코프 조정안 제시.

### B. AI MC 해설형 룰렛 추첨
룰렛이라는 검증된 단순 메커니즘에, LLM 기반 "AI 진행자 에이전트"가 참가자 이름/팀을 활용해 실시간으로 해설·드립·긴장감 조성 멘트를 생성한다. (카드 뒤집기도 후보였으나, 룰렛이 "돌아가는 동안"의 지속적 긴장감과 대형 스크린 가시성 면에서 우위라 룰렛으로 확정.) 구현 난이도가 낮고 250명 규모에서도 안정적이며, 시각적 화려함 대신 "말맛"으로 재미를 준다. 리스크가 가장 낮은 선택지.

### C. 토너먼트 서바이벌 대결
250명을 대진표에 배치해 라운드마다 절반씩 탈락(또는 생존)시키는 방식. `log2(250) ≈ 8라운드`로 규모가 자동으로 압축되어 250명 처리에 특히 강하고, 라운드가 진행될수록 인원이 좁혀지며 자연스럽게 긴장감이 고조된다. 대진표 구조 자체가 A안(레이싱)의 "예선 → 결선" 구조로도 재활용 가능.

### D. LLM 실시간 스토리텔링 던전
참가자를 파티원 삼아 AI가 실시간으로 던전 스토리를 생성하고, 이벤트마다 생존자를 가른다. 아이디어 자체는 매력적이나, 라이브 행사 중 LLM 응답 지연·품질 편차에 그대로 노출되어 "안정성" 리스크가 크다. **1순위 후보에서 제외**하고 향후 로드맵으로 이동.

### E. 심플 룰렛/슬롯 (안전망 MVP)
가장 단순한 형태. 그 자체로 제출용 메인 아이디어로 삼기엔 재미 요소가 부족하지만, **다른 모든 아이디어의 기저 안전판(fallback)** 역할을 한다. 개발 극초반에 이것부터 완성해두면, 이후 무엇이 지연되더라도 "행사 당일 실행 가능한 서비스가 없는" 최악의 상황을 원천 차단할 수 있다.

### F. 참여형 예측 게임 계층 (사용자 아이디어)
참가자가 QR로 개인 모바일에 접속해 라운드별로 팀·카트를 예측하고, 적중 시 점수를 얻는다(틀려도 감점 없음). 독립 아이디어가 아니라 **A안(레이싱) 위에 얹는 가산 계층**이라는 점이 중요하다. A안의 구조적 약점(R1 탈락자 200명의 이탈)을 정면으로 해결하고, 250명이 각자 폰으로 참여하는 그림이 "재미·긴장감"과 "AI 에이전트 기반 인터랙티브 서비스" 양쪽에 강하게 작용한다. 리스크는 모바일 클라이언트 추가로 스코프가 늘어난다는 점 — 이는 4절에서 결정론적 로컬 재생, 극단적 UI 단순화, 무손실 채점(순수 함수)으로 완화한다.

---

## 4. 최종 추천 — 단계적 빌드 전략 (Layered Rollout)

표의 점수만 보면 B가 근소하게 1위지만, **단일 아이디어를 택하기보다 "안전판 → 검증된 확장 → 플래그십 기능" 순으로 겹쳐 쌓는 전략**을 추천한다. 10일이라는 마감이 확정된 상황에서, 이 방식은 "무엇을 제출할지 못 정해서 아무것도 없는" 리스크를 없애면서도 사용자님이 구상한 레이싱 아이디어를 실제로 완성해낼 확률을 가장 높인다.

### 1순위(Day 1~3) — 안전망 MVP + 공정성 엔진 구축 [E + 핵심 원칙]
가장 먼저 완성해야 할 것은 화려함이 아니라 **명단 등록 → 당첨 인원 설정 → 커밋-리빌 추첨 → 결과 표시 → 초기화/재추첨**이 되는 최소 기능 제품이다. 여기에 중복 당첨 허용/제외 토글까지 붙이면, 이 시점에 이미 "제출 가능한 서비스"가 확보된다. 이후의 모든 작업은 이 위에 연출을 덧입히는 것뿐이다.

진행 구조는 개발 요건의 Input/Output 정의("약 250명 명단 + 당첨 인원수 → 설정한 인원수의 당첨자")에 정확히 맞춘다. **하나의 추첨 세션 = 하나의 커밋-리빌 사이클 = 설정 인원수만큼의 당첨자 출력**이 기본 단위이며, 연출 계층에서는 이 한 번의 추첨을 **3라운드 팀전 서바이벌 퍼널**(라운드마다 팀 단위로 생존 범위 압축, 4절 참조)로 극화한다. 경품이 여러 개인 행사라면 추첨 세션을 반복 실행하면 되고, 이때 "이미 당첨된 인원 제외" 토글과 세션별 커밋-리빌 사이클이 자연스럽게 맞물린다. 세션 이력·제외 로직·검증 구조는 나중에 덧붙이기 어려우므로 반드시 초기 설계에 포함한다.

### 2순위(Day 3~5) — AI MC 해설 에이전트 추가 [B]
룰렛 연출(확정)에 LLM 기반 진행자 에이전트를 붙인다. 구현 대비 임팩트(ROI)가 가장 높은 단계로, 이 시점에서 이미 "AI 에이전트 기반" 요구조건과 "재미" 요소를 상당 부분 만족하는, 그 자체로 완결된 제출 가능 버전이 나온다. **API 실패 대비 사전 캐시 및 정적 폴백 멘트를 반드시 함께 구현**해 라이브 이벤트 중 LLM 호출 실패가 서비스 중단으로 이어지지 않게 한다.

### 3순위(Day 5~8) — AI 레이싱 모드 [A, 스코프 조정판]
사용자님이 제안한 레이싱 게임을 **플래그십(대표) 연출 모드**로 얹는다. 다만 250명을 그대로 트랙에 세우지 않고, C안의 토너먼트 구조를 빌려 스케일 문제를 해결한다.

- **3라운드 부서 대항 서바이벌 퍼널**: 참가자의 카트를 **실제 소속 부서별로 팀 편성**한다. 부서 대항 구도는 임의의 A~E 팀보다 훨씬 강한 몰입을 만든다 — 자기 카트가 탈락해도 부서를 응원하게 되고, 예측 대상도 "내 부서에 걸까, 잘하는 부서에 걸까"라는 실제 감정이 붙는다.

  | 라운드 | 구성 | 집계 방식 | 생존 |
  |---|---|---|---|
  | R1 | 전원(250대) 출발, 부서별 팀 | **제한 시간(45초) 내 통과한 카트 수 / 부서 인원** = 통과율 → 부서 순위 | 통과 카트 **100대** (개별) |
  | R2 | 생존 100대, 부서별 팀 유지 | 동일 (제한 시간 내 통과율) | 통과 카트 **F대** (개별) |
  | R3 | **부서별 대표 카트 결선** | **결승선 도착 순서** | 상위 **N대** 최종 당첨 |

- **핵심 원칙 — 부서는 순위를 다투지만, 탈락은 개별로 결정된다.** 부서를 통째로 탈락시키면 그 부서에 속한 당첨자까지 탈락해 공정성 구조가 깨진다. 따라서 **생존 여부는 언제나 개별 카트 단위**(HMAC 순위 상위 100대 → F대)이고, 부서 순위는 그 결과를 집계해 보여주는 **표시·예측용 지표**다. 이 분리가 "부서 대항의 재미"와 "당첨자는 반드시 완주"를 동시에 성립시킨다.
- **통과율로 집계하는 이유**: 부서 인원수가 다르므로 **통과 카트 수(절대값)로 순위를 매기면 가장 큰 부서가 거의 항상 이긴다.** 예측이 "제일 큰 부서 찍기"로 수렴해 게임이 죽는다. 그래서 `통과율 = 통과 카트 수 ÷ 부서 인원`으로 집계해 규모와 무관하게 경쟁하도록 한다.
- **부서 그룹 편성**: 예측 대상이 20개면 아무도 못 맞히고 분포도 흩어진다. 실제 부서를 기준으로 **5~8개 그룹**으로 묶고(소규모 부서는 인접 부서와 연합팀 구성), Admin에서 그룹 편성을 확인·조정할 수 있게 한다. 자동 편성 기본값을 제공한다.
- **결선 진출 규칙**: `결선 진출 = 부서별 최상위 1대 ∪ 전체 상위 N대`. 당첨 인원 N=1이면 전체 1위가 이미 자기 부서 대표이므로 **정확히 "부서별 대표 카트만 남는" 구성**이 된다. N≥2일 때는 상위 N이 같은 부서에 몰릴 수 있어 부서 대표만으로는 당첨자가 결선에 못 오르는 모순이 생기므로, 상위 N을 합집합으로 반드시 포함시킨다("A부서는 결선에 3대를 올렸습니다!"라는 서사로 자연스럽게 소화된다). 두 규칙 모두 시드에서 유도되어 사후 검증 가능하다.
- **퍼널과 공정성의 결합 — 중첩 생존 집합**: Fairness Agent는 커밋된 시드 하나로 참가자별 점수(`HMAC(seed, participant_id)`)를 만들어 전체 순위를 정한다. 생존은 이 순위의 상위 절단이므로 `당첨자 N ⊂ 결선 진출 ⊂ R2 통과 F ⊂ R1 통과 100`이 수학적으로 자동 성립한다 — "당첨자가 중간에 탈락하는" 모순이 구조적으로 발생할 수 없다. 리빌 후에는 순위·라운드별 통과자·부서 집계·결선 진출자·당첨자 전부를 재계산으로 검증할 수 있다.
- **고정 시간 레이스 설계**: 레이스는 "결승선에 먼저 닿을 때까지"가 아니라 **처음부터 정해진 상영 시간(R1·R2 45초, R3 70초)에 맞춰 끝나도록 시뮬레이션을 설계**한다. 소요 시간이 가변이면 5분 예산을 지킬 수 없다. R1·R2의 "제한 시간 내 통과"라는 규칙이 이 고정 시간 설계와 자연스럽게 맞물린다.
- **카트 에이전트**: 문자 그대로 OS 프로세스 250개를 띄우는 멀티프로세싱보다, 단일 프로세스 내 경량 시뮬레이션 객체로 "에이전트"를 구현하는 편이 리소스 효율과 안정성 면에서 훨씬 유리하다. 각 카트는 속도 변동성을 갖고 움직이되 **최종 순위는 2절의 원칙대로 사전에 결정된 결과에 수렴**한다.

#### 연출 스코프 축소 — 일정 확보를 위한 의도적 결정

극적 연출은 매력적이지만 일정의 최대 위험 요소이므로, **1차 목표를 "게임이 온전히 돌아가는 것"에 두고 연출은 최소 수준으로 구현한다.** 아래 항목은 제출 스코프에서 제외하고, 시간이 남으면 그때 추가한다.

| 구분 | 1차 구현(필수) | 제외(여유 시 추가) |
|---|---|---|
| 카메라 | **고정 부감 뷰 하나** (전 라운드 동일) | 1인칭 시점, 줌 전환, 클로즈업, 리더 추적 |
| 카트 표현 | 부서 색상 점/막대 + 번호 | 스프라이트 아트, 애니메이션 |
| 수렴 방식 | **목표 순위로의 단순 보간 + 약한 노이즈** | 정교한 러버밴딩 튜닝, 부스터 역전 이벤트 |
| 화면 요소 | 진행 바, 통과선, **부서별 통과율 실시간 랭킹**, 잔여 시간 | 파티클·화면 효과, 배속 UI |
| MC | 구간별 텍스트 자막 | 음성, 타이핑 애니메이션 |

**대신 "게임"에 해당하는 것은 타협하지 않는다** — 라운드 진행 상태 머신, 개별 통과 판정, 부서별 통과율 집계·실시간 랭킹, 예측 선택 창·채점·리더보드, 커밋-리빌 검증. 심사 기준의 "재미·긴장감"은 화려한 그래픽이 아니라 **부서 대항 랭킹이 실시간으로 뒤집히는 것과 내 예측이 맞아가는 과정**에서 나온다고 판단했다.

- **타임박스 원칙**: Day 8까지 안정화되지 않으면 즉시 손절하고 2순위(B) 버전으로 제출한다. 이미 1·2순위가 완결된 제출물이므로, A안은 "되면 대박, 안 되도 무손실"인 상방 베팅으로 설계된다.

### 4순위(Day 8~9) — 참여형 예측 게임 계층 [F, 사용자 아이디어 / 무(無)도박 설계]

레이싱을 "보는 것"에서 "참여하는 것"으로 바꾸는 계층이다. 기존 퍼널의 가장 큰 약점이 **R1에서 탈락한 200명은 남은 3분을 구경만 한다**는 것이었는데, 이 계층이 그 문제를 정면으로 해결한다. 탈락한 참가자도 예측 게임에서는 끝까지 살아 있고, "내 카트는 떨어졌지만 내가 예측한 팀은 이기고 있다"는 이중 서사가 생긴다.

**단, 사내 행사라는 성격을 고려해 도박적 요소(판돈을 잃는 구조)를 완전히 배제한다.** 아래 설계는 "잃지 않지만 긴장되는" 구조이며, 부수적으로 개발 난이도도 낮아진다(잔액 검증·차감·파산 처리가 전부 불필요해지고 채점이 순수 함수가 된다).

#### 이원 화면 구조

| 화면 | 대상 | 내용 |
|---|---|---|
| **Stage (PC/대형 스크린)** | 전체 관람 | 전체 카트 주행(고정 부감 뷰) + **부서별 통과율 실시간 랭킹** + 잔여 시간 + **예측 리더보드 상위 10명** + **실시간 예측 분포**("영업본부 62%") + MC 자막 |
| **Mobile (개인 폰, QR 접속)** | 참가자 개인 | **오직 예측 UI만** — R1·R2·R3 예측 카드(확신도 배분은 미리 수정 가능, 대상 선택은 해당 라운드 창에서만) + 선택 카운트다운 + 내 점수·순위. **주행 화면은 없다** |

**모바일에서 주행 뷰를 완전히 제외한 것은 의도적 결정이다.** ① 250명이 각자 폰에서 레이스를 보게 하려면 결정론적 로컬 재생 같은 복잡한 동기화 장치가 필요했는데, 그것이 통째로 사라져 개발 리스크와 네트워크 부하가 동시에 크게 줄었다. ② 주행은 대형 스크린으로 다 함께 보는 것이 원래 더 좋은 경험이다 — 폰을 보고 있으면 정작 무대를 안 본다. ③ 모바일의 역할이 "선택하고 결과 확인하기"로 명확해져 UI가 단순해진다. 모바일 통신은 선택 요청과 상태 갱신 뿐이므로 트래픽이 극히 적고, 행사장 Wi-Fi 리스크도 사실상 해소된다.

#### 도박 요소 없이 긴장감을 만드는 5가지 장치

도박의 재미는 ① 내가 걸었다는 이해관계 ② 불확실성 ③ 내가 선택했다는 주체성에서 나온다. **"잃는다"는 요소만 제거하면 나머지 셋은 그대로 살릴 수 있다.**

| # | 장치 | 작동 방식 | 효과 |
|---|---|---|---|
| ① | **확신도 배분 (핵심 메커니즘)** | 잃을 돈을 거는 대신, 고정된 **확신도 100**을 R1·R2·R3에 배분한다(예: 20/30/50). 적중하면 배분한 확신도만큼 점수를 얻고, 틀리면 **0점 — 마이너스는 없다** | "올인할까 분산할까"라는 도박과 동일한 의사결정 긴장을 유지하면서 손실이 없다. 승부예측 이벤트와 같은 프레이밍 |
| ② | **라운드 가중치 상승** | R1 ×1.0, R2 ×1.5, R3 ×2.5로 후반 라운드 배점이 커진다 | **누구도 수학적으로 탈락하지 않는다.** R1·R2를 다 틀린 사람도 R3 적중으로 역전 가능 → 끝까지 이탈 동기가 없다. 앞서 도입한 "파산 재지급" 같은 임시 장치가 불필요해진다 |
| ③ | **소수 정답 보너스** | 보너스 배수 = `1 + (1 - 해당 대상 예측 비율)`. 20%만 고른 팀이 이기면 1.8배, 60%가 고른 팀이면 1.4배. **비율은 직접 선택한 참가자 기준**으로 계산해, 화면에 표시된 배수와 실제 지급 배수가 일치하게 한다 | 패리뮤추얼 배당률의 재미(인기 쏠림에 따른 보상 변동)를 도박 없이 그대로 재현한다. "역배 노리기"의 스릴 |
| ④ | **실시간 예측 분포 연출** | Stage 화면에 예측 비율 바가 실시간으로 요동친다. MC가 이를 해설("A팀에 표가 몰립니다! 지금 B팀을 고르면 1.8배입니다") | 선택 창 구간 자체가 볼거리가 된다. 배당률 대신 분포를 보여주므로 도박 프레이밍이 없다 |
| ⑤ | **부서 대항 서사 (구조에 내장)** | 카트 팀 편성 자체가 실제 부서 단위이고, Stage에 **부서별 통과율 랭킹이 실시간으로 뒤집힌다**. 모바일에는 "내 카트 통과!/탈락" 결과 표시 | 점수와 무관한 무료 긴장감. 부서 자존심이 걸리면 자기 카트가 탈락해도 부서를 응원하게 된다. 별도 기능 개발이 거의 필요 없다는 점도 장점 |

추가 후보로 **응원 탭(집단 응원으로 시각 효과 발동)** 도 검토했으나, "응원이 결과를 바꾼다"는 오해가 공정성 인식을 해칠 수 있어 이번 제출에서는 제외한다.

#### 예측 게임 규칙

- **온보딩 (2단계 실명 선택)**: QR 접속 → **① 부서 선택**(리스트) → **② 해당 부서의 이름 목록에서 본인 선택**(실명) → 확신도 100 지급. 250명을 한 목록에 늘어놓으면 찾기 어렵지만, 부서로 먼저 좁히면 목록이 20~50명으로 줄어 몇 초 만에 끝난다. 별도 가입·비밀번호·인증은 없다.
  - **실명 사용이 필수다**: 부서 대항 구도와 카트-참가자 대응이 실명 기준으로 성립하고, 당첨자 발표도 실명으로 이뤄지기 때문에 별칭 체계를 두면 두 개의 신원이 생겨 혼선이 발생한다.
  - **동명이인 처리**: 같은 부서에 동명이인이 있으면 목록에 사번 뒷자리를 함께 표시한다(`홍길동(…123)`).
  - **중복 선택 방지**: 최초 선택 시 기기에 토큰을 발급해 재접속·새로고침 시 본인 상태를 복원한다. 이미 선택된 이름을 다른 기기가 고르면 거부하고("이미 참여 중입니다"), 기기 교체 등 예외는 Admin에서 해제할 수 있게 한다.
  - **명단은 행사 전에 확정한다**: 온보딩이 시작되면 참가자는 확정된 명단에서만 본인을 찾을 수 있고, 커밋 해시도 이 명단 스냅샷을 포함해 이미 공개된 상태다. 따라서 **온보딩 개방 이후 명단을 변경하면 커밋을 재생성하고 다시 공개해야 한다.** Admin에 이 경고를 명시적으로 표시한다.
- **예측 대상과 난이도 곡선**: R1은 **통과율 상위 2개 부서 중 하나**를 맞히면 적중(상대적으로 쉬움 → 초반 성공 경험), R2는 **통과율 1위 부서**(난이도 상승), R3는 **당첨될 카트**(결선 진출 카트 중 N대). 쉬운 성공 → 어려운 도전 순으로 배치해 초반 이탈을 막는다.
- **점수 계산**: `획득 점수 = 배분한 확신도 × 라운드 가중치 × 소수 정답 보너스`. 라운드마다 누적되며 절대 감소하지 않는다.
- **최소 배분 제약**: 각 라운드에 **최소 10 이상** 배분해야 한다. 이것이 없으면 모두 R3에 올인해 R1·R2가 무의미해진다. (가중치·보너스·최소값의 균형 수치는 Day 9 리허설에서 실측 튜닝한다.)
- **보상**: 최종 점수 상위 1~3명에게 소상품(예: 커피 쿠폰)을 주면 몰입도가 크게 올라간다. **경품 추첨과 완전히 분리된 트랙**이며 타추위 협의 항목이다.
- **용어**: "베팅·판돈·배당률·사이버머니"가 아니라 **"예측·확신도·보너스·예측 포인트"**로 통일한다. 같은 메커니즘이라도 프레이밍이 사내 행사 수용성을 좌우한다.

#### 예측 입력 방식 — 확신도는 미리, 대상은 그 라운드에만

여기서 **확신도 배분(얼마나)** 과 **예측 대상(어디에)** 을 의도적으로 분리한다. 배분은 미리 설정 가능하지만, **대상은 해당 라운드의 선택 창에서만 고를 수 있다.**

| 항목 | 사전 설정 | 수정 가능 시점 |
|---|:-:|---|
| **확신도 배분** (R1/R2/R3, 예: 20/30/50) | **가능** | 잠기지 않은 라운드끼리 언제든 재배분 (합계 100, 각 ≥ 10) |
| **예측 대상** (팀 또는 카트) | **불가** | 해당 라운드의 선택 창이 열린 동안만. 이전 라운드에 미리 지정 불가 |

- **왜 대상을 미리 못 고르게 하는가**: 시작할 때 규칙 하나로 3라운드를 다 걸어두게 하면 이후 라운드에 신경을 쓰지 않게 된다. "설정하고 잊는" 경로를 없애야 매 라운드 폰을 볼 이유가 생긴다. 또한 R2 팀은 R1 종료 후 재편성되고 R3 결선 카트는 R2가 끝나야 정해지므로, 미리 고르는 것은 애초에 정보 없는 선택이기도 하다.
- **선택 시간 내 미선택 시 무작위 배정**: 0점 처리가 아니라 **무작위로 대상이 배정**된다. 미참여자를 게임에서 완전히 밀어내지 않으면서("어? 랜덤으로 걸렸는데 맞았네"), 동시에 "직접 고르지 않으면 운에 맡겨진다"는 압박이 다음 라운드 참여를 유도한다.
- **무작위 배정도 검증 가능하게 한다**: `HMAC(시드, "autopick:{참가자}:{라운드}")`로 유도한다. 커밋된 시드에서 파생되고 공식이 공개되므로, 사후에 "임의로 배정했다"는 의심 없이 재계산 검증이 가능하다.
- **긴박감 연출**: Stage와 모바일 양쪽에 선택 창 카운트다운을 크게 띄우고, Stage에는 미선택 인원을 실시간 표시한다("아직 87명이 선택하지 않았습니다!"). MC가 이를 해설하면 선택 창 구간 자체가 볼거리가 된다.
- 잠금은 **서버 시각 기준**이며, 잠긴 뒤 도착한 요청은 거부된다. 잠금 전 수정은 마지막 값이 유효하다(last-write-wins).
- **선택 창은 최소 30초를 확보한다.** 자동 규칙이라는 도피처가 없어졌으므로, 창이 짧으면 대다수가 무작위 배정으로 밀려 이 계층의 목적이 무너진다. 런북에서 R2·R3 선택 창에 각 30초를 배정했다.

#### 반드시 지켜야 할 3가지 제약

1. **예측 게임은 경품 추첨에 어떤 영향도 주지 않는다.** 점수를 많이 벌어도 당첨 확률은 변하지 않고, 예측에 참여하지 않아도 **당첨 후보에서 절대 배제되지 않는다.** 폰이 없거나 접속하지 않은 참가자도 250명 명단에 있으면 동일한 당첨 확률을 갖는다. 이 원칙이 깨지면 "폰 없는 사람은 경품에서 손해"라는 진짜 공정성 문제가 발생한다.
2. **모바일에는 주행 데이터를 전송하지 않는다.** 모바일이 예측 UI 전용이므로 통신은 ① 선택 요청 ② 선택 창 개폐·카운트다운 ③ 라운드 결과·점수·순위 갱신뿐이다. 라운드당 수 KB 수준이며, 이는 250대 동시 접속에서도 부담이 없다. (주행 화면을 모바일에 넣었다면 필요했던 결정론적 로컬 재생·프레임 동기 장치는 전부 불필요해졌다.)
3. **모바일 미접속·이탈이 행사 진행을 막지 않는다.** 예측 참여자가 0명이어도 레이스와 추첨은 정상 진행된다. 모바일 계층은 완전히 가산적(additive)이며, 장애 시 Stage 화면만으로 행사가 완주된다.

#### 온보딩은 5분 예산 밖에서 처리한다

250명이 QR을 찍고 본인을 선택하는 데는 현실적으로 2~3분이 걸린다. 이를 5분 안에 넣으면 추첨할 시간이 없다. 따라서 **QR과 접속 현황(“현재 187명 참여 중”)을 타운홀 시작 시점부터 화면 하단에 상시 노출**해 이전 안건이 진행되는 동안 온보딩을 끝낸다. 5분 런북은 "이미 접속이 끝난 상태"에서 시작한다.

### 5분 진행 런북 (Runbook)

5분을 초 단위로 역산해 설계한 기준 타임라인. 3라운드 팀전 퍼널(250 → 100 → F → N) + 예측 게임 기준이며, Admin에서 당첨 인원수를 바꾸면 Director Agent가 팀 구성과 시간 예산을 자동 재배분한다. **베팅 창은 직전 라운드의 정산 구간과 겹쳐서 열어** 별도 시간을 쓰지 않는 것이 핵심이다.

| 구간 | 시간 | 내용 |
|---|---|---|
| 오프닝 | 0:00 ~ 0:10 | MC 자막 인사 + 부서 그룹 소개 및 스타트 정렬 · *R1 선택 창 개방(온보딩 중 이미 열려 있음)* |
| R1 선택 잠금 | 0:10 ~ 0:20 | 10초 카운트다운, **예측 분포 바 요동 + 미선택 인원 카운터** |
| **R1 레이스** (250 → 100) | 0:20 ~ 1:05 | 제한 시간 45초. 통과선을 넘는 카트가 누적되며 **부서별 통과율 랭킹이 실시간으로 뒤집힘** |
| R1 채점 + **R2 선택 창** | 1:05 ~ 1:35 | 부서 통과율 최종 순위 + 점수 획득·리더보드 갱신(10초) → **R2 대상 선택 30초 창** |
| **R2 레이스** (100 → F) | 1:35 ~ 2:20 | 제한 시간 45초, 동일 구조 (인원이 줄어 개별 카트가 식별됨) |
| R2 채점 + **R3 선택 창** | 2:20 ~ 2:50 | 부서 순위 + **결선 진출 카트(부서별 대표) 발표** → **R3 대상 선택 30초 창**(가중치 ×2.5 강조, 마지막 역전 기회) |
| **R3 결선** (부서 대표 → N) | 2:50 ~ 4:00 | 결선 70초. **결승선 도착 순서로 순위 확정**, 상위 N대 당첨 |
| 최종 발표 | 4:00 ~ 4:30 | **당첨자 N명 발표** + 예측 게임 최종 리더보드 1~3위 발표 |
| 검증·마무리 | 4:30 ~ 5:00 | **시드 리빌 + 상단 배지의 해시와 일치 확인**, 검증 페이지 QR 표시 (각자 폰으로 순위·부서 집계·무작위 배정·자기 채점 내역까지 사후 검증) |

**커밋 해시는 5분 예산 밖에서 공개한다.** 커밋-리빌의 효력은 "결과가 정해지기 전에 공개적으로 봉인했다"는 사실에서 나오므로 **사전 공개는 필수**지만(없으면 사후 리빌이 아무것도 증명하지 못한다), 그 공개가 5분 안에 있어야 할 이유는 없다. 명단 업로드 직후 커밋을 생성해 **온보딩 화면(타운홀 시작부터 QR과 함께 상시 노출)에 전체 해시를 크게 띄운다** — 20초가 아니라 수 분간 공개되므로 증거로서 오히려 강하다. 5분 구간에는 **화면 상단에 해시 앞 8자리 배지만 상시 표시**해 연속성을 유지하고, 마무리에서 리빌 값과 이 배지를 대조한다. 이렇게 확보한 10초는 최종 발표와 검증 구간에 배분했다(각 25→30초).

핵심 설계 포인트: 각 구간은 고정 시간 연출이므로 전체가 정확히 5:00에 끝난다(10+10+45+30+45+30+70+30+30 = 300초). **선택 창은 채점·발표 구간과 겹쳐 배치**해 별도 시간을 쓰지 않으면서도 각 30초를 확보했다 — 대상을 미리 고를 수 없게 한 만큼 창이 짧으면 다수가 무작위 배정으로 밀리기 때문에, 이 30초는 타협 대상이 아니다. 레이스 시간(45/45/70초)을 조금씩 줄여 확보했다. 상세 검증(명단 스냅샷·시드·팀 편성·라운드별 생존 팀 재계산)은 QR 페이지에 위임해 공정성과 시간 예산을 모두 지킨다. 돌발 지연은 Director Agent가 이후 구간을 비례 축소해 흡수한다. 경품이 여러 개라 세션을 반복해야 하는 행사라면 축약 런북(2라운드 퍼널, 예측 1회)을 적용한다.

### 보류 (향후 로드맵) — LLM 스토리텔링 던전 [D]
이번 제출에는 포함하지 않는다. 실시간 생성형 콘텐츠는 라이브 행사에서 검증 없이 쓰기엔 리스크가 크다. 차기 버전 아이디어로 문서에만 남겨둔다.

---

## 5. AI 멀티에이전트 아키텍처

"AI 에이전트 기반으로 동작"이라는 제약을 전체 시스템의 뼈대로 반영한 설계다. 5개 에이전트가 역할을 분담한다.

| 에이전트 | 역할 | 비고 |
|---|---|---|
| **Fairness Agent (추첨 에이전트)** | 커밋-리빌 방식으로 실제 당첨자를 결정하는 유일한 주체. `secrets`/`hashlib` 기반 시드 생성, 해시 사전 공개, 사후 시드 공개 및 검증 API 제공. **커밋된 시드 하나에서 최종 당첨자 N명과 각 라운드 통과자 명단까지 결정론적으로 유도** | 시스템에서 가장 중요하고 가장 단순/견고하게 만들어야 하는 부분 |
| **Racer Agents (연출 에이전트, 참가자 수만큼)** | 카트별 속도 변동 시뮬레이션. Fairness Agent의 결과(목표 순위)에 수렴하도록 보간됨 | 경량 시뮬레이션 객체로 구현, OS 멀티프로세스 남용 지양. 1차 구현은 단순 보간 + 약한 노이즈 |
| **Director Agent (연출 감독 에이전트)** | **5분 시간 예산의 총괄 관리자** — 라운드별 시간 배분, 선택 창 30초 하한 보장, 지연 시 이후 구간 비례 축소, 퍼널 인원(100→F) 및 부서 그룹 편성 산출 | 규칙 기반. 5분 제약의 준수를 책임지는 핵심 에이전트 |
| **MC Agent (해설 에이전트)** | 참가자 이름/팀 정보를 활용해 실시간 해설·멘트 생성 (LLM 기반) | 행사 전 사전 생성·캐싱 + 정적 폴백 필수 (안정성 확보). **실명·사번·부서명은 외부 API로 보내지 않는다** — 멘트는 익명 슬롯 템플릿("{부서}의 {번호}번 카트가…")으로 생성하고 실제 값은 로컬에서 치환한다 |
| **Admin/Control Agent (운영 제어 에이전트)** | 명단 업로드(CSV/Excel), 경품 라운드 목록 등록·순차 진행, 당첨 인원 설정, 중복 허용/제외 토글, 초기화·재추첨 담당 | 사회자가 직접 조작하는 운영 콘솔의 백엔드 |
| **Prediction Agent (예측 게임 에이전트)** | 참가자별 예측 카드(확신도 배분·대상·자동 규칙) 보관, 라운드별 잠금 관리, 예측 분포 실시간 집계, 소수 정답 보너스 산출, 채점·리더보드 갱신 | 경품 추첨과 완전히 분리된 트랙. **채점이 저장된 예측의 순수 함수**이므로 언제든 재계산 가능(장애 복구 용이). 이 에이전트가 죽어도 추첨·레이스는 정상 진행 (가산적 계층) |

---

## 6. 브리핑 필수 기능 매핑

| 브리핑 요구사항 | 대응 기능 |
|---|---|
| 참가자 명단 간편 등록 | CSV/Excel 업로드 + 붙여넣기 지원. **실명·부서·사번 필수 컬럼**, 부서 누락 시 경고 (Admin Agent) |
| 250명 규모 안정 처리 | 3라운드 팀전 퍼널로 화면 인원 압축, 경량 비동기 에이전트 시뮬레이션, 모바일은 결정론적 로컬 재생 |
| 중복 당첨 설정 | 라운드별 "이미 당첨자 제외" 토글, 세션 내 당첨 이력 추적 |
| Output: 설정한 인원수의 당첨자 | 3라운드 퍼널의 결선 도착 상위 인원 = 설정 인원수 N (중첩 생존 집합으로 구조적 보장) |
| 개인 모바일 + 전체 PC 화면 (사용자 지정) | Stage 화면(주행·부서 통과율 랭킹·리더보드) + Mobile 화면(예측 UI 전용), QR 온보딩 |
| 참여형 몰입 (재미·긴장감 강화) | 확신도 배분 예측 게임, 라운드 가중치 상승(역전 가능), 소수 정답 보너스, 실시간 예측 분포, **부서 대항 통과율 랭킹** |
| 도박 요소 배제 (사내 행사 적합성) | 손실 없는 확신도 배분 구조 — 틀리면 0점, 마이너스 없음. 용어도 "예측·확신도"로 통일 |
| 여러 경품 순차 추첨 (묵시적 요구) | 추첨 세션 반복 실행 + "이미 당첨자 제외" 토글, 세션별 커밋-리빌 사이클 |
| **5분 내 진행 완료** | 고정 시간 레이스 + Director Agent의 라운드별 시간 예산 자동 배분/단축 (5분 런북) |
| 초기화·재추첨 | Admin Agent에 리셋/재추첨 버튼, 직전 라운드 상태 롤백 |
| PC/대형 스크린 구동 | 웹 기반 서비스로 구현 → 브라우저만 있으면 설치 없이 실행 |
| 공정성 검증 구조 | 커밋-리빌 해시 공개/검증 (2절 핵심 원칙) |
| 긴장감 있는 연출 | 레이싱/토너먼트 연출 + AI MC 해설 + 배속/부스터/카메라 전환 |

### 심사 관점 보완 — "행사장"이 아니라 "심사석"에서 첫인상이 결정된다

심사는 행사 당일이 아니라 **팀장님들이 제출물을 직접 열어보는 사전 평가**로 이뤄진다. 즉 첫 사용자는 250명의 참가자가 아니라, 명단 파일도 없이 배포 주소를 클릭해보는 심사자다. 이를 위해 다음을 제출물에 포함한다.

- **원클릭 데모 모드**: 첫 화면에서 "데모로 체험하기" 버튼 하나로 가상 참가자 250명(무작위 생성 이름/팀)이 자동 로드되어, 심사자가 아무 준비 없이 30초 안에 전체 흐름(명단 로드 → 당첨 인원 설정 → 3라운드 팀전 퍼널 연출 → 검증)을 경험할 수 있게 한다.
- **예측 게임의 시뮬레이션 참여자**: 심사자는 혼자 접속하므로 예측 게임의 재미(분포 변동, 리더보드 경쟁)를 체감할 수 없다. 데모 모드에서는 **가상 참가자 249명이 자동으로 예측하는 봇**을 돌려, 심사자가 자기 폰(또는 두 번째 브라우저 창)으로 들어와도 실시간 예측 분포 변동과 리더보드 경쟁을 그대로 체험하게 한다. 이 봇은 행사 당일에는 비활성화한다.
- **공정성 검증 시연 동선**: 데모 흐름 안에 커밋-리빌 검증 화면을 자연스럽게 배치해, "공정성" 심사 항목이 눈으로 확인되게 한다.
- **60초 안에 읽히는 README**: 스크린샷/GIF 중심의 사용 방법 문서로, 실행해보지 않는 심사자에게도 서비스가 전달되게 한다.

---

## 7. 기술 스택 제안

기존 저장소의 `.gitignore`가 Python 기반임을 감안해 아래와 같이 제안한다.

- **백엔드**: Python + FastAPI — REST API + WebSocket(실시간 화면 갱신용). Fairness/Director/MC/Admin 에이전트 로직을 여기서 관리.
- **프런트엔드**: 웹 기반 (설치 불필요, 대형 스크린 대응 용이). 레이싱 연출은 HTML5 Canvas 2D — 풀 3D 엔진은 시간 대비 리스크가 커서 지양. **Stage(대형 스크린) / Mobile(개인 폰) / Admin(운영 콘솔) / Verify(검증) 4개 화면**을 동일 코드베이스에서 서빙하고, 렌더러를 공유해 모바일용 별도 엔진을 만들지 않는다.
- **데이터 처리**: 표준 `csv` + `openpyxl`(xlsx 선택)로 참가자 명단 파싱. pandas는 의존성 대비 이득이 없어 사용하지 않는다.
- **공정성**: Python 표준 라이브러리 `secrets`, `hashlib` (외부 의존성 없이 검증 가능하게).
- **AI/LLM**: MC 에이전트용으로 LLM API 연동 (예: Anthropic Claude API). 행사 중 API 장애 대비 사전 생성 캐시 + 정적 폴백 필수.
- **배포**: 웹 서비스로 배포(URL 제출용)하되, **행사장 네트워크 불안정 리스크**를 감안해 로컬/사내망에서도 구동 가능한 형태(예: 로컬 서버 + 브라우저 접속)를 반드시 백업 옵션으로 준비.
- **오픈소스 라이선스 관리**: 사용하는 모든 라이브러리의 라이선스를 `THIRD_PARTY_NOTICES.md` 등으로 정리 — 제출 요건에 명시되어 있으므로 개발 초기부터 사용 라이브러리 목록을 누적 기록.

---

## 8. 10일 개발 일정 (7/28 ~ 8/7)

| 기간 | 목표 |
|---|---|
| Day 1~2 (7/28~7/29) | 아키텍처 확정, Fairness Agent(커밋-리빌 추첨 로직) 구현 및 검증 |
| Day 3 (7/30) | 안전망 MVP 완성 — 명단 업로드, 당첨 인원수 설정, 추첨 세션 반복 실행, 중복 옵션, 초기화/재추첨 → **이 시점부터 "제출 가능"** |
| Day 4~5 (7/31~8/1) | MC Agent(AI 해설) 연동, 폴백/캐싱 처리 → 2순위 버전 완성 |
| Day 6~7 (8/2~8/3) | 레이싱 모드 핵심 구현 — 3라운드 부서 대항 퍼널(고정 시간·제한 시간 통과 판정), 부서별 통과율 집계, 단순 보간 수렴, 고정 부감 뷰 |
| Day 8 (8/4) | Director Agent 시간 예산 배분·선택 창 하한 보장, 부서 그룹 자동 편성, Stage 랭킹 UI |
| Day 8~9 (8/4~8/5) | **참여형 예측 게임 계층** — QR 온보딩, 예측 카드(선택 창 상태 머신), 확신도 채점, 예측 분포·리더보드 |
| Day 9 (8/5) | 250명 규모 테스트(R1 250대 렌더링 + **모바일 250 동시 접속**), **5분 런북 전체 리허설(정확히 5:00 내 완주)**, 선택 입력률 실측, 오류 복구 시나리오, 대형 스크린 실측 |
| Day 10 (8/6) | 버그 픽스, 배포, 원클릭 데모 모드/샘플 데이터, 사용 설명서/README(스크린샷·GIF), 라이선스 정리 |
| 8/7 | 제출 |

Day 8까지 레이싱 모드가 불안정하면 즉시 2순위 버전으로 되돌려 제출 — 이미 앞선 단계가 독립적으로 완결된 제출물이므로 손실이 없다. 예측 게임 계층은 그보다 한 단계 더 바깥의 가산적 계층이므로, 미완성 시 이 계층만 비활성화하면 레이싱 버전이 그대로 제출된다.

**일정 압박에 대한 솔직한 평가**: 세 차례의 설계 단순화로 일정 여유가 실질적으로 생겼다 — ① 도박 요소 제거(잔액 차감·파산 처리·원자적 트랜잭션 소멸, 채점이 순수 함수화), ② **모바일 주행 뷰 제외**(결정론적 로컬 재생·프레임 동기 장치가 통째로 불필요), ③ **연출 스코프 축소**(카메라 전환·부스터·정교한 러버밴딩 튜닝 제외). 특히 ②와 ③은 이 프로젝트에서 난이도가 가장 높았던 두 작업을 제거한 것이다. 상세 재추정은 작업계획서 §6.1 참조.

---

## 9. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 250명 동시 시각화 시 가독성 저하 | R1은 개별 식별이 목적이 아니라 **부서별 통과율 랭킹**을 보는 구간(부서 색상 점으로 충분), 라운드가 진행되며 인원이 줄어 개별 식별이 가능해짐 |
| 1라운드 250개 카트 렌더링 성능 저하 | 2D 캔버스 + 점/막대 렌더링으로 부하 최소화(스프라이트 아트 미사용), Day 9에 250명 프레임 성능 실측 |
| **부서 인원 차이로 큰 부서가 항상 승리** | 통과 카트 **수**가 아니라 **통과율**(통과 수 ÷ 부서 인원)로 집계 |
| 부서 수가 너무 많아 예측이 무의미 | 실제 부서를 **5~8개 그룹**으로 묶어 편성(소규모 부서는 연합팀), Admin에서 조정 가능 |
| 부서 통째 탈락 시 당첨자까지 탈락 | 생존은 **개별 카트 단위**로만 결정, 부서 순위는 집계 표시·예측용 지표로 분리 |
| 명의 도용·중복 선택 (인증 없는 온보딩) | 최초 선택 시 기기 토큰 발급, 선택된 이름의 재선택 거부, Admin 해제 기능. 예측 게임은 당첨과 무관하므로 도용 유인이 낮음 |
| 동명이인 혼선 | 같은 부서 내 동명이인은 목록에 사번 뒷자리 병기 |
| 온보딩 개방 후 명단 변경 | 커밋 스냅샷이 무효화되므로 **커밋 재생성·재공개**가 필요. Admin에 경고 표시 및 재커밋 버튼 제공 |
| 행사장 Wi-Fi/네트워크 불안정 | 로컬 서버 구동 옵션 병행 준비, 클라우드 배포에만 의존하지 않음 |
| LLM API 지연/실패로 라이브 중 서비스 중단 | MC 멘트 사전 생성·캐싱 + 정적 폴백 텍스트 상시 준비 |
| 레이싱 모드가 기한 내 미완성 | 2순위(AI MC 룰렛) 버전을 항상 제출 가능한 상태로 유지 (타임박스 원칙) |
| "AI가 결과를 조작했다"는 불신 | 커밋-리빌 해시 공개/검증 절차를 화면에서 직접 시연 (커밋 대상에 명단·파라미터 포함) |
| 현장 진행자의 조작 실수 | 초기화/재추첨/되돌리기를 Admin 화면에 명시적 버튼으로 노출, 확인 절차 추가 |
| **5분 제한 초과 (최상위 리스크)** | 고정 시간 레이스로 소요 시간을 확정적으로 설계, Director Agent가 남은 예산 기준 자동 단축, 상세 검증은 QR 페이지로 오프로드, Day 9에 5:00 내 완주 리허설로 검증 |
| 돌발 상황(사회자 멘트, 발표 지연)으로 구간 지연 | Director Agent가 다음 구간 시간을 자동 축소해 흡수, 비상용 수동 배속/스킵 버튼 유지 |
| 임직원 실명·사번의 외부 LLM API 전송 | MC 멘트는 익명 템플릿으로 생성 후 로컬 치환 — 개인정보는 서비스 외부로 나가지 않음 |
| 250대 모바일 동시 접속으로 네트워크 마비 | **모바일은 예측 UI 전용**(주행 데이터 미전송) — 통신은 선택 요청·창 개폐·결과 갱신뿐으로 라운드당 수 KB. 주행 뷰를 뺀 것이 이 리스크를 근본적으로 해소 |
| **모바일 온보딩이 5분을 잡아먹음** | QR·접속 현황을 타운홀 시작부터 상시 노출해 이전 안건 진행 중 온보딩 완료. 5분 런북은 접속 완료 상태에서 시작 |
| 초반 실패 후 참가자의 이탈 | 라운드 가중치 상승(×1/×1.5/×2.5)으로 R1·R2를 다 틀려도 R3 역전 가능 — **수학적으로 누구도 탈락하지 않음** |
| 폰을 보지 않아 예측 미입력 | 시간 내 미선택 시 **무작위 배정**(0점 처리 안 함) → 전원이 모든 라운드에 예측 보유. 무작위 배정은 시드 파생으로 검증 가능 |
| 선택 창이 짧아 다수가 무작위로 밀림 | R2·R3 선택 창을 각 **30초**로 확보(채점 구간과 겹쳐 배치), Stage에 미선택 인원 카운터 표시로 참여 독려. Day 9 리허설에서 실제 입력률 측정 |
| **"회사 행사에서 도박" 프레이밍 우려** | **손실 구조 자체를 제거** — 확신도 배분 방식으로 틀려도 0점, 마이너스 없음. 용어를 "예측·확신도·보너스"로 통일(베팅·판돈·배당률·사이버머니 표현 사용 금지). 상위 입상 소상품 지급 여부만 **타추위 사전 협의 필요** |
| 모두 R3에 확신도를 몰아 R1·R2가 무의미해짐 | 라운드별 **최소 확신도 10** 제약 + Day 9 리허설에서 가중치·보너스 균형 실측 튜닝 |
| 예측 게임 미참여자가 경품에서 불이익 | 예측 게임 참여는 당첨 확률과 무관 — 명단에 있으면 폰 없이도 동일 확률. 검증 페이지에서 확인 가능 |
| 예측 채점 오류·분쟁 | 채점이 저장된 예측의 순수 함수이므로 언제든 재계산 검증 가능. 검증 페이지에서 참가자가 자기 채점 내역을 직접 확인 |
| 모바일 계층 미완성/장애 | 완전 가산적 계층으로 설계 — Stage 화면만으로 행사 완주 가능, 게이트 D에서 손절 판단 |

---

## 10. 제출물 체크리스트

- [x] 실행 가능한 서비스 또는 배포 주소 — https://taachuwi-grandprix.onrender.com/
- [ ] 소스 코드 저장소 주소 (현 GitHub 저장소)
- [ ] 사용한 오픈소스 목록 및 라이선스 정리 문서
- [ ] 서비스 소개 및 사용 방법 문서 (README 갱신, 스크린샷/GIF 포함)
- [ ] 원클릭 데모 모드 + 가상 참가자 250명 샘플 데이터 (심사자용)
- [ ] 250명 규모 부하 테스트 결과
- [ ] 5분 런북 리허설 통과 (3라운드 팀전 퍼널 + 예측 게임 정확히 5:00 내 완주)
- [ ] 팀 편성·라운드별 생존 팀까지 포함한 커밋-리빌 검증 페이지 동작 확인
- [ ] 공정성 검증 절차 시연 가능 여부 확인
- [ ] 모바일 250대 동시 접속 테스트 결과 (예측 채점 정확성 포함)
- [ ] 예측 게임 비활성화 상태에서도 정상 완주 확인 (가산적 계층 검증)
- [ ] 예측 카드 동작 확인 (확신도 사전 재배분 / 대상은 해당 라운드 창에서만 / 잠금 후 거부 / 미선택 시 무작위 배정)

---

## 결론

**우선순위: (1) 안전망 MVP + 공정성 엔진 → (2) AI MC 해설 룰렛 → (3) AI 레이싱 모드(사용자 아이디어) → (4) 참여형 예측 게임 계층(사용자 아이디어) → (보류) LLM 스토리텔링 던전.**

**"추첨 전체 5분"이라는 진행 시간 제약이 연출 설계의 최상위 기준이다.** 레이싱은 **3라운드 부서 대항 서바이벌 퍼널**로 설계했다 — R1·R2는 제한 시간 내 **부서별 통과율**로 순위를 다투고(통과는 개별 카트 단위: 250 → 100 → F), R3는 **부서별 대표 카트**가 결승선 도착 순서로 최종 N명을 가린다. 부서 단위 편성을 택한 이유는 두 가지다 — 실제 조직 구도가 걸리면 자기 카트가 탈락해도 부서를 응원하게 되어 몰입이 유지되고, 동시에 **예측 대상을 5~8개로 압축해 참여형 예측 게임을 성립시킨다**(250명 중 한 명을 맞히는 것은 불가능하지만 부서 하나는 누구나 고를 수 있다).

여기서 **"부서는 순위를 다투지만 탈락은 개별로 결정된다"** 는 분리가 핵심이다. 부서를 통째로 탈락시키면 그 부서의 당첨자까지 탈락해 공정성이 깨지기 때문에, 생존은 언제나 HMAC 순위의 상위 절단이고 부서 순위는 그것을 집계한 표시·예측용 지표다. 집계는 부서 인원 차이를 보정하기 위해 **통과 수가 아니라 통과율**을 쓴다(그렇지 않으면 가장 큰 부서가 항상 이겨 예측이 무의미해진다).

**참여형 예측 게임이 기존 설계의 최대 약점을 해결한다.** R1에서 탈락한 200명이 남은 3분을 구경만 하던 구조가, 예측으로 끝까지 게임에 남는 구조로 바뀐다. 이원 화면(Stage 전체 관람 + Mobile 개인 참여), 실시간 예측 분포, 리더보드가 "재미와 긴장감" 심사 기준에 직접 대응한다.

**사내 행사에 맞춰 도박적 요소는 완전히 배제했다.** 판돈을 잃는 구조 대신 **확신도 100을 3개 라운드에 배분**하고 적중 시에만 점수를 얻는 방식(틀리면 0점, 마이너스 없음)을 쓴다. 도박의 재미 중 "이해관계·불확실성·주체성"은 유지하고 "손실"만 제거한 설계다. 여기에 **라운드 가중치 상승(×1/×1.5/×2.5)** 으로 초반을 다 틀린 참가자도 마지막 라운드에서 역전할 수 있게 해, 수학적으로 누구도 중도 탈락하지 않는다. **소수 정답 보너스**가 배당률의 스릴을 대체한다. 예측 대상은 **해당 라운드의 선택 창(30초)에서만** 고를 수 있게 해 "설정하고 잊는" 경로를 차단했고, 미선택 시에는 0점이 아니라 **무작위 배정**으로 처리해 아무도 게임에서 밀려나지 않게 했다. 부수 효과로 개발 난이도도 낮아졌다(잔액 차감·파산 처리 불필요, 채점이 순수 함수).

이 계층은 **완전히 가산적**으로 설계되어 경품 추첨의 공정성에 영향을 주지 않고(미참여자도 동일한 당첨 확률), 장애 시 Stage 화면만으로 행사가 완주된다.

레이싱 아이디어는 재미·차별화·"AI 에이전트 기반" 요구조건 충족도 모든 면에서 가장 매력적인 대표 기능이지만, 10일이라는 마감을 고려하면 단독으로 베팅하기엔 리스크가 크다. 대신 낮은 리스크의 기반(안전망 MVP + AI MC 해설)을 먼저 확실히 완성해 "무엇이 있어도 제출 가능한" 상태를 만들고, 그 위에 레이싱 모드를 타임박스를 걸고 쌓아 올리는 전략을 추천한다. 이렇게 하면 최악의 경우에도 안정성·편의성·공정성 심사 기준을 충족하는 제출물이 확보되고, 최선의 경우 사용자님이 구상한 레이싱 게임까지 완성된 형태로 제출할 수 있다.
