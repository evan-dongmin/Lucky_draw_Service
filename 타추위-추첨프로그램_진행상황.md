# 진행 상황 (다음 세션 이어하기용)

> 마지막 갱신: 2026-08-04. 이 문서는 지금까지 뭘 했고, 다음에 뭘 이어서
> 해야 하는지를 빠르게 파악하기 위한 것이다. 설계 근거·심사 전략은
> `타추위-추첨프로그램_공모전_전략노트.md`를 참고.

---

## 1. 이번 세션에서 완료된 것 (시간순)

1. **무대 화면 전체화면 개편** -- F1 스타트 라이트, 실시간 포지션 타워,
   WebAudio 효과음, AI MC 음성 해설, FINAL LAP/PHOTO FINISH 배너, 시상대+
   컨페티+팡파레. (`static/stage.html/css/js`, `static/audio.js`,
   `static/fx.js`)
2. **버그 수정**: 총 진행 시간이 Director 하한 미만이면 레이스가 조용히
   멈추는 문제 → `/api/racing/start`·`/api/demo/start`에서 사전 검증 +
   400 반환. 회귀 테스트 포함.
3. **배포 준비**: `Dockerfile`, `.dockerignore`, `render.yaml`. 실제 배포
   실행(계정 연결)은 사용자 액션 필요 -- 아직 안 함.
4. **공모전 전략 문서** 작성 (`_공모전_전략노트.md`).
5. **사이버머니 갬블링(승인됨)** -- 패리뮤추얼 정산 방식.
   - 백엔드: `app/gambling.py`(`GamblingEngine`), `/api/bet/*` 엔드포인트,
     `Session.prediction_mode`("confidence"|"gambling") 필드로 세션당
     한 모드만 활성화.
   - 프론트: 모바일 베팅 카드(잔액·실시간 배당률·재베팅·정산 결과),
     Stage 우측 실시간 배당률 패널, 정산 배너+효과음+MC 멘트, admin
     모드 선택자+승인 경고 문구.
   - Playwright로 데모 시작→베팅→정산→포디움 전체 흐름 실제 검증
     완료(콘솔 에러 0건).
6. **팀 특수능력 → 개인 캐릭터/카트 선택으로 확장**.
   - 처음엔 부서(팀) 단위로 8종 능력을 자동 배정(색상과 같은 방식)했다가,
     사용자 요청으로 참가자가 모바일 온보딩에서 **직접 고르는** 방식으로
     확장했다. 고르지 않으면 부서 기반 자동 배정으로 폴백.
   - 백엔드: `app/characters.py`, `/api/character/{roster,choose,me,
     choices}`. 캐릭터 선택은 예측/베팅 게임과 무관하게 레이싱 세션이면
     항상 가능하도록 온보딩 게이트를 `predictions_enabled`에서
     `session.mode == "racing"`으로 바꿨다.
   - 프론트: 모바일 온보딩 3단계(부서→이름→캐릭터, 스킵 가능) + 게임
     화면 "카트 변경" 오버레이. Stage는 참가자 선택을 폴링해 추월
     이펙트·포지션 타워에 반영(카트 몸체 색은 항상 부서색 유지, 능력만
     개인화).
   - Playwright로 실제 클릭 흐름 검증 중 CSS 버그 발견·수정
     (`.character-overlay`가 `.hidden`이어도 클릭을 가로채던 문제).
7. **MC 음성 생동감 개선** -- 상황 태그별 속도/피치 프로파일(`MC_ENERGY`
   맵) + 매 발화 무작위 지터 + 느낌표 문장 볼륨 강조 + 가능하면 "Google
   한국어" 같은 네트워크 기반 음성을 우선 선택(로컬 OS 음성보다 자연스러운
   경우가 많음).

**테스트**: 302개 전부 통과(회귀 없음). 신규: `test_gambling.py`(20),
`test_bet_api.py`(11), `test_racing_gambling.py`(2),
`test_character_api.py`(12), `test_predictions.py`에 3개 추가.

**모든 변경사항은 커밋·푸시 완료.** (`git log` 최근 커밋 순서: 사이버머니
갬블링 백엔드 → 갬블링 프론트+MC 생동감 → 캐릭터 백엔드 → 캐릭터
프론트엔드)

---

## 2. 다음에 이어서 할 일

### 우선순위 높음

1. **최종 통합 Playwright 검증 (전체 기능 한 번에)**. 지금까지는
   기능별로 따로 검증했다(갬블링만, 캐릭터 선택만, 스테이지만). 아직
   **모든 기능이 동시에 켜진 상태**(갬블링 모드 + 여러 참가자가 각자
   다른 캐릭터 선택 + MC 음성 + 팀 능력 폴백이 섞인 상태)로 5분 런북을
   처음부터 끝까지 한 번에 돌려본 적은 없다. 다음 세션에서 이걸 먼저
   하고, 발견되는 버그가 있으면 고칠 것.
2. **README/전략노트는 이번 세션에서 갱신 완료**했지만, 다음에 새 기능을
   더 추가한다면 마찬가지로 즉시 문서에 반영할 것(README §3, 전략노트
   §5·§6).

### 사용자 액션이 필요한 것 (내가 대신 못 함)

- 실제 배포(Render 등 계정 연결) -- `Dockerfile`/`render.yaml` 준비만 됨.
- 실제 기기·행사장 Wi-Fi로 최소 1회 리허설.
- 오늘 추가한 MC 음성·효과음을 실제 사용할 브라우저에서 들어보고 만족
  스러운지 확인(안 되면 🎙 버튼으로 끄면 자막만으로 완전히 동작).

### 여유 있으면 (P2, 전략노트에 이미 정리됨)

- 예측 게임 최종 결과 화면에 "예측왕" 발표 연출 강화(컨페티 등).
- 관리자 콘솔 런북 미리보기(구간별 시간 배분 표).
- 능력별 카트 스프라이트 색감/파티클 차별화, 능력별 다른 효과음.
- 봇(데모 자동 채움)은 캐릭터를 고르지 않는다 -- 의도된 동작(부서 기반
  폴백을 그대로 보여주기 위함)이지 버그 아님. 필요하면 봇도 무작위
  캐릭터를 고르게 확장 가능.

---

## 3. 알아두면 좋은 것

- **세션당 예측 모드는 하나만** -- `Session.prediction_mode`가
  "confidence" 아니면 "gambling"만 가능. Admin에서 "참여형 예측 게임
  활성화" 체크 후에만 모드 선택자가 보인다.
- **캐릭터 선택은 예측 게임과 독립적** -- 예측 게임이 꺼져 있어도 모바일
  온보딩(부서→이름→캐릭터)까지는 항상 가능하다. `/api/predict/me`는
  `predictions_enabled=false`일 때 `card: null`을 반환하고 모바일은
  카드/리더보드 대신 안내 문구만 보여준다.
- **갬블링/캐릭터 스냅샷 파일**: `data/gambling_snapshot.json`,
  `data/character_choices.json` (둘 다 `.gitignore`에 이미 등록됨).
  서버 재시작 시 자동 복원.
- **실행**: `python run.py` (기존과 동일). 테스트: `pytest -q`.
- **Playwright 검증 환경**: 이번 세션에서 Chromium을 로컬에 설치해 실제
  브라우저 구동 검증을 여러 번 수행했다(`npx playwright install
  chromium`). 스크래치패드에 스크린샷/영상이 남아 있지만 저장소에는
  커밋하지 않았다(용량 문제 + 요청받지 않음).
