# 타추위 추첨 프로그램 — 공모전 1등 전략 노트

> 작성 시점: 2026-08-03 (제출 마감 2026-08-07, D-4)
> 이 문서는 `타추위-추첨프로그램_기획안.md`(설계 근거)·`_작업계획서.md`(실행 계획)를
> 이어받아, **마감 직전 시점에 무엇을 더 넣어야 1등 가능성이 올라가는지**를
> 우선순위와 함께 정리한다. 동시에 사용자가 물은 "사이버머니 갬블링" 질문에
> 대한 검토 결과도 담는다.

> **2026-08-04 갱신**: 사내 승인을 받았다는 확인에 따라 §3에서 권장하지
> 않았던 사이버머니 갬블링을 실제로 구현했다. §5에 실제 설계·안전장치·
> 구현 범위를 정리했다. §3의 원래 분석(왜 신중해야 하는지)은 그대로
> 남겨둔다 -- 승인 이후에도 §5의 안전장치들은 바로 그 우려들에 대한
> 직접적인 대응이기 때문이다. 함께 물었던 "팀별 특수능력" 아이디어도
> §6에 반영했다.

> **2026-08-05 갱신 (D-2)**: 테스트 322개 전부 통과(아래 §0·§1의 254/290/
> 302는 각 시점의 정확한 스냅샷이므로 그대로 둔다). 갬블링 경제를
> 확장해 라운드 통과/우승/최종 당첨 보상과 순수 코스메틱 업그레이드
> 상점을 추가했고(잔액은 여전히 0 미만으로 절대 내려가지 않음, 오직
> 보상으로만 우상향), 레이스 트랙을 곡선(S자 스플라인)으로 바꿔 카메라가
> 커브를 따라 팬 하고 카트가 진행 방향으로 회전하며 움직이는 장애물
> 연출을 추가했다. 총 진행 시간은 사용자 요청으로 5분→10분으로,
> 무대 화면에는 장면별(대기/선택/레이스/결과 발표)로 다른 합성
> 배경음악이 새로 붙었다. **그리고 이 문서가 처음부터 지적해온 "배포
> 주소" 요건이 실제로는 막혀 있었다**: `.dockerignore`가 `requirements.txt`
> 까지 걸러내 `docker build`가 항상 실패하고 있었음(오늘 발견·수정,
> 로컬 Docker로 빌드+기동+HTTP 200 확인 완료). **이후 사용자가 실제로
> Render 배포를 완료함**: https://taachuwi-grandprix.onrender.com/
> (README에 반영됨). 자세한 최신 상태는
> `타추위-추첨프로그램_진행상황.md`를 참고.

---

## 0. 요약(TL;DR)

- 이 프로젝트는 이미 공모전이 요구한 8개 항목(연출·안정성·직관성·공정성·
  명단등록·중복제외/재추첨·PC 대응·초기화)을 전부 충족한 상태다. 남은 4일은
  **새 기능을 더 쌓기보다, 이미 만든 것을 "심사자가 실제로 느끼게" 다듬는 데**
  써야 승률이 오른다.
- 오늘 세션에서 무대 화면을 진짜 전체화면 레이싱 중계로 개편하고
  (F1 스타트 라이트·포지션 타워·효과음·AI MC 음성·컨페티/시상대), 실제로
  숨어 있던 버그 하나(총 진행 시간이 하한 미만이면 레이스가 조용히 멈추는
  문제)를 Playwright로 실제 구동해보다 발견해 고쳤다. 배포용 `Dockerfile`/
  `render.yaml`도 추가했다 — "배포 주소" 제출 요구를 채우기 위한 준비다.
- **사이버머니 갬블링(진짜 손실이 있는 배팅) 도입은 권장하지 않는다.**
  이유는 §3에 자세히 적었다. 대신 이미 있는 확신도 배분 게임의 실시간
  연출을 더 강화하는 쪽(§2 P1-1)이 같은 긴장감을 더 안전하게 만든다.

---

## 1. 현재 구현이 이미 강한 지점 (심사 기준별)

공모 공고의 선정 기준 4개에 맞춰 정리한다.

### 재미와 긴장감
- 커밋-리빌 → 3라운드 부서 대항 카트 레이싱(250 → 100 → 결선 → N) 퍼널.
- **오늘 추가**: F1 스타트 라이트, 카트 엔진음(진행률에 따라 피치 상승),
  추월 whoosh, 클로즈콜/파이널랩/포토피니시 배너, 시상대 컨페티+팡파레+
  관중 함성, AI MC 실시간 음성 해설(Web Speech API).
- 참여형 예측 게임(확신도 배분, 손실 없음) — 청중이 관전자가 아니라
  이해관계자가 되게 만드는 핵심 장치.

### 안정성 및 완성도
- 254개 자동 테스트(공정성 결정론, 위변조 탐지, 레이스-Fairness 100% 일치,
  서버 재시작 후 상태 복원, 예측게임 off 회귀 없음 등).
- **오늘 발견·수정**: 총 진행 시간이 Director 하한 미만이면 레이스가
  백그라운드에서 조용히 죽어 무대가 "커밋" 화면에서 멈춘 것처럼 보이는
  버그. 이제 `/api/racing/start`·`/api/demo/start` 둘 다 사전 검증으로
  즉시 400을 반환한다 — 행사 당일 "버튼 눌렀는데 아무 반응 없음"이라는
  최악의 시나리오를 차단했다.
- 부하 테스트 재실행 결과(오늘, 로컬 asyncio 시뮬레이션):
  WebSocket 250/250 동시 접속 성공(2.53초), 참여(join) p50 720ms/p95
  1.6초, 확신도 배분(allocate) p50 2.0초, 예측 대상 선택(choose) p50
  2.1초, **실패 0건**. 단일 프로세스 Uvicorn 기준으로도 250명 규모를
  버틴다는 근거 수치다. (주의: 이는 로컬 시뮬레이션이며 실제 250대 폰+
  행사장 Wi-Fi 지연은 다를 수 있다 — README가 이미 명시한 대로 행사 전
  실제 기기로 최소 1회 실측을 강력 권장한다.)

### 사용 편의성
- 명단 붙여넣기/CSV/XLSX 업로드/샘플 생성, 커밋→추첨→재추첨 버튼 흐름,
  비상 조기 종료, 카메라 수동 전환.
- **오늘 추가**: 총 진행 시간이 Director 하한에 못 미치면 운영 콘솔에서
  실시간으로 경고를 띄운다(버튼을 누르기 *전에* 안다).

### 공정성
- 커밋(SHA-256)-리빌 구조, `/verify`에서 브라우저가 서버를 신뢰하지 않고
  직접 재계산(WebCrypto), Python↔JS 알고리즘 바이트 단위 일치를 교차
  검증 테스트로 보증.

---

## 2. 마감(08/07)까지 우선순위별 추가 권장사항

### P0 — 반드시 (제출 전, 오늘~내일 안에)

1. **[완료]** 무대 화면 전체화면 + 이펙트/효과음/MC 음성 개편.
2. **[완료]** Director 하한 미달 시 조용히 멈추는 버그 수정 + 회귀 테스트.
3. **[완료]** 배포 설정(`Dockerfile`, `render.yaml`) 추가 + **실제 Render
   배포 완료**: https://taachuwi-grandprix.onrender.com/ (README 반영됨).
4. **[완료]** 250명 부하 테스트 재실행, 실측 수치 확보(위 §1 참조). 제출
   문서에 이 수치를 그대로 인용하면 "안정성" 항목에 대한 근거가 된다.
5. **사용자 액션 필요** — 실제 기기·행사장 Wi-Fi로 최소 1회 리허설. 이건
   개발 환경에서 대신할 수 없는 유일한 항목이다. 특히 모바일 브라우저
   자동재생 정책(오디오/음성) 차이는 실기기에서만 확인 가능하다.
6. **사용자 액션 권장** — 오늘 새로 추가한 사운드/AI MC 음성을 실제 사용할
   브라우저(주로 Chrome)에서 한 번 들어보고, 음질·볼륨·타이밍이 만족
   스러운지 확인. 마음에 안 들면 🎙 버튼으로 언제든 끄고 자막만으로도
   완전히 동작한다(정적 폴백 원칙 그대로 유지).

### P1 — 강력 추천 (시간 남으면, 임팩트 대비 비용 낮음)

1. **예측 게임에 실시간 분포/배수 노출.** 지금은 라운드 종료 후에야 배당
   배수와 적중 여부가 보인다. 기획안이 이미 강조한 "역배 노리기의 스릴"
   (`round_share`, 소수 정답 보너스)을 선택 창이 **열려 있는 동안** 모바일
   화면에 "현재 A팀 32% 선택 중 · 지금 B팀 선택 시 예상 1.6배"로 실시간
   노출하면, 확신도 배분의 긴장감이 훨씬 직접적으로 느껴진다. MC가 이미
   이런 멘트("A팀에 표가 몰립니다! 지금 B팀을 고르면 1.8배입니다")를
   하도록 기획돼 있었으니, 화면에도 같은 숫자를 보여주는 것이 자연스러운
   마무리다. (백엔드: `PredictionEngine`에 라운드 진행 중 실시간 분포
   계산 메서드 추가 + WS 브로드캐스트, 프론트: `mobile.js` 카드에 표시)
2. **최종 예측 리더보드 발표 연출 강화.** 지금은 리스트만 갱신되고 끝난다.
   레이싱 시상대처럼 "예측왕" 발표에도 작은 팡파레/컨페티를 얹으면
   예측 게임 참여자에게도 확실한 보상감을 준다.
3. **관리자 콘솔에 런북 미리보기.** 세션 생성 시 각 구간(오프닝/R1/선택창/
   R2/…) 시간 배분을 표로 미리 보여주면, 총 진행 시간을 바꿨을 때 어떻게
   재배분되는지 운영자가 감을 잡기 쉬워진다.

### P2 — 여유 있으면 (없어도 무방)

1. 라운드 종료 시 극적인 추월 장면 재생(리플레이/슬로모션) — 임팩트 대비
   개발 비용이 P1보다 높아 우선순위를 낮췄다.
2. 다국어(영어) 자막 — 해외 지사 참석자가 있는 행사라면 고려.
3. 리허설 모드(자동 재생되는 프리뷰) — 운영자 사전 연습용.

---

## 3. "사이버머니 갬블링" 질문에 대한 답

> 원 질문: "사이버 머니를 매 라운드마다 거는 갬블링도 가능하대. 그것도
> 하면 청중의 집중력과 긴장감을 더 높일 수 있는 수단이 될까?"

**결론부터: 권장하지 않는다.** 이유는 아래 네 가지다.

### (1) 이미 검토되고 의도적으로 배제된 설계였다

`타추위-추첨프로그램_기획안.md` §4에 이 트레이드오프가 이미 상세히
분석돼 있다. 원래 사용자 아이디어도 "사이버머니를 받아 라운드별로 걸고
증감"하는 방식이었지만(21번줄), 사내 행사라는 성격 때문에 **"도박적
요소(판돈을 잃는 구조)를 완전히 배제한다"**고 명시적으로 결정했고
(131번줄), 그 대신 확신도 배분(틀려도 0점, 마이너스 없음) 구조를
채택했다. 이 문서는 도박의 재미가 "① 이해관계 ② 불확실성 ③ 주체성"
세 요소에서 나온다고 분석하고, **"잃는다"는 요소만 제거해도 나머지
셋은 그대로 살릴 수 있다**는 논리로 지금의 확신도 배분·라운드 가중치·
소수 정답 보너스 구조를 설계했다(142~167번줄). 즉, 지금 물은 질문은
이미 한 번 깊게 검토됐고, 그 결론이 지금 코드에 반영돼 있다.

### (2) 실제로 얻는 긴장감의 한계 효용은 작고, 리스크는 크다

손실 회피(loss aversion)가 순수 확신도 배분보다 더 강한 감정을 유발하는
것은 행동경제학적으로 사실이다. 하지만:

- **법적/컴플라이언스 리스크**: 사이버머니가 실제 경품 당첨 여부와
  연동되는 순간(예: 갬블링에서 딴 사이버머니가 최종 당첨 확률에 영향을
  준다거나, 사이버머니 자체가 소액이라도 실물로 교환 가능해지는 순간),
  "판돈 → 실물 보상"의 실질적 배팅 구조로 해석될 여지가 생긴다. 이
  프로젝트의 심사자가 팀장님들(관리자·경영진)이라는 점까지 고려하면,
  "사내 행사에 도박 시스템을 만들었다"는 인상 자체가 오히려 감점 요인이
  될 수 있다. 기획안도 정확히 이 우려를 짚었다(323번줄: "회사 행사에서
  도박' 프레이밍 우려").
- **개발 리스크**: 잔액 관리, 파산 처리, 잔액 조작 방지, 라운드 간 정산,
  마이너스 잔액 예외 처리 등 새로운 상태 관리가 필요하다. 마감 4일 앞두고
  이미 안정화된 코드베이스에 새 버그 표면적을 늘리는 선택이며, 기획안이
  이미 짚었듯 이 복잡도를 제거한 덕분에 지금의 일정 여유가 생긴 것이다.
- **"안정성 및 완성도"·"사용 편의성" 심사 기준에 오히려 불리**: 참가자에게
  새 규칙(판돈 개념, 파산 시 처리)을 설명해야 하는 부담이 늘고, 엣지
  케이스(사이버머니 0원인 참가자를 어떻게 할지 등)가 늘어난다. "누구나
  쉽게 이해하고 사용할 수 있는" 화면이라는 심사 기준과 반대 방향이다.

### (3) 이미 같은 심리적 효과를 안전하게 얻고 있다

확신도 배분(잃을 수도 있는 판돈 대신 "100을 어떻게 나눌지"라는 선택) +
라운드 가중치 상승(후반 배점 강화) + 소수 정답 보너스(역배 스릴)는
"이해관계·불확실성·주체성" 세 요소를 모두 제공한다. 여기에 오늘 추가한
실시간 이펙트/효과음/MC 음성, 그리고 §2 P1-1(실시간 분포·배수 노출)까지
더하면, 손실 공포 없이도 몰입도를 더 끌어올릴 여지가 남아 있다. 순수
배팅이 추가로 주는 한계 효용은 이미 상당 부분 흡수된 상태라고 판단한다.

### (4) 그래도 원한다면

사용자가 검토 후에도 여전히 실제 배팅형 갬블링을 원한다면, 리스크를
낮추는 최소한의 조건은 다음과 같다:

- 사내 인사팀/법무팀의 사전 승인(사행성 관련 사내 정책 확인)이 먼저다.
- 사이버머니는 매 라운드 리셋되는 "1회용 쿠폰" 개념으로 설계해 이월
  손실이 없게 한다(누적 파산 상태를 아예 만들지 않는다).
- 갬블링 결과는 본 추첨(실물 경품 당첨자 결정)과 **완전히 분리된 별도
  트랙**으로 운영하고, 그 트랙의 보상도 실물 경품이 아닌 아주 작은
  디지털 배지 수준으로 한정한다.

이 조건들을 지켜도 여전히 상당한 개발 범위가 추가되며, 남은 4일 안에
새로 설계·구현·테스트하기엔 무리가 있다고 판단해 **이번 제출에는
포함하지 않는 것을 권장한다.**

---

## 4. 제출 체크리스트 (공모 요구사항 대조)

| 요구사항 | 상태 | 비고 |
|---|---|---|
| 추첨 결과 공개 전 긴장감 연출 | ✅ | 커밋-리빌 + 3라운드 레이싱 + 예측게임 + 오늘 추가한 이펙트/효과음/MC 음성 |
| 250명 규모 안정성 | ✅ | 254개 테스트 + 실측 부하테스트 수치(§1) |
| 직관적 화면/진행 방식 | ✅ | 무대/운영/모바일/검증 4분리, 오늘 무대 전체화면 개편 |
| 공정성 검증 구조 | ✅ | 커밋-리빌 + `/verify` 브라우저 독립 재계산 |
| 명단 등록(이름/사번/팀) | ✅ | 붙여넣기/CSV/XLSX/샘플 생성 |
| 중복 당첨 제외/재추첨 | ✅ | 제외 명단 관리 + 재추첨(이전 당첨자 제외 옵션) |
| PC/대형 스크린 실행 | ✅ | 오늘 전체화면 대응 완료 |
| 초기화·재추첨, 오류 방지 | ✅ | 전체 초기화, 비상 조기 종료, 서버 재시작 후 상태 복원 |
| 배포 주소 | ✅ | https://taachuwi-grandprix.onrender.com/ (Render.com, `Dockerfile`/`render.yaml` 기반) |
| 실제 기기·행사장 네트워크 실측 | ⚠️ 사용자 액션 필요 | 개발 환경에서 대신할 수 없음 |
| 소스 코드 저장소 | ✅ | GitHub 원격 저장소 존재 |
| 오픈소스 라이선스 목록 | ✅ | `THIRD_PARTY_NOTICES.md`, 최신 의존성 기준 확인 완료(오늘 추가한 기능은 순수 WebAudio/Canvas/Web Speech API로 신규 외부 의존성 없음) |

> **2026-08-06 갱신**: "공정성 검증 구조" 행은 여전히 ✅지만 의미가
> 미묘해졌다 — `/verify`가 검증하는 건 이제 "레이스 결과"이고, "실제
> 경품 당첨자"는 레이스 위에 얹은 예측/갬블링 리더보드로 별도 결정된다
> (사용자 명시적 요청). 이 설계 전환의 이유·구방식과의 정직한 비교는
> **§7**을 참고.

---

## 5. (2026-08-04 추가) 사이버머니 갬블링 실제 구현

승인을 받았다는 확인에 따라 구현했다. §3에서 짚었던 세 가지 리스크(법적/
컴플라이언스, 개발 복잡도, UX 혼란)를 각각 정면으로 겨냥한 설계로
대응했다.

### 어떻게 리스크를 낮췄는가

| §3에서 지적한 리스크 | 실제 대응 |
|---|---|
| 실물 경품과 뒤섞여 "판돈→경품"으로 해석될 여지 | **완전히 분리된 별도 트랙**으로 구현. 갬블링 잔액(`GamblingEngine`)은 `draw.winners`(실물 당첨자) 계산에 어떤 식으로도 관여하지 않는다 -- fairness.py는 여전히 커밋된 시드만으로 당첨자를 결정한다. 갬블링 리더보드는 완전히 별개의 사이버머니 순위표다 |
| 잔액 관리·파산 처리·정산 복잡도 | **패리뮤추얼(pari-mutuel) 정산**을 채택해 "하우스가 지급해야 할 돈"이라는 개념 자체를 없앴다 -- 그 라운드에 걸린 전체 판돈을 맞춘 사람들이 자신이 건 비율대로 나눠 가질 뿐이라, 운영자가 추가로 지급할 재원을 준비할 필요가 없다. 잔액은 구조적으로 0 이상만 가능(마이너스 불가) |
| 초반 파산 시 남은 라운드 이탈 | 라운드 2·3 개방 시 소액 보너스 칩 지급(기획안의 "누구도 수학적으로 탈락하지 않는다" 철학을 그대로 적용) |
| 참가자 동의 없는 배팅 | 확신도 배분과 달리 **자동 배팅을 하지 않는다** -- 베팅하지 않은 참가자는 그 라운드를 그냥 구경한다(잔액 변화 없음). 잃는 돈이 있는 게임에서 "미선택 시 자동 참여"는 무손실 예측 게임과는 다른 문제라고 판단했다 |
| 참가자 혼란(무손실 게임과 뒤섞임) | **선택 모드**로 구현 -- 세션당 확신도 배분(무손실) 또는 갬블링(승인됨) 중 하나만 활성화된다. 참가자에게 두 게임을 동시에 들이밀지 않는다. Admin 콘솔에서 갬블링을 고르면 승인 필요 경고 문구가 뜬다 |

### 만든 것

- `app/gambling.py` (`GamblingEngine`): 시작 잔액 500, 패리뮤추얼 정산,
  재베팅(같은 라운드 안에서 대상/금액 변경) 지원. 단위 테스트 20개
  (파산 방지, 배당률 계산, 정산 멱등성, 스냅샷 복원 등).
- `/api/bet/place`, `/api/bet/leaderboard`, `/api/predict/live`(실시간
  배당률/분포 공개 조회) 신설. 기존 `/api/predict/join`·`/api/predict/me`는
  모드에 따라 확신도 카드 또는 베팅 카드를 반환하도록 확장.
- 모바일 화면: 라운드별 베팅 카드(보유 잔액, 실시간 배당률, 베팅/재베팅,
  정산 결과 +획득/-손실 표시).
- 무대 화면: 우측에 실시간 배당률 패널(2초 폴링), 정산 시 배너+효과음+MC
  멘트("gambling_result"), 최종 발표 시 "gambling_champion" 멘트. 데모
  화면에 "🎰 갬블링 모드로 체험하기" 버튼을 별도로 추가해 심사자가 두
  모드를 한 번씩 다 눌러볼 수 있게 했다.
- 서버 재시작 시에도 잔액·베팅·정산 이력이 그대로 복원된다(기존 확신도
  배분과 동일한 스냅샷 방식).
- 백엔드 테스트 290개 전부 통과(신규 33개 포함), 회귀 없음. Playwright로
  전체 흐름(데모 시작 → 베팅 → 정산 → 포디움)을 실제 구동해 콘솔 에러
  0건 확인.

### 그래도 남아 있는 진짜 리스크

기술적으로 안전하게 만든 것과, **행사에서 실제로 틀어도 되는지**는 다른
질문이다. 다음은 이번 구현이 해결하지 못하는, 사람이 판단해야 할 부분이다.

- **승인의 범위**: "갬블링 해도 괜찮다"는 확인이 이번 공모전 제출/데모
  범위에 대한 것인지, 실제 행사(현직자 대상, 실물 경품 연계) 당일 사용
  까지 포함하는지 다시 한번 확인을 권한다. 데모·심사용으로 보여주는 것과
  실제 임직원에게 사이버머니를 잃게 하는 것은 체감 리스크가 다르다.
- **기본값은 여전히 확신도 배분**이다. 세션 생성 시 명시적으로 갬블링을
  선택해야만 활성화되므로, 실수로 행사 당일 갬블링 모드가 켜질 위험은
  낮다.
- **금액의 의미**: 사이버머니에 실물 가치가 조금이라도 결부되는 순간(예:
  "1등에게 소정의 상품") §3에서 짚은 리스크가 되살아난다. 순수 오락용
  순위표로만 남겨두길 권한다.

---

## 6. (2026-08-04 추가) 팀별 특수능력

### 왜 순수 연출로만 만들었는가

레이싱 결과(누가 통과하고 누가 당첨되는지)는 커밋 시점에 시드로 완전히
고정된다 -- 이게 이 프로젝트의 핵심 신뢰 장치(`/verify`에서 누구나
재계산 가능)다. "팀 특수능력"이 실제로 순위나 통과 여부에 영향을 준다면,
그 순간 이 신뢰 장치가 깨진다. 그래서 특수능력은 **철저히 화면 연출
전용**으로 설계했다 -- race.py의 페이스 지수(카트마다 다른 가속 곡선을
줘도 최종 순위는 그대로인 것과 완전히 같은 원리)를 그대로 이어받은
설계다.

### 만든 것

- 부서명을 정렬한 순서에 팀 색상과 똑같은 방식으로 8종 특수능력을 고정
  배정한다(니트로 부스트·배리어 실드·스파크 러시·슬립스트림·럭키
  드래프트·타이달 웨이브·스타더스트·로켓 대시). 같은 명단이면 항상 같은
  능력을 받는다.
- 팀 레전드에 능력 아이콘 표시, 추월 이펙트가 해당 팀의 능력 이모지로
  바뀌고, AI MC가 "{team}의 {ability}가 발동합니다!" 식으로 실황
  해설한다. 포지션 타워의 선두 카트에도 능력 아이콘이 붙는다.
- 서버 쪽 로직·API·fairness 계산에는 아무 변경이 없다 -- 전부 `stage.js`
  안에서 이미 공개된 부서 목록으로부터 클라이언트가 스스로 계산하는
  결정론적 값이다.

### 다음에 시간이 남으면 (P2)

- 능력별로 카트 스프라이트 색감/파티클 모양을 좀 더 차별화(지금은 이모지
  아이콘 정도의 차이).
- 능력 발동 시 소리도 능력별로 다르게(지금은 공통 whoosh 효과음).

### (2026-08-04 추가) 팀 단위 → 개인 선택으로 확장

사용자 피드백에 따라 팀 단위 자동 배정에서 한 걸음 더 나아가, 참가자가
모바일 온보딩(부서→이름 선택 다음 단계)에서 **자기 카트의 특수능력을
직접 고르도록** 확장했다. 설계 원칙은 그대로다 -- 결과에는 영향 없음,
순수 연출.

- `app/characters.py`: 캐릭터 8종 고정 목록(참가자 선택용). `stage.js`의
  `ABILITY_ROSTER`(부서 기반 폴백용)와 id·label·emoji가 정확히 같다.
- `/api/character/{roster,choose,me,choices}` 신설. 캐릭터 선택은
  예측/베팅 게임과 **무관**하게 레이싱 세션이면 항상 가능하도록,
  온보딩 진입점(`/api/predict/departments`, `/api/predict/join`)의
  게이트를 `predictions_enabled`에서 `session.mode == "racing"`으로
  바꿨다 -- 예측 게임이 꺼진 순수 레이싱 세션에서도 카트는 고를 수
  있어야 한다는 판단.
- 모바일: 온보딩 3단계(부서→이름→캐릭터, 스킵 가능) + 게임 화면에서
  언제든 재선택 가능한 "카트 변경" 오버레이.
- Stage: 참가자가 고른 캐릭터가 있으면 그 능력을 쓰고, 없으면 기존 부서
  기반 배정으로 폴백한다. **카트 몸체 색은 항상 부서색을 유지**한다(팀
  우열을 한눈에 보는 정보이므로 개인화 대상에서 제외) -- 특수능력(이모지
  이펙트·MC 문구)만 개인화된다.
- Playwright로 실제 클릭 흐름을 검증하다 CSS 버그를 하나 잡았다:
  `.character-overlay`가 `.hidden` 클래스와 함께 있어도 화면을 가리고
  클릭을 가로챘다 -- 스타일시트에서 뒤에 나온 `.character-overlay
  {display:flex}` 규칙이 앞선 `.hidden{display:none}`을 셀렉터 우선순위
  동률로 덮어썼기 때문. `.character-overlay.hidden` 규칙을 추가해
  고쳤다. 백엔드 12개 테스트 추가(전체 302개 통과).

---

## 7. (2026-08-06 추가) "당첨자 = 레이스 결과" → "당첨자 = 예측/갬블링
    최종 리더보드"로 전환 — 구방식 vs 신방식 정직한 비교

사용자가 명시적으로 요청한 방향 전환이다(요지: "예측/갬블링은 그걸
기준으로 결국 승자가 가려지기 때문에 만든 것 -- 막판까지 예측 불가하고
리더보드가 끝없이 갱신되며, 레이스 1등이 최종 승자가 아닌 반전이
핵심"). 이 문서는 지금까지 "레이스 결과 = 당첨자, 예측/갬블링은 결과와
완전히 분리된 가산적 미니게임"을 전제로 §1·§3·§5 전체를 썼다 — 그
전제가 이제 바뀌었으므로, 공모 요구사항(`타추위-추첨프로그램 설명.txt`)
의 4개 심사 기준에 각각 어떤 영향을 주는지 숨기지 않고 정리한다.

### 요구사항 원문 재확인

> "룰렛, 사다리, 카드 선택, 미니게임, 퀴즈, 캐릭터 대결, 스토리텔링
> 연출 등 **구현 방식은 자유**입니다." / 심사: 재미와 긴장감·안정성 및
> 완성도·사용 편의성·공정성.

"공정성 = 순수 무작위"라고 못박은 요구사항은 없다 — "구현 방식 자유"라는
전제 위에 "결과를 신뢰할 수 있는가"가 공정성의 정의다. 신방식은 이
정의를 **다른 방식으로** 만족시킨다(아래 참고). 즉 신방식은 요구사항
위반이 아니라 "미니게임/스토리텔링 연출"이라는 명시적으로 허용된
해석의 연장선이라고 본다 — 다만 트레이드오프는 실재하므로 그대로 적는다.

### 항목별 비교

| 심사 기준 | 구방식(레이스 결과 = 당첨자) | 신방식(예측/갬블링 리더보드 = 당첨자) |
|---|---|---|
| **재미와 긴장감** | 레이스 자체의 추월·클로즈콜·파이널랩은 긴장감 있지만, 결선 통과 순간 사실상 결과가 나옴 -- 그 뒤 발표는 이미 정해진 걸 "보여주기"일 뿐. 예측 게임은 맞히면 기분 좋은 보조 재미. | **더 강하다.** 레이스가 끝나도 안 끝났다는 반전, 마지막 라운드 채점이 반영되는 순간까지 아무도 결과를 모름, 리더보드가 라운드마다 계속 뒤집힘. "레이스에서 졌어도 예측을 잘하면 이긴다"는 이중 승부 구조. 요구사항의 "추첨 결과가 공개되기 전까지 긴장감·기대감을 높이는 연출"에 정확히 부합. |
| **안정성 및 완성도** | 이미 328개 테스트로 검증된 단순한 단일 계산 경로(커밋 시점에 승자 확정). | 코드 경로가 하나 늘었다(라운드3 최종 채점 이후 리더보드 top-N 계산, WS 브로드캐스트 순서 의존성 -- `revealed` 이후 `prize_winners`가 오는 순서를 클라이언트가 기다려야 함). **다만 레이스/공정성 핵심(`fairness.py`, 322개 기존 테스트)은 한 줄도 안 건드렸다** -- 새 로직은 순수 추가(`_compute_prize_winners`)이고 신규 테스트 6개로 별도 검증했다. 복잡도는 늘었지만 회귀 위험은 최소화한 설계. |
| **사용 편의성(진행자)** | "레이스 결과 = 당첨자"라 설명이 직관적, 혼동 요소 없음. | 진행자가 "레이스 1등이 당첨자가 아닐 수 있다"를 참가자에게 **미리 안내해야 하는 부담**이 생긴다(게임소개.md·행사 당일 안내 문구 강화로 대응). Admin 콘솔은 "실제 경품 당첨자"를 레이스 결과보다 먼저·크게 보여주도록 고쳐서, 진행자가 실물 상품을 엉뚱한 사람에게 주는 실수를 구조적으로 막았다. |
| **공정성** | **가장 강하다.** 순수 무작위, 커밋-리빌로 사전 고정+사후 검증, **참여 여부와 무관하게 등록된 모든 인원이 동일 확률**. 전통적 "추첨(lottery)"의 정의에 가장 가깝다. | 레이스 자체는 여전히 100% 커밋-리빌로 고정·검증 가능(`/verify` 무변경) -- "레이스가 조작되지 않았다"는 신뢰는 그대로 유지된다. **하지만 "누가 상품을 받는가"에는 예측/베팅이라는 스킬·참여 요소가 개입한다.** 모바일 온보딩을 안 한 사람은 리더보드에 없어 당첨 자격이 아예 없어진다(구방식은 참여와 무관하게 항상 당첨 가능했음). 이건 "무작위 확률의 공정성"에서 "투명하고 조작 불가능하지만 참여·실력에 좌우되는 공정성"으로 정의가 바뀐 것 -- 장점(더 몰입되는 참여형 구조)이자 동시에 실재하는 트레이드오프다. |

### 결론(권장 커뮤니케이션)

심사자에게는 이 트레이드오프를 숨기지 않고 정확히 설명하는 편이 낫다:
"레이스는 여전히 암호학적으로 고정되고 검증 가능한 공정한 게임이지만,
그 위에 예측/베팅이라는 두 번째 게임을 얹어서 마지막까지 결과를 알 수
없는 반전을 만들었다"가 정확한 한 줄 요약이다. 예측 게임이 꺼져 있는
세션(또는 룰렛 모드)에서는 이 전환이 전혀 적용되지 않고 구방식 그대로
동작하므로, **"순수 무작위 추첨"이 필요한 상황과 "참여형 반전 게임"이
필요한 상황 둘 다 같은 서비스로 커버**한다는 점도 함께 강조할 만하다.

---

## 8. (2026-08-06 추가) "모바일 온보딩을 안 한 참가자" 처리 방식 상세 —
    검토 요청에 대한 설명

사용자 지적: §7의 전환 이후 "모바일 온보딩을 안 한 사람은 리더보드에
없어 당첨 자격이 아예 없어진다"는 문구를 보고, **관리자가 처음에
등록한 명단(DB)에 있는 사람이면 그 자체로 이미 "참여"한 것으로 쳐야
하고, 게임 진행 중 여러 선택 단계에서 폰을 안 만졌다면 그때그때
적당한 기본값(디폴트)을 대신 골라주는 방식**이어야 하지 않냐는
문제 제기. 지시를 받기 전에 **지금 코드가 정확히 어떻게 동작하는지**
먼저 정리한다(이번 커밋에는 동작 변경 없음 -- 설명 문서만 추가).

### 쉬운 설명

지금은 참가자가 **폰으로 QR을 스캔해서 부서/이름을 최소 한 번 고르는
순간**에만 예측/갬블링 "카드"(점수판)가 생깁니다. 이 카드가 없으면
그 사람은 리더보드에 아예 안 뜨고, 라운드마다 자동으로 대신 골라주는
기능의 대상도 아니고, 갬블링 보상도 못 받습니다. 즉 **관리자가 처음에
등록한 참가자 명단과, 실제로 예측 게임에 "참여 중"으로 인식되는 사람
목록이 서로 다릅니다** -- 후자는 전자의 부분집합일 뿐입니다.

### 상세 설명 (코드 근거)

1. **카드는 명단 등록이 아니라 `/api/predict/join` 호출 시점에 생깁니다.**
   `PredictionEngine.get_or_create_card`(`app/predictions.py:81`)/
   `GamblingEngine.get_or_create_card`(`app/gambling.py:115`)가 호출돼야
   `self.cards`에 항목이 생기는데, 이 함수가 불리는 경로는 모바일
   온보딩(`/api/predict/join`)과 데모 봇 채우기(`_bot_play_open_rounds`,
   실행사에는 없음)뿐입니다. 관리자가 `/api/session`으로 명단을 올릴
   때는 예측 엔진이 그 명단을 알지도 못합니다(별도 저장소).

2. **미선택 시 자동 배정은 이미 있지만, "카드가 있는 사람 한정"입니다.**
   확신도 모드의 `lock_round(round_index, seed)`(`app/predictions.py:140`)
   는 `self.cards.values()`를 순회하면서 `target`이 비어 있는 카드에만
   시드 기반 결정론적 자동 예측(`_autopick_index`)을 배정합니다. **한
   번도 join하지 않아 카드 자체가 없는 사람은 이 순회 대상에 아예
   없습니다.** 즉 지금 있는 "미선택 시 자동 배정" 로직은 "참여는 했는데
   이번 라운드만 깜빡함"만 구제하지, "애초에 폰을 안 만짐"은 구제하지
   않습니다.

3. **갬블링 모드는 "자동 베팅 금지"가 처음부터 명시적 설계 원칙입니다.**
   `GamblingEngine.lock_round`(`app/gambling.py:163`)의 주석: *"확신도
   배분과 달리 자동 베팅은 하지 않는다 -- 참가자 동의 없이 가상의
   돈이라도 마음대로 거는 것은 예측 게임의 '미선택 시 무작위 배정'과는
   성격이 다른 문제이기 때문이다. 베팅하지 않은 참가자는 그 라운드를
   그냥 구경만 한다."* 라운드 보상(`award_round_rewards`,
   `app/gambling.py:208`)도 **카드가 이미 있는 사람에게만** 지급하고,
   카드가 없는 사람에게 새로 카드를 만들어 보상을 주지는 않습니다
   (주석: "카드가 없는(한 번도 참여하지 않은) 참가자에게는 카드를 새로
   만들지 않는다 -- 리더보드에 아무 행동도 안 한 유령 항목이 뜨는 것을
   막기 위함").

### 왜 지금까지는 이게 문제가 아니었는가

§7 전환 전(레이스 결과 = 당첨자)에는 예측/갬블링 참여 여부가 순수
부가 재미였습니다 -- 명단에 있으면 참여 의사와 무관하게 레이스에는
항상 참가하고 당첨 확률도 동일했습니다. 오늘 "리더보드 = 당첨자"로
바뀌면서 처음으로 "누가 실제 참여자로 카운트되는가"가 당첨 여부에
직결되는 문제가 됐습니다 -- 사용자가 정확히 이 지점을 짚었습니다.

### 바꾸려면 건드려야 할 지점(아직 구현 안 함 -- 다음 지시 대기)

- **카드를 언제 만들 것인가**: 레이스 시작(커밋 또는 R1 개방) 시점에
  `session.participants` 전체를 순회하며 카드를 미리 만들어두는 방법이
  가장 간단합니다(`_open_round_1` 근처 한 곳만 고치면 됨).
- **자동 배정 대상을 "카드가 있는 사람"에서 "전체 명단"으로 확장할
  것인가**: 위와 사실상 같은 효과를 `lock_round` 쪽에서 낼 수도
  있습니다.
- **확신도 모드**: 이미 있는 `_autopick_index`(시드 기반, 결정론적)를
  그대로 재사용하면 되므로 정책 결정이 거의 필요 없습니다.
- **갬블링 모드는 정책 결정이 필요합니다** -- "동의 없는 자동 베팅
  금지"라는 기존 원칙과 "전원 자동 참여"라는 새 요청이 정면으로
  마주칩니다. 최소 두 방향이 있습니다:
  - (A) 베팅 자체는 계속 안 시키되, 라운드 통과 보상(+40/+60/+300)과
    라운드 보너스 칩은 카드 유무와 무관하게 전원에게 지급 -- 그러면
    "구경만 한" 사람도 보상만으로 리더보드에 오릅니다(베팅에 따른
    손익은 0).
  - (B) 매 라운드 후보 중 하나를 시드 기반으로 자동 소액 베팅까지
    시켜서 진짜로 "참여"시킴 -- 이러면 위 3번 원칙 자체를 재검토해야
    합니다(동의 없는 베팅이 맞는지, 소액이라 괜찮은지는 판단 필요).

---

## 9. (2026-08-07) §8의 미결 과제 해소 + 베팅 모드 폐지

§8은 "전원 자동 참가를 하려면 무엇을 건드려야 하는가"를 정리해두고
**정책 결정을 기다리는 상태**로 끝났다. 그 결정이 내려졌고, 함께 게임
전체의 성격도 바뀌었다. 요지는 세 가지다.

### 9-1. 갬블링 모드를 없앴다 — §8의 정책 딜레마가 소멸

§8은 "갬블링 모드는 정책 결정이 필요하다"며 (A) 자동 베팅은 안 하되
보상만 전원 지급, (B) 시드 기반 소액 자동 베팅 두 갈래를 제시했다.
사용자가 실제로 갬블링 모드를 돌려본 뒤 **모드 자체를 폐지**하기로
결정하면서 이 딜레마는 선택할 필요가 없어졌다. 폐지 근거(코드로 확인):

1. 패리뮤추얼은 걷힌 판돈만 재분배 → **참가자 전체 잔액 총량이 불변**
2. 정답 후보가 넓어(R1은 부서 8개 중 2개) 승자가 많으면 **배당 1.5~3배**
3. 20초 안에 "대상 + 금액" **두 번을 결정**해야 함 → 인지 부하
4. `lock_round`가 자동 베팅을 안 해 **미참여 시 라운드가 통째로 무효**

"소수가 크게 딴다"도 "다수가 조금씩 받는다"도 아니었다. 사내 행사에서
도박 메커니즘을 쓰는 컴플라이언스 부담까지 고려해, 잃는 요소가 전혀
없는 **예측 게임 하나로 통일**했다. `app/gambling.py`·`/api/bet/*`·
`Session.prediction_mode`는 전부 삭제됐고, UI 문구에서도 베팅/판돈/
배당/사이버머니가 사라졌다(§8 체크리스트의 "도박 용어 미사용" 항목이
이제 구조적으로 보장된다).

### 9-2. §7 트레이드오프 표의 최대 약점이 사라졌다

§7 비교표에서 신방식의 가장 아픈 지점은 이것이었다:

> "모바일 온보딩을 안 한 사람은 리더보드에 없어 **당첨 자격이 아예
> 없어진다**(구방식은 참여와 무관하게 항상 당첨 가능했음)."

이제 **커밋 시점에 명단 전원의 예측 카드가 만들어지고**
(`PredictionEngine.enroll_all`), 미선택자는 자동 배정된다 — **R1·R2는
자기 소속 부서**, R3는 시드 파생 무작위. §8이 "가장 간단하다"고 짚었던
`_open_round_1` 근처 한 곳 수정이 실제 채택된 방법이다.

효과를 정확히 말하면 이렇다:

| | 구방식(순수 추첨) | §7 직후 | 지금 |
|---|---|---|---|
| 미참여자 당첨 가능성 | 참여자와 동일 | **0 (완전 배제)** | **있음**(자기 부서 자동 예측 + 성과 점수) |
| 참여자 이점 | 없음 | 참여해야만 자격 | 부서 직접 선택 · 확신도 몰아쓰기 · R3 직접 선택 |

즉 **"참여는 유리하게, 미참여는 불리하되 배제되지 않게"** 로 착지했다.
심사 관점에서 "추첨인데 특정 인원이 구조적으로 배제된다"는 가장 날카로운
반론이 사라졌고, 그 대가로 "완전 균등 확률"은 여전히 포기한 상태다 —
이건 참여형 구조를 택한 이상 불가피한 트레이드오프이며 §7의 판단을 유지한다.

### 9-3. 채점을 이진에서 순위 차등으로 바꿨다

같은 뿌리의 문제였다. 기존에는 "통과율 상위 2개 부서에 들었나"만 봐서
R1에서 대다수가 0점을 받았고, 첫 라운드에 빗나간 사람은 남은 20분 동안
할 일이 없었다. 지금은 고른 대상이 **몇 등이었는지에 따라 계단식**으로
점수가 들어간다(1위 100% → 6위 이하 5%). 누구도 0점이 아니되 1위 적중과
꼴찌 사이 20배 격차로 변별력은 유지된다.

여기에 **R3 몰아주기(올인)** 를 열어(하한 없음, 가중치 3.0) 마지막까지
리더보드가 확정되지 않도록 했다 — §7이 이 전환의 핵심 가치로 내세운
"막판까지 결과를 알 수 없는 긴장"을, 이제 **참가자가 스스로 만들 수
있는 선택**으로 바꿔놓은 것이다.

> 규칙 상세와 상수 위치는 `타추위-추첨프로그램_게임구조.md` §4~5,
> 구현 경위는 `타추위-추첨프로그램_진행상황.md` §1의 32~35번 참고.
